services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ffpromo
      POSTGRES_PASSWORD: ffpromo
      POSTGRES_DB: ffpromo
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./docker/postgres/init-kotlin-db.sql:/docker-entrypoint-initdb.d/02-init-kotlin-db.sql:ro

  kotlin-worker:
    profiles: [kotlin]
    image: gradle:8.14.3-jdk21
    working_dir: /workspace/kotlin
    volumes:
      - .:/workspace
    depends_on:
      - postgres
      - temporal
    environment:
      DATABASE_URL: postgresql://ffpromo:ffpromo@postgres:5432/ffpromo_kotlin
      TEMPORAL_ADDRESS: temporal:7233
      TEMPORAL_TASK_QUEUE: promotion
    command: ["./gradlew", ":worker:run", "--no-daemon"]

  temporal:
    image: temporalio/admin-tools:1.27.2
    entrypoint:
      - temporal
      - server
      - start-dev
      - --ip
      - 0.0.0.0
      - --db-filename
      - /tmp/temporal.db
    ports:
      - "7233:7233"
      - "8233:8233"

  prometheus:
    image: prom/prometheus:v2.55.1
    profiles: [prometheus]
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro

  web:
    profiles: [dev]
    image: node:24-bookworm-slim
    working_dir: /workspace
    volumes:
      - .:/workspace
    ports:
      - "3001:3001"
    environment:
      API_URL: http://host.docker.internal:3000
      NEXT_PUBLIC_API_URL: /api/ff-promo
      NEXT_PUBLIC_DASHBOARD_ACTOR_ID: dashboard
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command: >
      sh -c "corepack enable pnpm &&
      pnpm install &&
      pnpm --filter @ff-promo/web dev --hostname 0.0.0.0"

volumes:
  pgdata:
