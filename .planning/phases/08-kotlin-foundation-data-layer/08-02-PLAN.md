---
phase: 08-kotlin-foundation-data-layer
plan: 02
type: execute
wave: 1
depends_on:
  - "08-01"
files_modified:
  - kotlin/modules/db/build.gradle.kts
  - kotlin/modules/db/src/main/resources/db/migration/V1__init.sql
  - kotlin/modules/db/src/main/resources/db/migration/V2__pipeline_config_audit.sql
  - kotlin/modules/db/src/main/kotlin/com/ffpromo/db/tables/Tables.kt
  - kotlin/modules/db/src/main/kotlin/com/ffpromo/db/DatabaseFactory.kt
  - kotlin/modules/db/src/main/kotlin/com/ffpromo/db/IdGenerator.kt
  - kotlin/modules/db/src/test/kotlin/com/ffpromo/db/TestDatabase.kt
  - kotlin/modules/db/src/test/kotlin/com/ffpromo/db/MigrationSmokeTest.kt
autonomous: true
requirements:
  - KOT-03
user_setup:
  - service: docker
    why: "Testcontainers Postgres for migration smoke tests"
    dashboard_config:
      - task: "Ensure Docker is running for ./gradlew :db:test"
        location: "Local machine"
must_haves:
  truths:
    - "Flyway V1 and V2 migrations apply cleanly to empty Postgres"
    - "Exposed Table objects map quoted camelCase columns matching Prisma migration SQL"
    - "DatabaseFactory runs Flyway migrate before Exposed Database.connect"
    - "IdGenerator produces string IDs compatible with v1 cuid-style primary keys"
  artifacts:
    - path: "kotlin/modules/db/src/main/resources/db/migration/V1__init.sql"
      provides: "Port of Prisma init migration"
      contains: "CREATE TABLE \"Pipeline\""
    - path: "kotlin/modules/db/src/main/kotlin/com/ffpromo/db/DatabaseFactory.kt"
      provides: "Flyway-first bootstrap"
      contains: "Flyway.configure"
    - path: "kotlin/modules/db/src/main/kotlin/com/ffpromo/db/tables/Tables.kt"
      provides: "Exposed table definitions"
      contains: "object Pipelines"
  key_links:
    - from: "kotlin/modules/db/src/main/kotlin/com/ffpromo/db/DatabaseFactory.kt"
      to: "db/migration"
      via: "Flyway locations classpath:db/migration"
      pattern: "migrate()"
    - from: "packages/db/prisma/migrations"
      to: "V1__init.sql"
      via: "SQL port reference"
      pattern: "20260622052811_init"
---

<objective>
Port v1 Prisma DDL to Flyway migrations and Exposed table definitions, with a Flyway-first database bootstrap and Testcontainers smoke tests.

Purpose: KOT-03 requires PostgreSQL schema parity with v1; Flyway owns DDL while Exposed maps existing tables (no SchemaUtils.create in prod path).
Output: Migrated empty Postgres with all 7 models; DatabaseFactory usable from repositories and tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-kotlin-foundation-data-layer/08-RESEARCH.md
@.planning/phases/08-kotlin-foundation-data-layer/08-PATTERNS.md
@.planning/phases/08-kotlin-foundation-data-layer/08-01-SUMMARY.md
@packages/db/prisma/schema.prisma
@packages/db/prisma/migrations/20260622052811_init/migration.sql
@packages/db/prisma/migrations/20260622222931_add_pipeline_config_audit/migration.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flyway SQL migrations from Prisma</name>
  <read_first>
    - packages/db/prisma/migrations/20260622052811_init/migration.sql — full DDL
    - packages/db/prisma/migrations/20260622222931_add_pipeline_config_audit/migration.sql
    - .planning/phases/08-kotlin-foundation-data-layer/08-RESEARCH.md — Schema Port Checklist
  </read_first>
  <files>kotlin/modules/db/src/main/resources/db/migration/V1__init.sql, kotlin/modules/db/src/main/resources/db/migration/V2__pipeline_config_audit.sql</files>
  <action>
    Copy Prisma migration SQL verbatim into Flyway files V1__init.sql and V2__pipeline_config_audit.sql. Preserve quoted identifiers (`"Pipeline"`, `"flagKey"`), native Postgres enums, indexes, and FK constraints.

    Do NOT reference Prisma migration history table. Document in db module README snippet that v2 schema changes go only through Flyway going forward.

    Add db module dependencies: exposed-core, exposed-jdbc, exposed-java-time, exposed-json (if JSONB), flyway-core, flyway-database-postgresql, postgresql, hikariCP.
  </action>
  <acceptance_criteria>
    - V1 creates Pipeline, Stage, GatePolicy, PromotionRun, GateResult, AuditEvent
    - V2 creates PipelineConfigAudit with PipelineConfigAction enum
    - SQL diff against Prisma sources is whitespace-only at most
  </acceptance_criteria>
  <verify>
    <automated>cd kotlin && ./gradlew :db:compileKotlin</automated>
  </verify>
  <done>Flyway migration files match v1 Prisma DDL.</done>
</task>

<task type="auto">
  <name>Task 2: Exposed tables and DatabaseFactory</name>
  <read_first>
    - .planning/phases/08-kotlin-foundation-data-layer/08-RESEARCH.md — Pattern 2, Exposed table sketch
    - .planning/phases/08-kotlin-foundation-data-layer/08-PATTERNS.md — Flyway+Exposed section
    - packages/db/prisma/schema.prisma — field types and relations
  </read_first>
  <files>kotlin/modules/db/src/main/kotlin/com/ffpromo/db/tables/Tables.kt, kotlin/modules/db/src/main/kotlin/com/ffpromo/db/DatabaseFactory.kt, kotlin/modules/db/src/main/kotlin/com/ffpromo/db/IdGenerator.kt</files>
  <action>
    Define Exposed `Table` objects for Pipeline, Stage, GatePolicy, PromotionRun, GateResult, AuditEvent, PipelineConfigAudit. Use explicit table names and column names matching migration SQL (quoted camelCase via Exposed column name strings).

    Map Postgres enums to Kotlin enum classes or varchar columns consistent with SQL.

    Implement `IdGenerator.newId(): String` — use a maintained CUID2 library or compatible generator; centralize all repository inserts on this.

    Implement `DatabaseFactory.connect(jdbcUrl, user, password)`:
    1. Create HikariDataSource
    2. Run Flyway migrate on classpath:db/migration
    3. Database.connect(dataSource)
    Provide `close()` for tests.

    Implement `DatabaseFactory.withTransaction { block }` helper wrapping Exposed `transaction`.
  </action>
  <acceptance_criteria>
    - All seven entities have Table objects
    - DatabaseFactory migrates then connects
    - IdGenerator used in at least one smoke insert test
  </acceptance_criteria>
  <verify>
    <automated>cd kotlin && ./gradlew :db:compileKotlin</automated>
  </verify>
  <done>Exposed maps migrated schema; bootstrap API ready for repositories.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testcontainers migration smoke test</name>
  <read_first>
    - packages/db/src/__tests__/setup.ts — v1 testcontainers pattern
    - .planning/phases/08-kotlin-foundation-data-layer/08-RESEARCH.md — Pitfall 4 dual migrate
  </read_first>
  <files>kotlin/modules/db/src/test/kotlin/com/ffpromo/db/TestDatabase.kt, kotlin/modules/db/src/test/kotlin/com/ffpromo/db/MigrationSmokeTest.kt, kotlin/modules/db/build.gradle.kts</files>
  <behavior>
    - Test: Flyway applies V1+V2 on empty Postgres container without error
    - Test: After migrate, can insert Pipeline row with generated id and read back flagKey
    - Test: Second DatabaseFactory.connect on same DB is idempotent (migrate already applied)
  </behavior>
  <action>
    Add test dependencies: testcontainers-postgresql, kotest or junit-jupiter.

    Implement TestDatabase.start(): starts PostgreSQL container, returns jdbcUrl/user/password; calls DatabaseFactory.connect.

    MigrationSmokeTest uses @BeforeAll/@AfterAll lifecycle (or Kotest extensions) to start/stop container.

    Support env SKIP_TESTCONTAINERS=1 with DATABASE_URL fallback pointing at local docker compose postgres (mirror v1 setup.ts behavior).

    Document in TestDatabase KDoc: prefer dedicated DB `ffpromo_kotlin` for local dev to avoid Prisma/Flyway history conflicts, OR baselineOnMigrate if sharing v1 DB.
  </action>
  <acceptance_criteria>
    - ./gradlew :db:test passes with Docker available
    - Smoke test proves schema matches insert expectations
  </acceptance_criteria>
  <verify>
    <automated>cd kotlin && ./gradlew :db:test</automated>
  </verify>
  <done>Automated proof that Flyway+Exposed stack works against real Postgres.</done>
</task>

</tasks>

<threat_model>
## Threat Model: Phase 8 Plan 02

### Assets
- PostgreSQL schema integrity (KOT-03)
- Migration history consistency

### Threats
| Threat | Likelihood | Impact | Mitigation |
|--------|------------|--------|------------|
| Prisma/Flyway drift over time | Medium | High | Verbatim SQL port; document Flyway-only rule for v2 |
| Wrong column names in Exposed | Medium | High | Smoke insert/read test per entity in plan 03 |
| Dual migration on same DB | Medium | Medium | Document ffpromo_kotlin DB; Testcontainers ephemeral DB |
| ID format mismatch with v1 data | Low | Medium | IdGenerator centralization; length/format test |

### Out of Scope
- Repository business logic
- Production connection secrets management
</threat_model>

<verification>
## Plan 02 Verification

```bash
cd kotlin && ./gradlew :db:test
# Optional local without testcontainers:
SKIP_TESTCONTAINERS=1 DATABASE_URL=postgresql://ffpromo:ffpromo@localhost:5432/ffpromo_kotlin ./gradlew :db:test
```
</verification>
