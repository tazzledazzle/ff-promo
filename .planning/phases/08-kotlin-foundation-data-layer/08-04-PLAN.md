---
phase: 08-kotlin-foundation-data-layer
plan: 04
type: execute
wave: 3
depends_on:
  - "08-03"
files_modified:
  - kotlin/modules/worker/build.gradle.kts
  - kotlin/modules/worker/src/main/kotlin/com/ffpromo/worker/WorkerMain.kt
  - kotlin/modules/worker/src/main/kotlin/com/ffpromo/worker/workflows/PromotionWorkflow.kt
  - kotlin/modules/worker/src/main/kotlin/com/ffpromo/worker/workflows/PromotionSignals.kt
  - kotlin/modules/worker/src/main/kotlin/com/ffpromo/worker/activities/PersistRunStateActivity.kt
  - kotlin/modules/worker/src/main/kotlin/com/ffpromo/worker/activities/RecordAuditEventActivity.kt
  - kotlin/modules/worker/src/main/kotlin/com/ffpromo/worker/activities/EvaluateGateActivity.kt
  - kotlin/modules/worker/src/test/kotlin/com/ffpromo/worker/PromotionWorkflowTest.kt
autonomous: true
requirements:
  - KOT-04
  - SAFE-01
user_setup: []
must_haves:
  truths:
    - "Worker registers promotion workflow on task queue promotion matching v1"
    - "Workflow loops stages with stub gate evaluation returning pass"
    - "pause, resume, abort signals defined and handled in workflow skeleton"
    - "Activities bridge to db repositories — workflow code does not import JDBC/Exposed"
    - "workflowId equals promotionRun.id for correlation per D-07"
  artifacts:
    - path: "kotlin/modules/worker/src/main/kotlin/com/ffpromo/worker/WorkerMain.kt"
      provides: "Temporal worker bootstrap"
      contains: "promotion"
    - path: "kotlin/modules/worker/src/main/kotlin/com/ffpromo/worker/workflows/PromotionWorkflow.kt"
      provides: "FSM skeleton"
      contains: "PromotionWorkflow"
    - path: "kotlin/modules/worker/src/main/kotlin/com/ffpromo/worker/activities/EvaluateGateActivity.kt"
      provides: "Stub gate activity"
      contains: "pass"
  key_links:
    - from: "kotlin/modules/worker/src/main/kotlin/com/ffpromo/worker/workflows/PromotionWorkflow.kt"
      to: "PersistRunStateActivity"
      via: "ActivityStub"
      pattern: "PersistRunState"
    - from: "apps/worker/src/worker.ts"
      to: "WorkerMain.kt"
      via: "task queue parity"
      pattern: "promotion"
---

<objective>
Wire Temporal promotion workflow FSM skeleton with signals, stub activities bridging to Kotlin repositories, and worker bootstrap — enabling durable promotion runs tied to persisted Postgres records.

Purpose: KOT-04 requires Kotlin worker process against dev Temporal; mirrors v1 Phase 1 plan 04 skeleton before Phase 11 fleshes out real gate/LD logic.
Output: Runnable `./gradlew :worker:run`; workflow tests pass with TestWorkflowEnvironment.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-kotlin-foundation-data-layer/08-RESEARCH.md
@.planning/phases/08-kotlin-foundation-data-layer/08-PATTERNS.md
@.planning/phases/08-kotlin-foundation-data-layer/08-03-SUMMARY.md
@apps/worker/src/worker.ts
@apps/worker/src/workflows/promotion.workflow.ts
@apps/worker/src/workflows/signals.ts
@apps/worker/src/activities/persist-run-state.ts
@apps/worker/src/activities/record-audit-event.ts
@apps/worker/src/activities/evaluate-gate.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stub activities bridging to repositories</name>
  <read_first>
    - apps/worker/src/activities/persist-run-state.ts
    - apps/worker/src/activities/record-audit-event.ts
    - apps/worker/src/activities/evaluate-gate.ts
    - kotlin/modules/db/src/main/kotlin/com/ffpromo/db/repositories/RepositoryFactory.kt
  </read_first>
  <files>kotlin/modules/worker/src/main/kotlin/com/ffpromo/worker/activities/PersistRunStateActivity.kt, kotlin/modules/worker/src/main/kotlin/com/ffpromo/worker/activities/RecordAuditEventActivity.kt, kotlin/modules/worker/src/main/kotlin/com/ffpromo/worker/activities/EvaluateGateActivity.kt</files>
  <action>
    Add worker dependencies: temporal-sdk, temporal-kotlin, project(":db"), project(":contracts").

    Implement @ActivityInterface + Kotlin impl classes:

    PersistRunStateActivity: Accept PersistRunStateInput; connect DatabaseFactory from DATABASE_URL env; call PromotionRunRepository.updateState; set temporalWorkflowId to promotionRunId on first active transition per D-07; close connection in finally.

    RecordAuditEventActivity: Validate input; AuditRepository.append milestone actions only; include gateResultId when action is gate_evaluated.

    EvaluateGateActivity: Stub — always pass; create GateResult via GateResultRepository with error_rate mock values matching v1 stub metadata `{ stub: true }`.

    Activities MUST NOT be referenced from workflow implementation classpath with db JDBC — use interfaces only in workflow module package separation if needed (single worker module acceptable if workflow only uses ActivityStub interfaces).

    Read DATABASE_URL, TEMPORAL_ADDRESS (default localhost:7233), TEMPORAL_TASK_QUEUE (default promotion) from environment.
  </action>
  <acceptance_criteria>
    - Three activities implement v1 stub contracts
    - evaluateGate always returns pass with gateResultId
    - Activities use RepositoryFactory, not raw SQL in workflow
  </acceptance_criteria>
  <verify>
    <automated>cd kotlin && ./gradlew :worker:compileKotlin</automated>
  </verify>
  <done>Activity implementations ready for worker registration.</done>
</task>

<task type="auto">
  <name>Task 2: Promotion workflow and signals</name>
  <read_first>
    - apps/worker/src/workflows/promotion.workflow.ts — stage loop, signal handlers
    - apps/worker/src/workflows/signals.ts
    - .planning/phases/08-kotlin-foundation-data-layer/08-RESEARCH.md — Pattern 4 Temporal shell
  </read_first>
  <files>kotlin/modules/worker/src/main/kotlin/com/ffpromo/worker/workflows/PromotionWorkflow.kt, kotlin/modules/worker/src/main/kotlin/com/ffpromo/worker/workflows/PromotionSignals.kt, kotlin/modules/worker/src/test/kotlin/com/ffpromo/worker/PromotionWorkflowTest.kt</files>
  <action>
    Define @WorkflowInterface PromotionWorkflow with run(promotionRunId, pipelineId, flagKey) method.

    Define signals: pause, resume, abort (match v1 signal names).

    PromotionWorkflowImpl:
    - Loop through stages (load stage count via activity or input — stub with fixed 3 stages acceptable for Phase 8)
    - Call evaluateGate stub activity per stage
    - On pass: persist run state + audit milestones via activities
    - Handle pause/resume/abort signals with Workflow.await pattern (temporal-kotlin Async helpers)

    Write PromotionWorkflowTest using Temporal TestWorkflowEnvironment:
    - Workflow completes stage loop when gates pass
    - abort signal stops workflow
    - pause/resume signal handling smoke test

    Do NOT import Exposed/JDBC in workflow class file.
  </action>
  <acceptance_criteria>
    - Workflow interface + impl compile
    - TestWorkflowEnvironment tests pass
    - Signal names match v1
  </acceptance_criteria>
  <verify>
    <automated>cd kotlin && ./gradlew :worker:test</automated>
  </verify>
  <done>Workflow skeleton with signals tested in-memory.</done>
</task>

<task type="auto">
  <name>Task 3: WorkerMain bootstrap</name>
  <read_first>
    - apps/worker/src/worker.ts — Worker.create, taskQueue, workflowsPath
    - https://docs.temporal.io/develop/java/workers/run-worker-process
  </read_first>
  <files>kotlin/modules/worker/src/main/kotlin/com/ffpromo/worker/WorkerMain.kt, kotlin/modules/worker/build.gradle.kts</files>
  <action>
    Implement WorkerMain.kt:
    - WorkflowServiceStubs from TEMPORAL_ADDRESS
    - WorkerFactory.newInstance
    - newWorker(TEMPORAL_TASK_QUEUE) default "promotion"
    - registerWorkflowImplementationTypes(PromotionWorkflowImpl::class.java)
    - registerActivitiesImplementations(activity instances with env-based DatabaseFactory)
    - factory.start() blocking main

    Add application plugin with mainClass = com.ffpromo.worker.WorkerMainKt; `./gradlew :worker:run` task.

    Log startup: queue name, temporal address, database host (redact password).
  </action>
  <acceptance_criteria>
    - ./gradlew :worker:run starts without crash when temporal + postgres available
    - Task queue is "promotion"
  </acceptance_criteria>
  <verify>
    <automated>cd kotlin && ./gradlew :worker:build</automated>
  </verify>
  <done>Kotlin worker process runnable against docker compose stack.</done>
</task>

</tasks>

<threat_model>
## Threat Model: Phase 8 Plan 04

### Assets
- Temporal workflow determinism
- DB credentials in worker process

### Threats
| Threat | Likelihood | Impact | Mitigation |
|--------|------------|--------|------------|
| JDBC in workflow classpath breaks sandbox | Medium | High | Gradle separation; workflow uses ActivityStub only |
| Non-deterministic workflow code | Medium | High | No random/time in workflow; use Workflow.currentTimeMillis |
| DATABASE_URL leaked in logs | Low | Medium | Redact password in startup logs |

### Out of Scope
- Real LaunchDarkly/Prometheus activities (Phases 9–11)
- Production Temporal mTLS
</threat_model>

<verification>
## Plan 04 Verification

```bash
cd kotlin && ./gradlew :worker:test :worker:build
# Manual: docker compose up -d postgres temporal
DATABASE_URL=postgresql://ffpromo:ffpromo@localhost:5432/ffpromo_kotlin \
TEMPORAL_ADDRESS=localhost:7233 \
./gradlew :worker:run
```
</verification>
