---
phase: 01-foundation-data-layer
plan: 04
type: execute
wave: 3
depends_on:
  - "01-03"
files_modified:
  - apps/worker/src/worker.ts
  - apps/worker/src/workflows/signals.ts
  - apps/worker/src/workflows/promotion.workflow.ts
  - apps/worker/src/activities/index.ts
  - apps/worker/src/activities/persist-run-state.ts
  - apps/worker/src/activities/record-audit-event.ts
  - apps/worker/src/activities/evaluate-gate.ts
  - apps/worker/src/__tests__/promotion.workflow.test.ts
  - apps/worker/src/__tests__/promotion.signals.test.ts
  - apps/worker/package.json
autonomous: true
requirements:
  - SAFE-01
must_haves:
  truths:
    - "Temporal worker registers promotion workflow on task queue promotion"
    - "Workflow loops through environment stages with stub gate evaluation returning pass"
    - "pause, resume, abort, gatePassed, gateFailed signals are defined and handled"
    - "Workflow persists run state and audit milestones via activities — never imports Prisma directly"
    - "workflowId equals promotionRun.id for correlation per D-07"
  artifacts:
    - path: "apps/worker/src/workflows/promotion.workflow.ts"
      provides: "FSM skeleton per D-09"
      contains: "promotionWorkflow"
    - path: "apps/worker/src/workflows/signals.ts"
      provides: "Standard signals per D-10"
      contains: "pauseSignal"
    - path: "apps/worker/src/activities/evaluate-gate.ts"
      provides: "Stub gate activity per D-11"
      contains: "stub"
  key_links:
    - from: "apps/worker/src/workflows/promotion.workflow.ts"
      to: "apps/worker/src/activities/persist-run-state.ts"
      via: "proxyActivities"
      pattern: "proxyActivities"
    - from: "apps/worker/src/activities/record-audit-event.ts"
      to: "packages/db/src/repositories/audit.repository.ts"
      via: "AuditRepository.append"
      pattern: "append"
---

<objective>
Wire the Temporal promotion workflow FSM skeleton with pre-defined signals, stub activities bridging to @ff-promo/db repositories, and worker bootstrap — enabling durable promotion runs tied to persisted Postgres records.

Purpose: D-09/D-10/D-11 establish execution skeleton Phase 4 will flesh out; D-07 requires activities as the only DB bridge; SAFE-01 audit milestones written at transitions.
Output: Runnable worker process; promotion workflow completing stage loop in tests; signal handlers for pause/resume/abort.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-layer/01-CONTEXT.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer/01-PATTERNS.md
@.planning/phases/01-foundation-data-layer/01-03-SUMMARY.md
@packages/db/src/index.ts
@packages/contracts/src/audit.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stub activities bridging to repositories</name>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md — Stub evaluateGate Activity and Pattern 3
    - .planning/phases/01-foundation-data-layer/01-PATTERNS.md — activities/*.ts section
    - .planning/phases/01-foundation-data-layer/01-CONTEXT.md — D-07, D-11
    - packages/db/src/repositories/index.ts
  </read_first>
  <files>apps/worker/src/activities/persist-run-state.ts, apps/worker/src/activities/record-audit-event.ts, apps/worker/src/activities/evaluate-gate.ts, apps/worker/src/activities/index.ts</files>
  <action>
    Implement three stub activities per D-11 that use @ff-promo/db repositories (NOT direct Prisma in workflow):

    persist-run-state.ts: Accept PersistRunStateInputSchema input. Create PrismaClient via createPrismaClient(process.env.DATABASE_URL). Call PromotionRunRepository.updateState. On first active transition set temporalWorkflowId to promotionRunId per D-07. Close/disconnect client in finally block.

    record-audit-event.ts: Validate AuditEventInputSchema. Call AuditRepository.append with milestone action only per D-01. Include rich metadata JSON for stage context per D-03. When action is gate_evaluated include gateResultId per D-08.

    evaluate-gate.ts: Stub per D-11 — always returns pass verdict. Create GateResult via GateResultRepository with metricType error_rate, mock observedValue 0.001, threshold 0.01, metadata { stub: true, message: 'Phase 1 mock pass' }. Return { verdict: 'pass', gateResultId }.

    Export all three from activities/index.ts. Activities may import @ff-promo/db and @ff-promo/contracts. Do NOT import LaunchDarkly or Prometheus — out of scope.

    Use DATABASE_URL from environment; document in activity file that local dev requires docker compose postgres running.
  </action>
  <acceptance_criteria>
    - Three activities exported: persistRunState, recordAuditEvent, evaluateGate
    - evaluateGate always returns pass with gateResultId per D-11 stub contract
    - record-audit-event validates with AuditEventInputSchema before append
    - persist-run-state sets temporalWorkflowId equal to promotionRunId on first active write per D-07
    - No LaunchDarkly or Prometheus imports
  </acceptance_criteria>
  <verify>
    <automated>pnpm turbo run build --filter=@ff-promo/worker</automated>
  </verify>
  <done>Stub activities compile and bridge workflow to Postgres repositories.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promotion workflow FSM with signals</name>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md — Pattern 3 (Temporal FSM Skeleton) lines 443-593
    - .planning/phases/01-foundation-data-layer/01-PATTERNS.md — promotion.workflow.ts section
    - .planning/phases/01-foundation-data-layer/01-CONTEXT.md — D-09, D-10
  </read_first>
  <files>apps/worker/src/workflows/signals.ts, apps/worker/src/workflows/promotion.workflow.ts</files>
  <behavior>
    - Workflow imports only @temporalio/workflow and import type for activities — no @ff-promo/db
    - Stage loop calls evaluateGate stub and advances currentStageIndex on pass
    - persistRunState called BEFORE incrementing currentStageIndex per D-07 write ordering
    - pauseSignal sets isPaused true and records run_paused audit milestone
    - abortSignal sets status aborted and exits loop
  </behavior>
  <action>
    Create signals.ts per D-10: defineSignal for pause, resume, abort; gatePassedSignal with { stageIndex: number }; gateFailedSignal with { stageIndex, reason }; defineQuery status returning { status, currentStageIndex, isPaused }.

    Implement promotionWorkflow(input: { promotionRunId, stageCount, actor }) per RESEARCH Pattern 3 FSM:
    - proxyActivities with 30s timeout, 3 retry attempts
    - setHandler for each signal; persist state via activities inside handlers
    - On start: persistRunState active + recordAuditEvent run_started
    - while loop: await wf.condition(() => !isPaused); stage_entered audit; evaluateGate stub; on fail pause; persist before advancing index; stage_advanced audit with gateResultId
    - On completion: persistRunState completed + run_completed audit
    - gatePassed/gateFailed signal handlers for future Phase 4 gate integration

    CRITICAL sandbox rule: workflow files import ONLY @temporalio/workflow and `import type * as activities` — never Prisma, pg, fs, or @ff-promo/db.

    Export promotionWorkflow as named export for worker registration.
  </action>
  <acceptance_criteria>
    - All five signals defined per D-10: pause, resume, abort, gatePassed, gateFailed
    - Workflow uses proxyActivities for all DB I/O per D-09
    - State persisted before advancing currentStageIndex per D-07 split-brain prevention
    - No forbidden imports in workflows/ directory
    - statusQuery returns current workflow state
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run apps/worker/src/__tests__/promotion.workflow.test.ts apps/worker/src/__tests__/promotion.signals.test.ts 2>/dev/null || pnpm turbo run build --filter=@ff-promo/worker</automated>
  </verify>
  <done>FSM workflow with signal handlers implemented; sandbox isolation maintained.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Worker bootstrap and Temporal integration tests</name>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md — Temporal Integration Test Skeleton
    - .planning/phases/01-foundation-data-layer/01-PATTERNS.md — worker.ts section
    - .planning/phases/01-foundation-data-layer/01-VALIDATION.md — D-09, D-10 test map
  </read_first>
  <files>apps/worker/src/worker.ts, apps/worker/src/__tests__/promotion.workflow.test.ts, apps/worker/src/__tests__/promotion.signals.test.ts, apps/worker/package.json</files>
  <behavior>
    - Test: promotionWorkflow with stageCount 3 completes without error using TestWorkflowEnvironment.createTimeSkipping
    - Test: pause signal during stage loop causes workflow to block until resume signal
    - Test: abort signal terminates workflow with aborted status persisted via mocked or real activities
    - Test: workflow started with workflowId equal to promotionRunId
  </behavior>
  <action>
    Implement worker.ts bootstrap: Worker.create with taskQueue from TEMPORAL_TASK_QUEUE env default 'promotion', workflowsPath pointing to promotion.workflow, activities object from ./activities. Log startup and run worker.run(). Handle SIGINT graceful shutdown.

    Write promotion.workflow.test.ts using @temporalio/testing TestWorkflowEnvironment.createTimeSkipping (120s beforeAll timeout). Use real activities against testcontainers Postgres OR mock activities that verify call order — prefer real activities with test DB for end-to-end proof. Start workflow with workflowId: promotionRunId per D-07. Assert workflow completes all stages.

    Write promotion.signals.test.ts: start workflow, send pauseSignal mid-loop, verify isPaused via statusQuery, send resumeSignal, verify continuation. Test abortSignal stops workflow.

    Update apps/worker package.json test script to run vitest. Add dev script `tsx src/worker.ts`.

    Tests may skip when SKIP_TESTCONTAINERS=1 if DB-dependent; core workflow structure test can use mocked activities.
  </action>
  <acceptance_criteria>
    - worker.ts registers promotionWorkflow on task queue promotion
    - workflowId set to promotionRun.id in test start call per D-07
    - promotion.workflow.test.ts proves skeleton completes stage loop per D-09
    - promotion.signals.test.ts proves pause/resume per D-10
    - Worker builds via turbo without workflow bundle sandbox errors
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run apps/worker/src/__tests__/promotion.workflow.test.ts apps/worker/src/__tests__/promotion.signals.test.ts</automated>
  </verify>
  <done>Worker boots; Temporal integration tests prove FSM skeleton and signal handling; durable workflow tied to persisted run record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow sandbox → Activities | Untrusted workflow code isolated; I/O only in activities |
| Worker → Temporal server | localhost:7233 gRPC |
| Activities → PostgreSQL | DATABASE_URL credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | Workflow sandbox escape | mitigate | No Prisma/fs imports in workflow files; activity-only DB access |
| T-01-11 | Denial of Service | Activity retries | mitigate | maximumAttempts: 3, 30s timeout on proxyActivities |
| T-01-12 | Repudiation | Audit in activities | mitigate | recordAuditEvent at every milestone transition per D-01 |
| T-01-SC | Tampering | @temporalio/* packages | mitigate | Approved in RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- Workflow bundle builds without sandbox violations
- Integration tests pass for workflow and signals
- `pnpm turbo run build --filter=@ff-promo/worker` succeeds
</verification>

<success_criteria>
- Temporal worker can start durable promotion workflow skeleton tied to persisted run record (ROADMAP criterion 4)
- D-09/D-10/D-11/D-12 worker implementation complete
- SAFE-01 audit milestones written during workflow transitions
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-layer/01-04-SUMMARY.md` when done
</output>
