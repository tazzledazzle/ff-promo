---
phase: 01-foundation-data-layer
plan: 03
type: execute
wave: 2
depends_on:
  - "01-02"
files_modified:
  - packages/db/src/repositories/pipeline.repository.ts
  - packages/db/src/repositories/promotion-run.repository.ts
  - packages/db/src/repositories/gate-result.repository.ts
  - packages/db/src/repositories/audit.repository.ts
  - packages/db/src/repositories/index.ts
  - packages/db/src/index.ts
  - packages/db/src/__tests__/audit.integration.test.ts
  - packages/db/src/__tests__/pipeline.integration.test.ts
  - packages/db/src/__tests__/gate-result.integration.test.ts
  - packages/db/src/__tests__/promotion-run.integration.test.ts
autonomous: true
requirements:
  - SAFE-01
must_haves:
  truths:
    - "Operator can query audit history for a promotion run via AuditRepository.findByRunId"
    - "Audit events record actor, action, timestamp, and optional gate result linkage"
    - "Pipeline definitions with stages and gate policies persist and reload correctly"
    - "PromotionRun state updates persist as canonical business truth per D-07"
    - "GateResult records are queryable per run/stage and linkable from audit milestones"
  artifacts:
    - path: "packages/db/src/repositories/audit.repository.ts"
      provides: "Append-only audit per D-01/D-04"
      exports: ["AuditRepository"]
    - path: "packages/db/src/repositories/promotion-run.repository.ts"
      provides: "Canonical run state persistence per D-07"
      contains: "updateState"
    - path: "packages/db/src/__tests__/audit.integration.test.ts"
      provides: "SAFE-01 automated verification"
      contains: "findByRunId"
  key_links:
    - from: "packages/db/src/repositories/audit.repository.ts"
      to: "AuditEvent"
      via: "create only"
      pattern: "auditEvent\\.create"
    - from: "packages/db/src/repositories/audit.repository.ts"
      to: "GateResult"
      via: "gateResultId FK"
      pattern: "gateResultId"
---

<objective>
Implement Prisma repository layer for pipeline definitions, promotion runs, gate results, and append-only audit trail — with integration tests proving SAFE-01 compliance and D-01 through D-08 behavior.

Purpose: Data layer is the persistence foundation consumed by worker activities (plan 04) and future REST API (Phase 5); audit repository enforces milestone-only append-only writes.
Output: Four repository classes exported from @ff-promo/db; integration tests green against testcontainers Postgres.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-layer/01-CONTEXT.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer/01-PATTERNS.md
@.planning/phases/01-foundation-data-layer/01-02-SUMMARY.md
@packages/db/src/client.ts
@packages/contracts/src/audit.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pipeline and PromotionRun repositories</name>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md — Pattern 1 schema relations
    - .planning/phases/01-foundation-data-layer/01-CONTEXT.md — D-06, D-07
    - .planning/phases/01-foundation-data-layer/01-PATTERNS.md — repository patterns
    - packages/db/src/__tests__/setup.ts
  </read_first>
  <files>packages/db/src/repositories/pipeline.repository.ts, packages/db/src/repositories/promotion-run.repository.ts, packages/db/src/repositories/index.ts, packages/db/src/index.ts, packages/db/src/__tests__/pipeline.integration.test.ts, packages/db/src/__tests__/promotion-run.integration.test.ts</files>
  <behavior>
    - Test: createPipeline with 3 stages (dev, staging, prod) and gate policies persists and findById returns nested stages ordered by orderIndex
    - Test: createRun snapshots pipelineVersion at creation time and defaults status pending
    - Test: updateState changes status and currentStageIndex; sets temporalWorkflowId to run id on first update per D-07
    - Test: findById returns run after simulated disconnect/reconnect (new PrismaClient instance)
  </behavior>
  <action>
    Implement PipelineRepository with create(input matching PipelineCreateInputSchema), findById(id) including stages ordered by orderIndex and nested gatePolicies, findByFlagKey(flagKey). Use Prisma nested creates for Pipeline → Stage → GatePolicy per D-06 normalized storage.

    Implement PromotionRunRepository with create({ pipelineId, flagKey }) that reads pipeline.version and stores pipelineVersion frozen snapshot per D-07; updateState({ promotionRunId, status, currentStageIndex?, pauseReason?, temporalWorkflowId? }) that sets temporalWorkflowId to promotionRunId when first transitioning to active if not set; findById(id); findByStatus(status).

    Export repositories from repositories/index.ts and @ff-promo/db package index alongside createPrismaClient.

    Write integration tests using setup.ts testcontainers helper. Tests run migrate deploy before suite. Use AuditAction-agnostic fixtures — create pipeline/run directly via repositories.

    Validate repository inputs with @ff-promo/contracts Zod schemas at public method boundaries where input objects are accepted.
  </action>
  <acceptance_criteria>
    - PipelineRepository.create persists nested stages and gate policies in single transaction
    - PromotionRunRepository.create stores frozen pipelineVersion from parent pipeline
    - PromotionRunRepository.updateState sets temporalWorkflowId equal to run id on first active transition per D-07
    - Integration tests pass against ephemeral Postgres via testcontainers
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run packages/db/src/__tests__/pipeline.integration.test.ts packages/db/src/__tests__/promotion-run.integration.test.ts</automated>
  </verify>
  <done>Pipeline and PromotionRun repositories persist normalized config and canonical run state; D-06/D-07 integration tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GateResult and append-only Audit repositories</name>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md — Pattern 2 (Append-Only Audit Repository)
    - .planning/phases/01-foundation-data-layer/01-CONTEXT.md — D-01, D-02, D-03, D-04, D-08
    - .planning/phases/01-foundation-data-layer/01-PATTERNS.md — Append-Only Audit shared pattern
    - packages/contracts/src/audit.ts
  </read_first>
  <files>packages/db/src/repositories/gate-result.repository.ts, packages/db/src/repositories/audit.repository.ts, packages/db/src/__tests__/gate-result.integration.test.ts, packages/db/src/__tests__/audit.integration.test.ts</files>
  <behavior>
    - Test: GateResultRepository.create stores verdict, metricType, observedValue, threshold, metadata JSON
    - Test: AuditRepository.append creates event with actorType, actorId, displayName, occurredAt auto-set
    - Test: AuditRepository.append with gateResultId links gate_evaluated milestone to GateResult per D-08
    - Test: AuditRepository.findByRunId returns events ascending by occurredAt with gateResult included
    - Test: AuditRepository exposes NO update or delete methods (append + query only per D-04)
  </behavior>
  <action>
    Implement GateResultRepository with create({ promotionRunId, stageId, verdict, metricType, observedValue, threshold, metadata }) returning created record; findByRunId(promotionRunId) ordered by evaluatedAt desc; findByRunAndStage(promotionRunId, stageId).

    Implement AuditRepository per D-01/D-04 with ONLY append(input) and findByRunId(promotionRunId, { limit?, cursor? }) methods — deliberately omit update/delete to enforce append-only. append validates input via AuditEventInputSchema from @ff-promo/contracts before create. Include gateResult relation in findByRunId query per D-08. Metadata must accept forensics fields: metric values, thresholds, flagKey, environment, stageIndex per D-03.

    Export helper createRepositories(db) factory returning all four repository instances for worker activities.

    Write gate-result.integration.test.ts and audit.integration.test.ts covering SAFE-01: actor, action, timestamp recorded; gate result FK linkage; query by run ID returns chronological history.
  </action>
  <acceptance_criteria>
    - AuditRepository has append and findByRunId only — no update/delete methods per D-04
    - Milestone AuditAction enum values used in tests — no reconciler tick actions per D-01
    - Structured actor identity stored: actorType + actorId + optional displayName per D-02
    - gateResultId FK populated on gate_evaluated audit events per D-08
    - SAFE-01 integration tests pass
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run packages/db/src/__tests__/audit.integration.test.ts packages/db/src/__tests__/gate-result.integration.test.ts</automated>
  </verify>
  <done>Append-only audit and gate result repositories satisfy SAFE-01; operator-queryable audit history by promotion run ID works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → PostgreSQL | All writes via Prisma parameterized queries |
| Contracts → Repository | Zod validation on audit/gate inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Repudiation | AuditRepository | mitigate | Append-only API; no update/delete methods per D-04 |
| T-01-08 | Tampering | Audit metadata | mitigate | AuditEventInputSchema.parse before insert per D-03 |
| T-01-09 | Information Disclosure | DB errors | mitigate | Repository throws typed errors; no raw SQL in messages to callers |
| T-01-SC | Tampering | testcontainers image | mitigate | Pin postgres:16-alpine; isolated ephemeral containers |
</threat_model>

<verification>
- All four integration test files pass: `pnpm exec vitest run --project db`
- grep confirms AuditRepository has no update/delete methods
</verification>

<success_criteria>
- SAFE-01 satisfied: audit trail records actor, action, timestamp, gate results
- D-01 through D-08 repository behaviors implemented and tested
- Data layer query interface ready for worker activities and Phase 5 API
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-layer/01-03-SUMMARY.md` when done
</output>
