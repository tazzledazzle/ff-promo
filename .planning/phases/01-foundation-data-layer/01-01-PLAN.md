---
phase: 01-foundation-data-layer
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - tsconfig.base.json
  - vitest.config.ts
  - biome.json
  - docker-compose.yml
  - .env.example
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/src/index.ts
  - apps/worker/package.json
  - apps/worker/tsconfig.json
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/src/index.ts
  - apps/cli/package.json
  - apps/cli/tsconfig.json
  - apps/cli/src/index.ts
  - packages/contracts/package.json
  - packages/contracts/tsconfig.json
  - packages/contracts/src/index.ts
  - packages/db/package.json
  - packages/db/tsconfig.json
autonomous: true
requirements: []
user_setup:
  - service: docker
    why: "Local Postgres + Temporal dev server per D-12"
    dashboard_config:
      - task: "Ensure Docker Desktop or Docker Engine is running"
        location: "Local machine"
must_haves:
  truths:
    - "pnpm install succeeds at repo root with workspace packages linked"
    - "turbo run build completes across all scaffolded packages"
    - "docker compose up starts Postgres 16 and Temporal dev server on ports 5432/7233"
    - "Vitest project config exists for packages/db and apps/worker"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Monorepo workspace definition"
      contains: "apps/*"
    - path: "turbo.json"
      provides: "Build/test/lint orchestration"
      contains: '"tasks"'
    - path: "docker-compose.yml"
      provides: "Local dev stack per D-12"
      contains: "postgres:16"
    - path: "vitest.config.ts"
      provides: "Test runner projects per D-15"
      contains: "projects"
  key_links:
    - from: "apps/worker/package.json"
      to: "packages/db"
      via: "workspace:* dependency"
      pattern: "@ff-promo/db"
    - from: "docker-compose.yml"
      to: "packages/db"
      via: "DATABASE_URL in .env.example"
      pattern: "ffpromo"
---

<objective>
Bootstrap the full TypeScript monorepo scaffold with pnpm workspaces, Turborepo task graph, Docker Compose local stack, Vitest test harness, and placeholder app shells — establishing the foundation all Phase 1 packages build on.

Purpose: Greenfield repo has zero application code; D-13/D-14/D-12/D-15 require infrastructure before schema or worker work can begin.
Output: Runnable `pnpm install`, `pnpm turbo run build`, `docker compose up -d`, and empty Vitest projects ready for Wave 1 tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-layer/01-CONTEXT.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer/01-PATTERNS.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root monorepo workspace and Turborepo config</name>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md — Pattern 4 (Monorepo Bootstrap Sequence)
    - .planning/phases/01-foundation-data-layer/01-PATTERNS.md — Root monorepo bootstrap section
    - .planning/research/STACK.md — Monorepo Layout and version pins
  </read_first>
  <files>package.json, pnpm-workspace.yaml, turbo.json, tsconfig.base.json, biome.json, .env.example</files>
  <action>
    Initialize root package.json with `"private": true`, `"packageManager": "pnpm@10.33.0"`, `"engines": { "node": ">=24" }`, scripts: `build`, `test`, `lint`, `dev` delegating to `turbo run`. Pin devDependencies: typescript@~5.8.0, turbo@2.9.18, vitest@4.1.9, @testcontainers/postgresql@12.0.3, biome, tsx, dotenv per RESEARCH Standard Stack.

    Create pnpm-workspace.yaml listing `apps/*` and `packages/*` per D-13.

    Create turbo.json using v2 `tasks` key (NOT deprecated `pipeline`): build dependsOn `^build` outputs `dist/**`; test dependsOn `^build`; lint; dev with `cache: false, persistent: true` per D-14.

    Create tsconfig.base.json with strict TypeScript settings (strict, esModuleInterop, moduleResolution bundler, target ES2022). All package tsconfigs extend this.

    Add biome.json for root lint/format with minimal rules.

    Create .env.example with `DATABASE_URL=postgresql://ffpromo:ffpromo@localhost:5432/ffpromo`, `TEMPORAL_ADDRESS=localhost:7233`, `TEMPORAL_TASK_QUEUE=promotion`. Do NOT commit secrets.

    Run `corepack enable && pnpm install` at root after workspace files exist.
  </action>
  <acceptance_criteria>
    - Root package.json has packageManager field and turbo scripts
    - turbo.json uses `tasks` key with build/test/lint/dev
    - pnpm-workspace.yaml includes apps/* and packages/*
    - .env.example documents DATABASE_URL and TEMPORAL_ADDRESS
  </acceptance_criteria>
  <verify>
    <automated>pnpm turbo run build 2>&1 | head -5; test -f pnpm-workspace.yaml && test -f turbo.json</automated>
  </verify>
  <done>Root workspace installs cleanly; turbo.json validates; tsconfig.base.json exists for packages to extend.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold apps and packages shells</name>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md — Recommended Project Structure
    - .planning/phases/01-foundation-data-layer/01-PATTERNS.md — App shells section
    - .planning/research/STACK.md — package naming (@ff-promo/*)
  </read_first>
  <files>apps/api/package.json, apps/api/tsconfig.json, apps/api/src/index.ts, apps/worker/package.json, apps/worker/tsconfig.json, apps/web/package.json, apps/web/tsconfig.json, apps/web/src/index.ts, apps/cli/package.json, apps/cli/tsconfig.json, apps/cli/src/index.ts, packages/contracts/package.json, packages/contracts/tsconfig.json, packages/contracts/src/index.ts, packages/db/package.json, packages/db/tsconfig.json</files>
  <action>
    Scaffold four app shells per D-13: `apps/api`, `apps/worker`, `apps/web`, `apps/cli`. Each gets package.json (name `@ff-promo/api`, `@ff-promo/worker`, `@ff-promo/web`, `@ff-promo/cli`), tsconfig extending root base, and placeholder src entry that logs shell name and exits 0 on build. No Fastify routes, no Next.js pages, no Commander commands — shells only.

    Scaffold `packages/contracts` with zod@4.4.3 dependency and empty barrel export in src/index.ts.

    Scaffold `packages/db` with prisma@7.8.0, @prisma/client@7.8.0, @prisma/adapter-pg@7.8.0, pg, zod, workspace dependency on `@ff-promo/contracts`. Add build script running `prisma generate` (schema added in plan 02). Export placeholder from src/index.ts.

    Wire `apps/worker` package.json with workspace deps on `@ff-promo/db`, `@ff-promo/contracts`, and Temporal SDK packages (@temporalio/worker, workflow, client, activity @1.18.1; @temporalio/testing devDep).

    Each package.json includes `build`, `test`, `lint` scripts compatible with turbo. Use `workspace:*` protocol for internal deps per RESEARCH.

    Do NOT add Prisma schema yet — that is plan 02.
  </action>
  <acceptance_criteria>
    - All six packages exist under apps/* and packages/* with @ff-promo/* names
    - apps/worker depends on @ff-promo/db and Temporal SDK via workspace:*
    - packages/contracts has zod dependency
    - packages/db has prisma 7.8.0 and adapter-pg dependencies
    - No functional API routes, Next.js app, or CLI commands implemented
  </acceptance_criteria>
  <verify>
    <automated>pnpm turbo run build</automated>
  </verify>
  <done>All app and package shells build successfully via turbo; workspace protocol links packages.</done>
</task>

<task type="auto">
  <name>Task 3: Docker Compose stack and Vitest harness</name>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md — Docker Compose example and Pattern 5 (testcontainers)
    - .planning/phases/01-foundation-data-layer/01-PATTERNS.md — docker-compose.yml and integration tests sections
    - .planning/phases/01-foundation-data-layer/01-VALIDATION.md — Wave 0 Requirements
  </read_first>
  <files>docker-compose.yml, vitest.config.ts, packages/db/src/__tests__/setup.ts</files>
  <action>
    Create docker-compose.yml per D-12 with two services: (1) postgres:16-alpine, user/password/db `ffpromo`, port 5432, named volume pgdata; (2) temporal using temporalio/admin-tools:1.27.2 running `temporal server start-dev --ip 0.0.0.0 --db-filename /tmp/temporal.db` on ports 7233 and 8233. App Postgres is separate from Temporal embedded SQLite — do NOT share Prisma migrations with Temporal tables (RESEARCH Pitfall 7).

    Create root vitest.config.ts per D-15 with projects array for `packages/db` and `apps/worker`. Each project has its own test include glob. Support env var `SKIP_TESTCONTAINERS=1` to skip integration tests in setup helper.

    Create packages/db/src/__tests__/setup.ts exporting helpers: `startTestDatabase()` using PostgreSqlContainer('postgres:16-alpine'), sets DATABASE_URL, runs `pnpm exec prisma migrate deploy` from packages/db cwd with 120s timeout; `stopTestDatabase()` stops container. Export skip guard when SKIP_TESTCONTAINERS=1.

    Add placeholder smoke test file packages/db/src/__tests__/smoke.test.ts asserting true — proves vitest project wiring. Full integration tests come in plan 05.

    Add root script `"test:db": "pnpm exec vitest run --project db"`.
  </action>
  <acceptance_criteria>
    - docker-compose.yml defines postgres:16 and temporal dev server services
    - vitest.config.ts defines db and worker projects
    - setup.ts exports testcontainers lifecycle with migrate deploy hook
    - SKIP_TESTCONTAINERS=1 bypasses container startup
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project db packages/db/src/__tests__/smoke.test.ts</automated>
  </verify>
  <done>Docker Compose file ready for local dev; Vitest projects configured; testcontainers setup helper exists for Wave 1+ integration tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → Docker | Compose exposes Postgres/Temporal ports to localhost |
| npm registry → monorepo | External packages installed via pnpm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | npm packages | mitigate | Use Package Legitimacy Audit from RESEARCH.md; pnpm lockfile; workspace:* protocol |
| T-01-02 | Tampering | docker-compose.yml | accept | Local dev only; no production secrets in compose |
| T-01-03 | Elevation | Docker socket | mitigate | testcontainers uses isolated ephemeral containers per RESEARCH security table |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | All packages Approved in RESEARCH audit; lockfile committed |
</threat_model>

<verification>
- `pnpm install && pnpm turbo run build` succeeds
- `pnpm exec vitest run --project db` runs smoke test
- `docker compose config` validates compose file syntax
</verification>

<success_criteria>
- Monorepo scaffold matches D-13/D-14 layout with six packages building via turbo
- Docker Compose provides Postgres + Temporal per D-12
- Vitest + testcontainers harness ready per D-15
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-layer/01-01-SUMMARY.md` when done
</output>
