---
phase: 01-foundation-data-layer
plan: 06
type: execute
wave: 0
depends_on:
  - "01-01"
files_modified:
  - package.json
  - docker-compose.yml
  - vitest.config.ts
  - packages/db/src/__tests__/setup.ts
  - packages/db/src/__tests__/smoke.test.ts
autonomous: true
requirements: []
user_setup:
  - service: docker
    why: "Local Postgres + Temporal dev server per D-12"
    dashboard_config:
      - task: "Ensure Docker Desktop or Docker Engine is running"
        location: "Local machine"
must_haves:
  truths:
    - "docker compose up starts Postgres 16 and Temporal dev server on ports 5432/7233"
    - "Vitest project config exists for packages/db and apps/worker"
    - "testcontainers setup helper starts ephemeral Postgres and runs migrate deploy"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Local dev stack per D-12"
      contains: "postgres:16"
    - path: "vitest.config.ts"
      provides: "Test runner projects per D-15"
      contains: "projects"
    - path: "packages/db/src/__tests__/setup.ts"
      provides: "testcontainers lifecycle helper"
      contains: "startTestDatabase"
  key_links:
    - from: "docker-compose.yml"
      to: "packages/db"
      via: "DATABASE_URL in .env.example"
      pattern: "ffpromo"
    - from: "packages/db/src/__tests__/setup.ts"
      to: "packages/db/prisma/migrations"
      via: "prisma migrate deploy"
      pattern: "migrate deploy"
---

<objective>
Add Docker Compose local stack, Vitest multi-project config, and testcontainers harness — enabling integration tests in plans 03–05.

Purpose: D-12/D-15 require local Postgres + Temporal and test infrastructure; separated from app shell bootstrap to keep plan 01 within scope.
Output: Runnable `docker compose up -d`, Vitest projects for db and worker, testcontainers setup helper for Wave 1+ integration tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-layer/01-CONTEXT.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer/01-PATTERNS.md
@.planning/phases/01-foundation-data-layer/01-VALIDATION.md
@.planning/phases/01-foundation-data-layer/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest and testcontainers at root</name>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md — Standard Stack and Pattern 5
    - .planning/phases/01-foundation-data-layer/01-VALIDATION.md — Wave 0 Requirements
  </read_first>
  <files>package.json</files>
  <action>
    Add root devDependencies vitest@4.1.9 and @testcontainers/postgresql@12.0.3 per D-15 and RESEARCH Package Legitimacy Audit (Approved).

    Add root scripts: `"test:db": "pnpm exec vitest run --project db"`, `"test:worker": "pnpm exec vitest run --project worker"`.

    Run `pnpm install` to update lockfile.
  </action>
  <acceptance_criteria>
    - vitest@4.1.9 and @testcontainers/postgresql@12.0.3 in root devDependencies
    - test:db script exists in root package.json
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.devDependencies?.vitest) process.exit(1)"</automated>
  </verify>
  <done>Vitest and testcontainers installed at monorepo root.</done>
</task>

<task type="auto">
  <name>Task 2: Docker Compose stack per D-12</name>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md — Docker Compose example and Open Questions (RESOLVED)
    - .planning/phases/01-foundation-data-layer/01-PATTERNS.md — docker-compose.yml section
  </read_first>
  <files>docker-compose.yml</files>
  <action>
    Create docker-compose.yml per D-12 with two services: (1) postgres:16-alpine, user/password/db `ffpromo`, port 5432, named volume pgdata; (2) temporal using temporalio/admin-tools:1.27.2 running `temporal server start-dev --ip 0.0.0.0 --db-filename /tmp/temporal.db` on ports 7233 and 8233.

    App Postgres is separate from Temporal embedded SQLite — do NOT share Prisma migrations with Temporal tables (RESEARCH Pitfall 7). RESOLVED: Temporal uses embedded store in compose; app data stays in Postgres ffpromo database.
  </action>
  <acceptance_criteria>
    - docker-compose.yml defines postgres:16-alpine and temporal dev server services
    - Temporal service uses embedded SQLite via --db-filename flag
    - Postgres service exposes port 5432 with ffpromo credentials
  </acceptance_criteria>
  <verify>
    <automated>docker compose config --quiet</automated>
  </verify>
  <done>Docker Compose file validates and defines Postgres + Temporal per D-12.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest harness and testcontainers setup helper</name>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md — Pattern 5 (testcontainers)
    - .planning/phases/01-foundation-data-layer/01-VALIDATION.md — Wave 0 Requirements
  </read_first>
  <files>vitest.config.ts, packages/db/src/__tests__/setup.ts, packages/db/src/__tests__/smoke.test.ts</files>
  <action>
    Create root vitest.config.ts per D-15 with projects array for `packages/db` (project name `db`) and `apps/worker` (project name `worker`). Each project has its own test include glob.

    Create packages/db/src/__tests__/setup.ts exporting helpers: `startTestDatabase()` using PostgreSqlContainer('postgres:16-alpine'), sets DATABASE_URL, runs `pnpm exec prisma migrate deploy` from packages/db cwd with 120s timeout; `stopTestDatabase()` stops container. Export skip guard when SKIP_TESTCONTAINERS=1.

    Add placeholder smoke test packages/db/src/__tests__/smoke.test.ts asserting true — proves vitest project wiring. Full integration tests are created in plan 03 (repositories) and plan 04 (worker workflow tests).

    Support env var SKIP_TESTCONTAINERS=1 to skip integration tests in setup helper.
  </action>
  <acceptance_criteria>
    - vitest.config.ts defines db and worker projects
    - setup.ts exports testcontainers lifecycle with migrate deploy hook
    - SKIP_TESTCONTAINERS=1 bypasses container startup
    - smoke.test.ts runs under --project db
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project db packages/db/src/__tests__/smoke.test.ts</automated>
  </verify>
  <done>Vitest projects configured; testcontainers setup helper exists for plan 03+ integration tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → Docker | Compose exposes Postgres/Temporal ports to localhost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering | docker-compose.yml | accept | Local dev only; no production secrets in compose |
| T-01-03 | Elevation | Docker socket | mitigate | testcontainers uses isolated ephemeral containers per RESEARCH security table |
| T-01-SC | Tampering | vitest/testcontainers installs | mitigate | Approved packages in RESEARCH audit; lockfile committed |
</threat_model>

<verification>
- `pnpm exec vitest run --project db` runs smoke test
- `docker compose config` validates compose file syntax
</verification>

<success_criteria>
- Docker Compose provides Postgres + Temporal per D-12
- Vitest + testcontainers harness ready per D-15
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-layer/01-06-SUMMARY.md` when done
</output>
