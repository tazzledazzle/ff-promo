---
phase: 01-foundation-data-layer
plan: 02
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - packages/db/prisma/schema.prisma
  - packages/db/prisma.config.ts
  - packages/db/package.json
  - packages/db/src/client.ts
  - packages/db/src/index.ts
  - packages/contracts/src/pipeline.ts
  - packages/contracts/src/promotion-run.ts
  - packages/contracts/src/audit.ts
  - packages/contracts/src/gate-result.ts
  - packages/contracts/src/index.ts
autonomous: true
requirements:
  - SAFE-01
must_haves:
  truths:
    - "PostgreSQL schema contains Pipeline, Stage, GatePolicy, PromotionRun, GateResult, AuditEvent models"
    - "Prisma migration applies cleanly to local Postgres"
    - "Zod schemas in contracts mirror Prisma enums for domain validation"
    - "PrismaClient instantiates via @prisma/adapter-pg driver adapter"
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      provides: "Full core domain schema per D-05/D-06"
      contains: "model AuditEvent"
    - path: "packages/db/prisma.config.ts"
      provides: "Prisma 7 configuration"
      contains: "defineConfig"
    - path: "packages/contracts/src/audit.ts"
      provides: "Audit Zod schemas per D-02"
      contains: "ActorTypeSchema"
  key_links:
    - from: "packages/db/src/client.ts"
      to: "packages/db/prisma/schema.prisma"
      via: "prisma generate"
      pattern: "PrismaPg"
    - from: "packages/contracts/src/audit.ts"
      to: "packages/db/prisma/schema.prisma"
      via: "enum parity"
      pattern: "AuditAction"
---

<objective>
Define the full Prisma 7 domain schema (normalized Pipeline → Stage → GatePolicy, PromotionRun, GateResult, AuditEvent), shared Zod contracts, Prisma client bootstrap, and apply initial migration to Postgres.

Purpose: D-05/D-06 require full relational schema day one; SAFE-01 audit model must exist before repositories write events; D-07 requires PromotionRun as canonical business state store.
Output: Migrated database with all six models; generated Prisma client; contracts package exporting validated domain types.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-layer/01-CONTEXT.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer/01-PATTERNS.md
@.planning/phases/01-foundation-data-layer/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma schema and Prisma 7 config</name>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md — Pattern 1 (Prisma Schema) lines 246-393
    - .planning/phases/01-foundation-data-layer/01-PATTERNS.md — schema.prisma section
    - .planning/phases/01-foundation-data-layer/01-CONTEXT.md — D-05, D-06, D-07, D-08
  </read_first>
  <files>packages/db/prisma/schema.prisma, packages/db/prisma.config.ts, packages/db/package.json</files>
  <action>
    Create packages/db/prisma/schema.prisma per D-05/D-06 with normalized relational model from RESEARCH Pattern 1:

    Enums: ActorType (user, system, api_key), PromotionStatus (pending, active, paused, completed, aborted), GateVerdict (pass, fail, pending, skipped), AuditAction (run_started, run_paused, run_resumed, run_aborted, run_completed, stage_entered, stage_advanced, gate_evaluated).

    Models: Pipeline (name, version, flagKey, projectKey, isActive, stages relation), Stage (pipelineId, orderIndex, environment, displayName, unique on pipelineId+orderIndex and pipelineId+environment), GatePolicy (stageId, metricType, threshold, comparisonMode default absolute, windowSeconds, minSampleSize, serviceName, unique stageId+metricType), PromotionRun (pipelineId, pipelineVersion frozen snapshot per D-07, flagKey, status, currentStageIndex, temporalWorkflowId unique nullable, pauseReason, timestamps), GateResult (promotionRunId, stageId, verdict, metricType, observedValue, threshold, metadata Json per D-03, evaluatedAt), AuditEvent (promotionRunId, action, actorType, actorId, displayName optional, gateResultId optional FK per D-08, metadata Json, occurredAt — NO updatedAt field per D-04 append-only).

    Generator output to `./generated/client`. Datasource uses PostgreSQL without url in schema (Prisma 7 pattern).

    Create packages/db/prisma.config.ts using defineConfig from prisma/config with schema path, migrations path, seed command `tsx prisma/seed.ts`, datasource url from env('DATABASE_URL') per RESEARCH Prisma 7 config example.

    Update packages/db/package.json scripts: `"db:generate": "prisma generate"`, `"db:migrate": "prisma migrate dev"`, `"build": "prisma generate && tsc"`. Add prisma seed config pointing to seed.ts (seed implementation in plan 05).

    Do NOT use Prisma 5 patterns (no url in schema, no bare new PrismaClient()).
  </action>
  <acceptance_criteria>
    - All six models and four enums exist in schema.prisma
    - AuditEvent has no updatedAt column (append-only per D-04)
    - PromotionRun.pipelineVersion column exists for frozen snapshot per D-07
    - GateResult.metadata and AuditEvent.metadata are Json type per D-03
    - prisma.config.ts uses defineConfig with migrations and seed paths
  </acceptance_criteria>
  <verify>
    <automated>cd packages/db && pnpm exec prisma validate</automated>
  </verify>
  <done>Schema validates; Prisma 7 config file exists; all D-05/D-06 entities modeled.</done>
</task>

<task type="auto">
  <name>Task 2: Shared Zod contracts mirroring schema</name>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-PATTERNS.md — packages/contracts section
    - .planning/phases/01-foundation-data-layer/01-CONTEXT.md — D-01, D-02, D-03
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md — Security Domain V5
  </read_first>
  <files>packages/contracts/src/pipeline.ts, packages/contracts/src/promotion-run.ts, packages/contracts/src/audit.ts, packages/contracts/src/gate-result.ts, packages/contracts/src/index.ts</files>
  <action>
    Create Zod 4 schemas in packages/contracts mirroring Prisma enums and key input shapes:

    pipeline.ts: PipelineCreateInputSchema (name, flagKey, projectKey, stages array with orderIndex, environment, displayName, gatePolicies array with metricType, threshold, serviceName). Export StageEnvironmentSchema as z.enum(['dev','staging','prod']).

    promotion-run.ts: PromotionStatusSchema, PromotionRunCreateInputSchema (pipelineId, flagKey, actor), PersistRunStateInputSchema (promotionRunId, status, currentStageIndex optional, pauseReason optional) for D-07 activity contract.

    audit.ts per D-01/D-02: ActorTypeSchema, AuditActionSchema (milestone actions only — no reconciler tick actions), AuditEventInputSchema with actorType, actorId, displayName optional, gateResultId optional, metadata as z.record(z.string(), z.unknown()) for D-03 forensics JSON.

    gate-result.ts: GateVerdictSchema, GateResultCreateInputSchema (promotionRunId, stageId, verdict, metricType, observedValue, threshold, metadata).

    Update index.ts barrel to export all schemas and inferred TypeScript types.

    Contracts package must NOT depend on @ff-promo/db or Temporal packages.
  </action>
  <acceptance_criteria>
    - AuditActionSchema includes exactly the nine milestone actions from D-01
    - ActorTypeSchema matches D-02 (user, system, api_key)
    - AuditEventInputSchema validates metadata as record for D-03
    - PersistRunStateInputSchema exported for worker activities per D-07
    - No imports from @ff-promo/db in contracts package
  </acceptance_criteria>
  <verify>
    <automated>pnpm turbo run build --filter=@ff-promo/contracts</automated>
  </verify>
  <done>Contracts compile and export Zod schemas matching Prisma domain enums.</done>
</task>

<task type="auto" gate="blocking">
  <name>[BLOCKING] Task 3: Prisma client bootstrap and initial migration</name>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md — Prisma 7 client bootstrap and Pitfall 1
    - .planning/phases/01-foundation-data-layer/01-PATTERNS.md — client.ts section
  </read_first>
  <files>packages/db/src/client.ts, packages/db/src/index.ts, packages/db/prisma/migrations/</files>
  <action>
    Implement packages/db/src/client.ts with createPrismaClient(connectionString: string) using PrismaPg adapter from @prisma/adapter-pg and generated PrismaClient from ../generated/client per RESEARCH Pattern 1. Export singleton factory only — no global singleton with hardcoded URL.

    Export createPrismaClient and generated enum types from packages/db/src/index.ts.

    Ensure Postgres is reachable (docker compose up postgres or testcontainers). Run initial migration using non-TTY workaround: `CI=true pnpm exec prisma migrate dev --name init --skip-seed` from packages/db directory with DATABASE_URL set to postgresql://ffpromo:ffpromo@localhost:5432/ffpromo.

    After migration succeeds, run `pnpm exec prisma generate` and verify packages/db builds.

    Migration files must be committed under packages/db/prisma/migrations/. This task MUST complete before plan 03 repository work or any integration tests.
  </action>
  <acceptance_criteria>
    - createPrismaClient uses @prisma/adapter-pg (not bare PrismaClient)
    - Initial migration exists in prisma/migrations/ with init name
    - prisma migrate dev exits 0 against running Postgres
    - pnpm turbo run build --filter=@ff-promo/db succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd packages/db && CI=true pnpm exec prisma migrate dev --name init --skip-seed 2>&1; pnpm exec prisma generate && pnpm run build</automated>
  </verify>
  <done>Database schema deployed; Prisma client generated with driver adapter; @ff-promo/db package exports createPrismaClient.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → PostgreSQL | DATABASE_URL connection; parameterized queries via Prisma |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | Raw SQL | mitigate | Prisma ORM only in Phase 1; no $executeRaw |
| T-01-05 | Repudiation | AuditEvent schema | mitigate | Append-only design: no updatedAt on AuditEvent per D-04 |
| T-01-06 | Tampering | Audit metadata JSON | mitigate | Zod-validate metadata shape in contracts before insert (plan 03) |
| T-01-SC | Tampering | prisma/npm installs | mitigate | Approved packages from RESEARCH audit; lockfile |
</threat_model>

<verification>
- `pnpm exec prisma validate` passes
- Migration applies to Postgres without error
- All enum values in contracts match schema enums
</verification>

<success_criteria>
- Full core schema persisted per D-05/D-06 with migration history
- Prisma 7 adapter pattern working per RESEARCH
- Contracts provide validation boundary for SAFE-01 audit inputs
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-layer/01-02-SUMMARY.md` when done
</output>
