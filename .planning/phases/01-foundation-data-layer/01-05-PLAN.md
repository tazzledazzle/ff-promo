---
phase: 01-foundation-data-layer
plan: 05
type: execute
wave: 4
depends_on:
  - "01-03"
  - "01-04"
files_modified:
  - packages/db/prisma/seed.ts
  - packages/db/package.json
  - packages/db/src/__tests__/seed.smoke.test.ts
  - packages/db/src/__tests__/setup.ts
  - README.md
  - .env.example
autonomous: true
requirements:
  - SAFE-01
must_haves:
  truths:
    - "Seed script creates dev → staging → prod pipeline with gate policies per stage"
    - "Seed script creates one mock PromotionRun in pending status for local demo"
    - "pnpm exec prisma db seed runs idempotently against local Postgres"
    - "Full integration test suite passes via turbo test"
    - "Developer can start local stack with documented commands"
  artifacts:
    - path: "packages/db/prisma/seed.ts"
      provides: "Demo data per D-16"
      contains: "dev"
    - path: "packages/db/src/__tests__/seed.smoke.test.ts"
      provides: "Seed verification"
      contains: "staging"
    - path: "README.md"
      provides: "Local dev workflow documentation"
      contains: "docker compose"
  key_links:
    - from: "packages/db/prisma/seed.ts"
      to: "PipelineRepository"
      via: "create pipeline with stages"
      pattern: "orderIndex"
    - from: "README.md"
      to: "docker-compose.yml"
      via: "documented startup sequence"
      pattern: "docker compose up"
---

<objective>
Deliver seed data for local development and demo, smoke tests verifying seed output, README dev workflow, and full-suite test gate proving Phase 1 foundation is complete.

Purpose: D-16 requires sample pipeline and mock run; D-15 requires integration test coverage; operators need documented local stack startup per D-12.
Output: Runnable seed command; smoke test; README with docker compose + migrate + seed + test instructions; full turbo test green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-layer/01-CONTEXT.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer/01-VALIDATION.md
@.planning/phases/01-foundation-data-layer/01-03-SUMMARY.md
@.planning/phases/01-foundation-data-layer/01-04-SUMMARY.md
@packages/db/src/repositories/pipeline.repository.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed script with demo pipeline and mock run</name>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md — D-16 seed pattern and Prisma 7 seeding
    - .planning/phases/01-foundation-data-layer/01-PATTERNS.md — seed.ts section
    - .planning/phases/01-foundation-data-layer/01-CONTEXT.md — D-16
    - packages/db/prisma.config.ts
  </read_first>
  <files>packages/db/prisma/seed.ts, packages/db/package.json</files>
  <action>
    Implement packages/db/prisma/seed.ts per D-16:

    Use createPrismaClient(process.env.DATABASE_URL!) with @prisma/adapter-pg. Fail fast with clear error if DATABASE_URL missing.

    Upsert or find-or-create pipeline named "default-promotion" version 1 with flagKey "demo-feature-flag", projectKey "default", three stages: orderIndex 0 environment dev, 1 staging, 2 prod with displayNames. Each stage gets two GatePolicy rows: error_rate threshold 0.01 and latency_p95 threshold 500ms, serviceName "demo-service", windowSeconds 300.

    Create one PromotionRun in pending status linked to seeded pipeline with flagKey matching pipeline, pipelineVersion snapshotted from pipeline.version. Do NOT start Temporal workflow in seed — run stays pending for manual worker demo.

    Make seed idempotent: use upsert on pipeline name+version or check existence before create to allow repeated `pnpm exec prisma db seed` without duplicate key errors.

    Ensure prisma.config.ts seed command is `tsx prisma/seed.ts`. Add package.json script `"db:seed": "prisma db seed"`.

    Prisma 7 requires explicit seed invocation — document that migrate reset does NOT auto-seed.
  </action>
  <acceptance_criteria>
    - Seed creates exactly 3 stages in dev → staging → prod order per D-16
    - Each stage has error_rate and latency_p95 gate policies
    - One PromotionRun in pending status exists after seed
    - Running seed twice does not throw duplicate constraint errors
    - Seed uses createPrismaClient with adapter (not bare PrismaClient)
  </acceptance_criteria>
  <verify>
    <automated>cd packages/db && DATABASE_URL=postgresql://ffpromo:ffpromo@localhost:5432/ffpromo pnpm exec prisma db seed 2>&1; echo exit:$?</automated>
  </verify>
  <done>Seed script populates demo pipeline and pending promotion run for local development.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed smoke test and full integration suite gate</name>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-VALIDATION.md — Per-Task Verification Map
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md — Pattern 5 testcontainers
    - packages/db/src/__tests__/setup.ts
  </read_first>
  <files>packages/db/src/__tests__/seed.smoke.test.ts, packages/db/src/__tests__/setup.ts</files>
  <behavior>
    - Test: after running seed logic against testcontainers DB, pipeline findById returns 3 stages with environments dev, staging, prod in order
    - Test: seeded pipeline has gate policies on each stage
    - Test: pending PromotionRun exists for seeded pipeline
    - Test: audit integration tests still pass in full db project suite
  </behavior>
  <action>
    Write seed.smoke.test.ts that starts testcontainers Postgres, runs migrate deploy, executes seed script logic (import seed main function or spawn prisma db seed), then queries via PipelineRepository and PromotionRunRepository to assert D-16 invariants.

    Enhance setup.ts if needed: export runSeed(connectionString) helper wrapping seed execution for tests.

    Ensure all packages/db integration tests run under vitest project db: audit, gate-result, pipeline, promotion-run, seed.smoke, smoke.

    Run full monorepo test gate: `pnpm turbo run test` must pass (worker tests may skip with SKIP_TESTCONTAINERS=1 but db project must be fully green).
  </action>
  <acceptance_criteria>
    - seed.smoke.test.ts verifies dev → staging → prod stage order
    - Full db project test suite passes in under 45 seconds warm per VALIDATION.md
    - turbo run test completes at monorepo root
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project db && pnpm turbo run test</automated>
  </verify>
  <done>Seed smoke test green; full integration test suite passes.</done>
</task>

<task type="auto">
  <name>Task 3: README local development workflow</name>
  <read_first>
    - .planning/PROJECT.md — Core value and constraints
    - .planning/phases/01-foundation-data-layer/01-CONTEXT.md — D-12, D-16
    - .env.example
  </read_first>
  <files>README.md, .env.example</files>
  <action>
    Replace minimal README with Phase 1 local dev guide covering:

    Prerequisites: Node 24+, pnpm 10+, Docker.

    Quick start sequence: (1) cp .env.example .env, (2) docker compose up -d, (3) pnpm install, (4) cd packages/db && CI=true pnpm exec prisma migrate dev --name init --skip-seed (or migrate deploy if migration exists), (5) pnpm exec prisma db seed, (6) pnpm turbo run test.

    Document worker start: cd apps/worker && pnpm dev (requires Temporal from compose).

    Document test shortcuts: pnpm exec vitest run --project db, SKIP_TESTCONTAINERS=1 for quick runs.

    Document Phase 1 scope boundary: no LD API, no PromQL, no REST endpoints yet — foundation only.

    Keep README concise (under 120 lines). No secrets in README.
  </action>
  <acceptance_criteria>
    - README documents docker compose, migrate, seed, and test commands
    - README states Phase 1 scope boundary (no LD/PromQL/API)
    - .env.example variables match README instructions
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' README.md | grep -c 'docker compose' | awk '{exit ($1 >= 1 ? 0 : 1)}'</automated>
  </verify>
  <done>Developer can follow README to bootstrap local foundation stack end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed script → PostgreSQL | Local dev credentials only |
| README → Developer | Documents non-secret env vars |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | Information Disclosure | Seed data | mitigate | No real API keys or PII in seed per RESEARCH V8 |
| T-01-14 | Tampering | Seed idempotency | accept | Upsert prevents duplicate demo data; dev-only |
| T-01-SC | Tampering | tsx/prisma seed | mitigate | Approved packages; seed runs only against DATABASE_URL |
</threat_model>

<verification>
- `pnpm exec prisma db seed` succeeds against compose Postgres
- `pnpm turbo run test` full suite green
- README quick start steps are accurate
</verification>

<success_criteria>
- D-15 integration test coverage complete across db and worker projects
- D-16 seed data available for local dev and demo
- Phase 1 ROADMAP success criteria achievable: persistence, audit, query, Temporal skeleton
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-layer/01-05-SUMMARY.md` when done
</output>
