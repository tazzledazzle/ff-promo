---
phase: 04-promotion-engine
plan: 04
type: execute
wave: 3
depends_on:
  - "04-03"
files_modified:
  - apps/worker/src/lib/start-promotion-run.ts
  - apps/worker/src/scripts/start-run.ts
  - apps/worker/src/__tests__/start-promotion-run.test.ts
  - apps/worker/src/__tests__/promotion-engine.integration.test.ts
  - apps/worker/src/__tests__/promotion.signals.test.ts
  - apps/worker/package.json
  - README.md
autonomous: true
requirements:
  - PIPE-02
  - PIPE-03
  - PIPE-04
  - SAFE-02
must_haves:
  truths:
    - "Developer can start pending promotion run via worker helper (PIPE-02)"
    - "End-to-end integration test runs real activities with nock LD + Prometheus"
    - "Abort signal stops in-flight promotion immediately (SAFE-02)"
    - "README documents worker start-run command and Phase 4 scope"
  artifacts:
    - path: "apps/worker/src/lib/start-promotion-run.ts"
      provides: "Temporal workflow starter for pending runs"
      exports: ["startPromotionRun"]
    - path: "apps/worker/src/__tests__/promotion-engine.integration.test.ts"
      provides: "E2E promotion engine test with nock"
      contains: "nock"
  key_links:
    - from: "apps/worker/src/lib/start-promotion-run.ts"
      to: "promotionWorkflow"
      via: "client.workflow.start"
      pattern: "workflow.start"
---

<objective>
Deliver `startPromotionRun` for PIPE-02, full promotion-engine integration tests with nock at the HTTP boundary, SAFE-02 abort coverage, and README updates.

Purpose: Phase 4 completion gate — prove engine end-to-end without REST API.
Output: start helper + dev script, integration test suite, abort test, docs.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-promotion-engine/04-RESEARCH.md
@.planning/phases/04-promotion-engine/04-VALIDATION.md
@apps/worker/src/__tests__/promotion.signals.test.ts
@packages/db/src/seed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: startPromotionRun helper (PIPE-02, D-15)</name>
  <read_first>
    - .planning/phases/04-promotion-engine/04-RESEARCH.md — startPromotionRun packaging
    - packages/db/src/repositories/promotion-run.repository.ts
    - apps/worker/src/workflows/promotion.workflow.ts — PromotionWorkflowInput
  </read_first>
  <files>apps/worker/src/lib/start-promotion-run.ts, apps/worker/src/scripts/start-run.ts, apps/worker/src/__tests__/start-promotion-run.test.ts, apps/worker/package.json</files>
  <action>
    Implement startPromotionRun({ promotionRunId, temporalAddress?, taskQueue?, actor }):
    - Load run from DB; verify status pending
    - Load pipeline stage count
    - Connect Temporal Client
    - updateState pending → active, set temporalWorkflowId = run.id
    - client.workflow.start(promotionWorkflow, { workflowId: run.id, taskQueue, args: [{ promotionRunId, stageCount, actor }] })
    - Return { workflowId, runId }

    Add script to package.json: `"start-run": "tsx src/scripts/start-run.ts"` with minimal CLI parsing promotionRunId from argv (or export lib only + test).

    Unit/integration test: pending run → active + workflow start (TestWorkflowEnvironment or mock client).

    Document TEMPORAL_ADDRESS / TEMPORAL_TASK_QUEUE env usage.
  </action>
  <acceptance_criteria>
    - Pending run transitions to active before workflow start
    - workflowId equals promotion run id
    - Test covers happy path
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project worker start-promotion-run</automated>
  </verify>
  <done>PIPE-02 start mechanism implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: promotion-engine integration tests (PIPE-02/03/04)</name>
  <read_first>
    - apps/worker/src/__tests__/promotion.workflow.test.ts — testcontainers seed
    - packages/telemetry/src/__tests__/telemetry-integration.test.ts
    - apps/worker/src/__tests__/helpers/nock-*.ts
  </read_first>
  <files>apps/worker/src/__tests__/promotion-engine.integration.test.ts</files>
  <behavior>
    - PIPE-02: startPromotionRun + workflow completes when gates pass
    - PIPE-03: stage index advances only after gate pass
    - PIPE-04: gate fail leaves run paused with pauseReason
    - D-12: two GateResult rows per stage when two policies (error_rate + latency_p95)
  </behavior>
  <action>
    Write promotion-engine.integration.test.ts:
    - Use TestWorkflowEnvironment + real Worker registering all activities
    - testcontainers DB with seeded pipeline (mirror seed policies)
    - nock LD GET/PATCH + Prometheus query intercepts for pass and fail scenarios
    - Tag tests with PIPE-02, PIPE-03, PIPE-04 requirement IDs
    - Assert nock.isDone() on happy paths

    Do NOT require live LD or Prometheus.
  </action>
  <acceptance_criteria>
    - Full stage loop passes with nock
    - Gate fail scenario asserts paused + pauseReason
    - GateResult count matches policies evaluated
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project worker promotion-engine.integration</automated>
  </verify>
  <done>E2E integration tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: SAFE-02 abort tests + README Phase 4 section</name>
  <read_first>
    - apps/worker/src/__tests__/promotion.signals.test.ts
    - README.md — telemetry section pattern
  </read_first>
  <files>apps/worker/src/__tests__/promotion.signals.test.ts, README.md</files>
  <action>
    Extend promotion.signals.test.ts:
    - SAFE-02: abort during active stage processing — send abortSignal, assert run status aborted, workflow completes without further stage advancement
    - Use mocked or nock activities as appropriate

    Update README.md:
    - Phase 4 promotion engine section
    - Worker test command, start-run script, env vars (LD + Prometheus + Temporal)
    - Note CI uses nock; live LD token needed for manual e2e
    - Clarify REST API deferred to Phase 5
  </action>
  <acceptance_criteria>
    - SAFE-02 test tagged in promotion.signals.test.ts
    - README documents start-run and worker test command
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project worker promotion.signals && pnpm exec vitest run --project worker</automated>
  </verify>
  <done>Abort covered; docs updated; full worker suite green.</done>
</task>

</tasks>

<verification>
- pnpm exec vitest run --project worker
- pnpm exec vitest run --project telemetry
- pnpm exec vitest run --project ld-adapter
</verification>

<success_criteria>
- All Phase 4 requirements have automated test coverage
- Worker package ready for Phase 5 API wiring
</success_criteria>

<output>
Create `.planning/phases/04-promotion-engine/04-04-SUMMARY.md` when done
</output>
