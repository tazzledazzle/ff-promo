---
phase: 04-promotion-engine
plan: 02
type: execute
wave: 1
depends_on:
  - "04-01"
files_modified:
  - apps/worker/src/activities/run-preflight.ts
  - apps/worker/src/activities/apply-stage-targeting.ts
  - apps/worker/src/activities/index.ts
  - apps/worker/src/__tests__/run-preflight.activity.test.ts
  - apps/worker/src/__tests__/apply-stage-targeting.activity.test.ts
  - apps/worker/src/__tests__/fixtures/ld-flag-boolean.json
  - apps/worker/src/__tests__/fixtures/ld-patch-success.json
  - apps/worker/src/__tests__/helpers/nock-launchdarkly.ts
  - apps/worker/src/__tests__/helpers/nock-prometheus.ts
autonomous: true
requirements:
  - PIPE-03
must_haves:
  truths:
    - "runPreflight activity calls telemetry runPreflightChecks and records audit on fail (D-05/D-06/D-07)"
    - "applyStageTargeting activity writes LD targeting for current stage environment (D-08/D-10)"
    - "Activities use shared lib loaders and client factories (D-20)"
  artifacts:
    - path: "apps/worker/src/activities/run-preflight.ts"
      provides: "TELE-04 workflow activity"
      exports: ["runPreflight"]
    - path: "apps/worker/src/activities/apply-stage-targeting.ts"
      provides: "LD stage targeting activity"
      exports: ["applyStageTargeting"]
  key_links:
    - from: "apps/worker/src/activities/run-preflight.ts"
      to: "packages/telemetry/src/preflight/run-preflight.ts"
      via: "runPreflightChecks"
      pattern: "runPreflightChecks"
    - from: "apps/worker/src/activities/apply-stage-targeting.ts"
      to: "packages/ld-adapter/src/provider/launch-darkly-provider.ts"
      via: "applyTargeting"
      pattern: "applyTargeting"
---

<objective>
Implement `runPreflight` and `applyStageTargeting` Temporal activities with nock-based unit tests — wiring Phase 3 pre-flight and Phase 2 LD writes into the worker tier.

Purpose: D-05/D-08 require activities before workflow extension. Pre-flight fail path must abort without GateResult rows.
Output: Two new activities exported from activities/index.ts, test helpers, JSON fixtures.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-promotion-engine/04-CONTEXT.md
@.planning/phases/04-promotion-engine/04-RESEARCH.md
@apps/worker/src/lib/clients.ts
@apps/worker/src/lib/load-run-context.ts
@apps/worker/src/lib/stage-targeting.ts
@packages/telemetry/src/preflight/run-preflight.ts
@packages/ld-adapter/src/__tests__/launch-darkly-provider.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: nock test helpers and fixtures</name>
  <read_first>
    - packages/ld-adapter/src/__tests__/fixtures/flag-boolean.json
    - packages/telemetry/src/__tests__/telemetry-integration.test.ts — promql routing
    - .planning/phases/04-promotion-engine/04-PATTERNS.md — nock helpers
  </read_first>
  <files>apps/worker/src/__tests__/helpers/nock-launchdarkly.ts, apps/worker/src/__tests__/helpers/nock-prometheus.ts, apps/worker/src/__tests__/fixtures/ld-flag-boolean.json, apps/worker/src/__tests__/fixtures/ld-patch-success.json</files>
  <action>
    Copy/adapt LD fixtures from ld-adapter for worker tests.
    Create nock-launchdarkly.ts: intercept GET flag + PATCH semantic patch for baseUrl (configurable, default https://app.launchdarkly.com).
    Create nock-prometheus.ts: intercept GET /api/v1/query with query-string router for treatment/control/sample PromQL fragments (reuse telemetry fixture shapes).

    Export setup/teardown helpers used in activity tests.
  </action>
  <acceptance_criteria>
    - Fixtures exist under apps/worker/src/__tests__/fixtures/
    - Helpers export functions to register LD and Prometheus nock intercepts
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/worker/src/__tests__/helpers/nock-launchdarkly.ts && test -f apps/worker/src/__tests__/helpers/nock-prometheus.ts</automated>
  </verify>
  <done>Test infrastructure ready for activity tests.</done>
</task>

<task type="auto">
  <name>Task 2: runPreflight activity (TELE-04, D-05/D-06/D-07)</name>
  <read_first>
    - .planning/phases/04-promotion-engine/04-CONTEXT.md — preflight decisions
    - packages/telemetry/src/preflight/run-preflight.ts
    - apps/worker/src/activities/record-audit-event.ts — audit append pattern
  </read_first>
  <files>apps/worker/src/activities/run-preflight.ts, apps/worker/src/__tests__/run-preflight.activity.test.ts, apps/worker/src/activities/index.ts</files>
  <action>
    Implement runPreflight({ promotionRunId }):
    - Load all gate policies across pipeline stages (or max minSampleSize policies from first stage — use all policies from pipeline for preflight per RESEARCH)
    - Build GateRunContext: need variation IDs — call getFlagState for first stage environment OR resolve once using stage 0
    - createWorkerPrometheusClient + runPreflightChecks
    - On pass: return { status: 'pass', report }
    - On fail: record audit event with metadata { cause: 'preflight_failed', report }; return { status: 'fail', report } — do NOT create GateResult rows
    - Use loadRunStageContext / extended loader for policies list

    Activity test with nock Prometheus: pass and fail paths.
    Export from activities/index.ts.
  </action>
  <acceptance_criteria>
    - Preflight pass returns status pass
    - Preflight fail returns status fail with blockReason in report
    - No GateResult rows created on preflight fail (assert in test via db count)
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project worker run-preflight.activity</automated>
  </verify>
  <done>runPreflight activity tested with nock.</done>
</task>

<task type="auto">
  <name>Task 3: applyStageTargeting activity (D-08, PROV-02/03)</name>
  <read_first>
    - packages/ld-adapter/src/write/apply-targeting.ts
    - apps/worker/src/lib/stage-targeting.ts
  </read_first>
  <files>apps/worker/src/activities/apply-stage-targeting.ts, apps/worker/src/__tests__/apply-stage-targeting.activity.test.ts, apps/worker/src/activities/index.ts</files>
  <action>
    Implement applyStageTargeting({ promotionRunId, stageIndex }):
    - loadRunStageContext
    - createWorkerLdProvider
    - buildStageTargetingIntent(stage)
    - provider.applyTargeting({ projectKey, flagKey, intent })
    - Record audit `stage_entered` or dedicated `targeting_applied` metadata if needed (optional — workflow may already audit stage_entered)
    - Return { environmentKey, treatmentVariationId, controlVariationId } resolved from flag state after patch for downstream evaluateGate

    Activity test with nock LD: verify PATCH called with semantic patch content type.
    Export from activities/index.ts.
  </action>
  <acceptance_criteria>
    - Activity calls applyTargeting for stage.environment
    - Returns resolved variation IDs for gate context
    - nock.isDone() on happy path
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project worker apply-stage-targeting.activity</automated>
  </verify>
  <done>applyStageTargeting activity tested with nock.</done>
</task>

</tasks>

<verification>
- pnpm exec vitest run --project worker run-preflight apply-stage-targeting
- pnpm --filter @ff-promo/worker run build
</verification>

<success_criteria>
- TELE-04 and LD write activities callable from worker with nock tests green
</success_criteria>

<output>
Create `.planning/phases/04-promotion-engine/04-02-SUMMARY.md` when done
</output>
