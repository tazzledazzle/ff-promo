---
phase: 04-promotion-engine
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/worker/package.json
  - apps/worker/src/lib/clients.ts
  - apps/worker/src/lib/load-run-context.ts
  - apps/worker/src/lib/stage-targeting.ts
  - apps/worker/src/lib/__tests__/stage-targeting.test.ts
  - apps/worker/src/lib/__tests__/load-run-context.test.ts
  - packages/contracts/src/promotion-engine.ts
  - packages/contracts/src/index.ts
  - pnpm-lock.yaml
autonomous: true
requirements: []
must_haves:
  truths:
    - "Worker depends on @ff-promo/ld-adapter and @ff-promo/telemetry"
    - "Shared client factories read env at activity boundary only (D-20)"
    - "loadRunStageContext loads run, pipeline, stage, and gate policies from Postgres"
    - "buildStageTargetingIntent and buildGateRunContext produce v1 rollout + cohort context per stage (D-09)"
  artifacts:
    - path: "apps/worker/src/lib/clients.ts"
      provides: "createWorkerLdProvider and createWorkerPrometheusClient factories"
      exports: ["createWorkerLdProvider", "createWorkerPrometheusClient"]
    - path: "apps/worker/src/lib/load-run-context.ts"
      provides: "loadRunStageContext for activities"
      exports: ["loadRunStageContext"]
    - path: "apps/worker/src/lib/stage-targeting.ts"
      provides: "TargetingIntent and GateRunContext builders"
      exports: ["buildStageTargetingIntent", "buildGateRunContextFromFlagState"]
  key_links:
    - from: "apps/worker/src/lib/stage-targeting.ts"
      to: "packages/contracts/src/launchdarkly.ts"
      via: "TargetingIntentSchema"
      pattern: "TargetingIntent"
    - from: "apps/worker/src/lib/load-run-context.ts"
      to: "packages/db"
      via: "PromotionRun findUnique with pipeline stages"
      pattern: "promotionRun.findUnique"
---

<objective>
Add worker dependencies and shared lib modules — client factories, run/stage context loader, and stage targeting mappers — so new activities in plans 02–03 share one code path for LD + telemetry integration.

Purpose: D-09/D-20 require consistent variation resolution and env-injected clients before activities land. Locks RESEARCH Pattern 3 (turnOn + 50/50 fallthrough rollout, user context kind).
Output: `apps/worker/src/lib/*` with unit tests; optional thin contracts for activity input DTOs.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-promotion-engine/04-CONTEXT.md
@.planning/phases/04-promotion-engine/04-RESEARCH.md
@.planning/phases/04-promotion-engine/04-PATTERNS.md
@packages/contracts/src/launchdarkly.ts
@packages/contracts/src/telemetry.ts
@packages/contracts/src/pipeline.ts
@apps/worker/src/activities/evaluate-gate.ts
@packages/db/src/seed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Worker package deps and activity input contracts</name>
  <read_first>
    - .planning/phases/04-promotion-engine/04-RESEARCH.md — Standard Stack, Package Legitimacy Audit
    - apps/worker/package.json — add workspace deps
    - packages/ld-adapter/package.json — nock version pin
  </read_first>
  <files>apps/worker/package.json, packages/contracts/src/promotion-engine.ts, packages/contracts/src/index.ts, pnpm-lock.yaml</files>
  <action>
    Add to apps/worker/package.json dependencies: `@ff-promo/ld-adapter`, `@ff-promo/telemetry` (workspace:*).
    Add devDependency: `nock@14.0.15` (Approved — same as ld-adapter/telemetry).

    Create packages/contracts/src/promotion-engine.ts with Zod schemas for shared activity inputs:
    - RunStageInputSchema: { promotionRunId: string, stageIndex: number }
    - PreflightActivityResultSchema: { status: 'pass' | 'fail', report: PreflightReportSchema (import from telemetry) }
    - EvaluateGateActivityResultSchema: { verdict: 'pass' | 'fail', gateResultIds: string[], pauseReason?: string }

    Export from packages/contracts/src/index.ts. Run pnpm install.
  </action>
  <acceptance_criteria>
    - worker package.json lists ld-adapter and telemetry
    - promotion-engine.ts exports RunStageInputSchema
    - pnpm install succeeds
  </acceptance_criteria>
  <verify>
    <automated>pnpm install && pnpm --filter @ff-promo/contracts exec tsc --noEmit</automated>
  </verify>
  <done>Worker deps and shared activity DTOs available.</done>
</task>

<task type="auto">
  <name>Task 2: Client factories and run context loader</name>
  <read_first>
    - .planning/phases/04-promotion-engine/04-CONTEXT.md — D-20
    - packages/ld-adapter/src/provider/launch-darkly-provider.ts
    - packages/telemetry/src/client/prometheus-client.ts
    - apps/worker/src/activities/evaluate-gate.ts — include pattern for run load
  </read_first>
  <files>apps/worker/src/lib/clients.ts, apps/worker/src/lib/load-run-context.ts, apps/worker/src/lib/__tests__/load-run-context.test.ts</files>
  <action>
    Create apps/worker/src/lib/clients.ts:
    - createWorkerLdProvider() — reads LD_ACCESS_TOKEN, LD_BASE_URL, LD_API_VERSION from env; throws if token missing
    - createWorkerPrometheusClient() — delegates to createPrometheusClient with env fallback

    Create apps/worker/src/lib/load-run-context.ts:
    - export async function loadRunStageContext(promotionRunId, stageIndex) returning { run, pipeline, stage, gatePolicies, projectKey, flagKey }
    - Use createPrismaClient + findUniqueOrThrow with pipeline.stages (ordered) and stage.gatePolicies
    - Throw descriptive error if stageIndex not found
    - Disconnect prisma in finally

    Unit test load-run-context with mocked prisma OR integration test against testcontainers (prefer integration mirroring evaluate-gate stub pattern if feasible; otherwise mock db module).

    No process.env in exported pure mappers (stage-targeting task).
  </action>
  <acceptance_criteria>
    - Client factories throw when LD_ACCESS_TOKEN missing
    - loadRunStageContext returns gate policies array for stage
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project worker load-run-context</automated>
  </verify>
  <done>Shared loaders and factories compile and test.</done>
</task>

<task type="auto">
  <name>Task 3: Stage targeting and GateRunContext mappers (D-09, RESEARCH Pattern 3)</name>
  <read_first>
    - .planning/phases/04-promotion-engine/04-RESEARCH.md — Resolved TargetingIntent (turnOn + 50/50 fallthrough, user context)
    - packages/ld-adapter/src/resolve/variation-resolver.ts
    - packages/contracts/src/launchdarkly.ts — RolloutIntentSchema
    - packages/db/src/seed.ts — variation names in LD fixtures
  </read_first>
  <files>apps/worker/src/lib/stage-targeting.ts, apps/worker/src/lib/__tests__/stage-targeting.test.ts</files>
  <action>
    Create apps/worker/src/lib/stage-targeting.ts (pure functions):

    - buildStageTargetingIntent(stage, options?): TargetingIntent — environmentKey = stage.environment, turnOn: true, rollout with treatmentVariationRef/controlVariationRef from seed-aligned defaults (first two variations by name `true`/`false` or by index 0/1 — document choice), treatmentPercentThousandths: 50000 (50%), rolloutContextKind: 'user', rolloutBucketBy: 'key', mode: 'fallthrough'

    - buildGateRunContextFromFlagState(flagState, flagKey, treatmentVariationId, controlVariationId): GateRunContext — map resolved _ids from ld-adapter resolvers

    - export resolveStageVariationIds(flagState, treatmentRef, controlRef) using resolveVariationId from ld-adapter

    Write stage-targeting.test.ts with fixture FlagState JSON (minimal) asserting TargetingIntent shape and GateRunContext output.

    Do NOT call HTTP or read process.env in this module.
  </action>
  <acceptance_criteria>
    - TargetingIntent includes turnOn and 50/50 fallthrough rollout with user context kind
    - GateRunContext includes treatment and control variation IDs
    - Pure functions covered by unit tests
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project worker stage-targeting</automated>
  </verify>
  <done>Stage targeting mappers ready for activities.</done>
</task>

</tasks>

<verification>
- pnpm --filter @ff-promo/worker run build
- pnpm exec vitest run --project worker lib
</verification>

<success_criteria>
- Worker lib foundation supports D-09 variation resolution and D-20 env-at-boundary clients
- No new npm packages beyond nock devDep
</success_criteria>

<output>
Create `.planning/phases/04-promotion-engine/04-01-SUMMARY.md` when done
</output>
