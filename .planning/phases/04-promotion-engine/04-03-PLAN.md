---
phase: 04-promotion-engine
plan: 03
type: execute
wave: 2
depends_on:
  - "04-02"
files_modified:
  - apps/worker/src/activities/evaluate-gate.ts
  - apps/worker/src/__tests__/evaluate-gate.activity.test.ts
  - apps/worker/src/workflows/promotion.workflow.ts
  - apps/worker/src/worker.ts
  - apps/worker/src/__tests__/promotion.workflow.test.ts
autonomous: true
requirements:
  - PIPE-03
  - PIPE-04
must_haves:
  truths:
    - "evaluateGate calls evaluateStageGates and persists one GateResult per policy (D-11/D-12/D-13)"
    - "Gate fail returns verdict fail with pauseReason from first failing policy (D-04)"
    - "Workflow runs preflight once then per stage: applyStageTargeting → evaluateGate (D-01/D-05/D-21)"
    - "Stage index advances only when evaluateGate verdict is pass (PIPE-03)"
    - "Gate fail sets paused status with pauseReason persisted (PIPE-04)"
  artifacts:
    - path: "apps/worker/src/activities/evaluate-gate.ts"
      provides: "Real telemetry gate evaluation activity"
      exports: ["evaluateGate"]
    - path: "apps/worker/src/workflows/promotion.workflow.ts"
      provides: "Extended promotion FSM with preflight and targeting"
      contains: "runPreflight"
  key_links:
    - from: "apps/worker/src/workflows/promotion.workflow.ts"
      to: "apps/worker/src/activities/evaluate-gate.ts"
      via: "proxyActivities evaluateGate"
      pattern: "evaluateGate"
    - from: "apps/worker/src/activities/evaluate-gate.ts"
      to: "packages/telemetry/src/evaluate/evaluate-stage-gates.ts"
      via: "evaluateStageGates"
      pattern: "evaluateStageGates"
---

<objective>
Replace the evaluateGate stub with real telemetry evaluation + GateResult persistence, and extend promotionWorkflow to orchestrate preflight → apply targeting → gate evaluation per stage.

Purpose: PIPE-03/04 core engine loop. Workflow must set pauseReason on gate fail (D-04).
Output: Real evaluateGate, updated workflow + worker registration, activity and workflow tests.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-promotion-engine/04-CONTEXT.md
@apps/worker/src/workflows/promotion.workflow.ts
@apps/worker/src/activities/evaluate-gate.ts
@packages/telemetry/src/evaluate/evaluate-stage-gates.ts
@packages/db/src/repositories/gate-result.repository.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real evaluateGate activity (D-11/D-12/D-13/D-14)</name>
  <read_first>
    - .planning/phases/04-promotion-engine/04-RESEARCH.md — gate persistence pattern
    - packages/telemetry/src/evaluate/evaluate-stage-gates.ts
    - packages/contracts/src/gate-result.ts
  </read_first>
  <files>apps/worker/src/activities/evaluate-gate.ts, apps/worker/src/__tests__/evaluate-gate.activity.test.ts</files>
  <action>
    Replace stub evaluateGate implementation:
    - Input: { promotionRunId, stageIndex, treatmentVariationId?, controlVariationId? } — use IDs from applyStageTargeting when provided, else resolve via getFlagState
    - loadRunStageContext → gatePolicies
    - build GateRunContext
    - evaluateStageGates(createWorkerPrometheusClient(), policies, runContext)
    - For each result: GateResultRepository.create with verdict pass/fail mapped to GateVerdictSchema, observedValue = observedDelta or treatmentValue, metadata includes treatmentValue, controlValue, observedDelta, reason
    - recordAuditEvent gate_evaluated per GateResult with gateResultId
    - Return { verdict: stage.verdict, gateResultIds, pauseReason: first failing policy metadata.reason }

    Activity tests with nock Prometheus: pass (all policies), fail (threshold_exceeded), fail closed (no_data).
    Assert GateResult count equals policy count.
  </action>
  <acceptance_criteria>
    - One GateResult row per policy per evaluation
    - gate_evaluated audit events include gateResultId
    - Fail returns pauseReason string
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project worker evaluate-gate.activity</automated>
  </verify>
  <done>evaluateGate persists real telemetry results.</done>
</task>

<task type="auto">
  <name>Task 2: Extend promotionWorkflow (D-01/D-05/D-21)</name>
  <read_first>
    - .planning/phases/04-promotion-engine/04-CONTEXT.md — stage ordering
    - apps/worker/src/workflows/promotion.workflow.ts — existing loop
  </read_first>
  <files>apps/worker/src/workflows/promotion.workflow.ts, apps/worker/src/worker.ts</files>
  <action>
    Update promotion.workflow.ts:
    - Proxy new activities: runPreflight, applyStageTargeting (in addition to existing)
    - After run_started audit: call runPreflight; if status fail → persist aborted + audit + return (D-06)
    - Inside stage loop BEFORE evaluateGate: call applyStageTargeting; pass variation IDs to evaluateGate
    - **D-17:** Call `if (hasAborted()) break;` immediately before `applyStageTargeting` and before `evaluateGate` so abort during pause/stage_entered cannot trigger further LD writes or gate evaluation in the same iteration
    - On evaluateGate fail: persist paused with pauseReason from activity result (not just status paused)
    - Keep gatePassed/gateFailed signal handlers but do not rely on them for v1 flow (D-21)
    - Do NOT import adapter packages in workflow file

    Register new activities in worker.ts with same timeout/retry as existing activities.
  </action>
  <acceptance_criteria>
    - Workflow calls runPreflight before stage loop
    - Each stage calls applyStageTargeting then evaluateGate
    - pauseReason set on gate fail via persistRunState
    - hasAborted() guards before applyStageTargeting and evaluateGate (D-17)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @ff-promo/worker run build</automated>
  </verify>
  <done>Workflow orchestrates full stage pipeline.</done>
</task>

<task type="auto">
  <name>Task 3: Update workflow tests for new activity graph</name>
  <read_first>
    - apps/worker/src/__tests__/promotion.workflow.test.ts
    - apps/worker/src/__tests__/promotion.signals.test.ts
  </read_first>
  <files>apps/worker/src/__tests__/promotion.workflow.test.ts</files>
  <action>
    Update promotion.workflow.test.ts:
    - Mock or nock-backed activities for runPreflight, applyStageTargeting, evaluateGate
    - Existing complete-all-stages test should still pass when gates pass
    - Add test: gate fail pauses run (PIPE-04) — mock evaluateGate returning fail, assert paused status in DB
    - Add test: preflight fail aborts without advancing stage index
    - Add test: abort signal before applyStageTargeting prevents LD write in same iteration (D-17) — mock activities or spy on applyStageTargeting call count after abort

    Prefer mocked activities for workflow unit tests if nock+DB is covered in plan 04 integration test — keep workflow tests fast.
  </action>
  <acceptance_criteria>
    - Workflow test suite passes with new activity proxies
    - Gate fail pause scenario covered
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project worker promotion.workflow</automated>
  </verify>
  <done>Workflow tests reflect new orchestration.</done>
</task>

</tasks>

<verification>
- pnpm exec vitest run --project worker
- pnpm --filter @ff-promo/worker run build
</verification>

<success_criteria>
- PIPE-03/04 satisfied at workflow + activity layer
- Stub evaluateGate fully replaced
</success_criteria>

<output>
Create `.planning/phases/04-promotion-engine/04-03-SUMMARY.md` when done
</output>
