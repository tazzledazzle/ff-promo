---
phase: 02-launchdarkly-adapter
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/contracts/src/launchdarkly.ts
  - packages/contracts/src/index.ts
  - packages/ld-adapter/package.json
  - packages/ld-adapter/tsconfig.json
  - packages/ld-adapter/src/index.ts
  - packages/ld-adapter/src/client/ld-api-client.ts
  - packages/ld-adapter/src/errors/ld-adapter-error.ts
  - packages/ld-adapter/src/provider/flag-provider.ts
  - packages/ld-adapter/src/__tests__/ld-api-client.test.ts
  - vitest.config.ts
  - .env.example
  - pnpm-lock.yaml
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Shared FlagState and semantic patch types exist in @ff-promo/contracts"
    - "packages/ld-adapter builds as a workspace package with launchdarkly-api@20.0.0"
    - "LD client factory pins LD-API-Version 20240415 and supports LD_BASE_URL override"
    - "Vitest ld-adapter project runs in CI without a live LaunchDarkly account"
  artifacts:
    - path: "packages/contracts/src/launchdarkly.ts"
      provides: "FlagState, TargetingIntent, SemanticPatchInstruction Zod schemas"
      contains: "FlagStateSchema"
    - path: "packages/ld-adapter/src/client/ld-api-client.ts"
      provides: "createLaunchDarklyClient factory"
      exports: ["createLaunchDarklyClient"]
    - path: "packages/ld-adapter/package.json"
      provides: "@ff-promo/ld-adapter workspace package"
      contains: "launchdarkly-api"
  key_links:
    - from: "packages/ld-adapter/src/client/ld-api-client.ts"
      to: "launchdarkly-api"
      via: "FeatureFlagsApi + ApiClient headers"
      pattern: "LD-API-Version"
    - from: "packages/contracts/src/index.ts"
      to: "packages/contracts/src/launchdarkly.ts"
      via: "barrel export"
      pattern: "launchdarkly"
---

<objective>
Scaffold `packages/ld-adapter`, add LaunchDarkly domain contracts to `@ff-promo/contracts`, and implement a typed LD REST client factory — the foundation all read/write/resolver work builds on.

Purpose: Phase 2 has no adapter package yet; contracts and client factory must exist before PROV-01/02/03 implementation. Locks research decisions: `launchdarkly-api@20.0.0`, `LD-API-Version: 20240415`, `LD_BASE_URL` for EU, no `@launchdarkly/node-server-sdk`.
Output: Buildable `@ff-promo/ld-adapter` package shell, contracts schemas, `createLaunchDarklyClient`, `FlagProvider` interface stub, typed error hierarchy skeleton.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-launchdarkly-adapter/02-RESEARCH.md
@.planning/phases/02-launchdarkly-adapter/02-PATTERNS.md
@packages/db/src/repositories/pipeline.repository.ts
@packages/contracts/src/pipeline.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: LaunchDarkly contracts in @ff-promo/contracts</name>
  <read_first>
    - .planning/phases/02-launchdarkly-adapter/02-RESEARCH.md — Pattern 1 FlagState, Pattern 2 semantic patch, Pattern 3 resolver refs
    - .planning/phases/02-launchdarkly-adapter/02-PATTERNS.md — launchdarkly.ts analog from pipeline.ts
    - packages/contracts/src/pipeline.ts — Zod schema + inferred type pattern
  </read_first>
  <files>packages/contracts/src/launchdarkly.ts, packages/contracts/src/index.ts</files>
  <action>
    Create packages/contracts/src/launchdarkly.ts with Zod schemas and exported types:

    - FlagVariationSchema: { id, name?, value } (maps LD _id to id in domain model)
    - FlagRuleSchema: { id, description?, clauses, variationOrRollout }
    - FlagEnvironmentStateSchema: { on, rules, fallthrough, offVariation?, targets? }
    - FlagStateSchema: { projectKey, flagKey, variations, environments: Record&lt;string, FlagEnvironmentState&gt; }
    - VariationRefSchema: discriminated union { by: 'id'|'name'|'value', ... }
    - RuleRefSchema: { by: 'id'|'description', ... } for env-scoped rule lookup
    - RolloutIntentSchema: { mode: 'fallthrough' (v1 default per RESEARCH open Q1), treatmentVariationRef, controlVariationRef, treatmentPercentThousandths, rolloutContextKind, rolloutBucketBy } — fallthrough is default; ruleRef optional for future rule-targeted rollouts
    - SemanticPatchInstructionSchema: discriminated union on kind using official instruction names: turnFlagOn, turnFlagOff, updateFallthroughVariationOrRollout, updateRuleVariationOrRollout — do NOT use updatePercentageRollout (stale STACK.md name)
    - TargetingIntentSchema: { environmentKey, comment?, turnOn?, rollout?: RolloutIntentSchema }
    - GetFlagStateInputSchema: { projectKey, flagKey, environmentKey }
    - ApplyTargetingInputSchema: { projectKey, flagKey, intent: TargetingIntentSchema }
    - LaunchDarklyClientConfigSchema: { accessToken, baseUrl?, apiVersion? }

    Reuse StageEnvironmentSchema from pipeline.ts where environment keys align; export environment as string key for LD env keys beyond dev/staging/prod.

    Export all schemas/types from packages/contracts/src/index.ts via export * from './launchdarkly.js'.

    Do NOT add Prisma schema changes.
  </action>
  <acceptance_criteria>
    - launchdarkly.ts exports FlagStateSchema, SemanticPatchInstructionSchema, TargetingIntentSchema, LaunchDarklyClientConfigSchema
    - Instruction kinds use updateFallthroughVariationOrRollout not updatePercentageRollout
    - RolloutIntentSchema defaults mode to fallthrough per RESEARCH v1 decision
    - contracts index re-exports launchdarkly module
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @ff-promo/contracts exec tsc --noEmit</automated>
  </verify>
  <done>Contracts compile; FlagState and semantic patch types available to ld-adapter.</done>
</task>

<task type="auto">
  <name>Task 2: ld-adapter package shell, deps, vitest project</name>
  <read_first>
    - .planning/phases/02-launchdarkly-adapter/02-RESEARCH.md — Standard Stack, Package Legitimacy Audit, Wave 0 Gaps
    - .planning/phases/02-launchdarkly-adapter/02-PATTERNS.md — package.json and vitest.config analogs
    - packages/db/package.json — workspace package manifest pattern
    - vitest.config.ts — existing db/worker projects
  </read_first>
  <files>packages/ld-adapter/package.json, packages/ld-adapter/tsconfig.json, packages/ld-adapter/src/index.ts, vitest.config.ts, .env.example, pnpm-lock.yaml</files>
  <action>
    Create packages/ld-adapter with name @ff-promo/ld-adapter, type module, exports pointing to dist/index.js.

    package.json scripts: build=tsc, test=pnpm -w exec vitest run --project ld-adapter, lint=biome check src.

    dependencies: launchdarkly-api@20.0.0, @ff-promo/contracts workspace:*, zod@4.4.3, bottleneck@2.19.5, p-retry@8.0.0 (Approved per RESEARCH Package Legitimacy Audit — install now; nock deferred to plan 04).

    devDependencies: typescript ~5.8.3. Do NOT add @launchdarkly/node-server-sdk anywhere.

    tsconfig.json extends ../../tsconfig.base.json with outDir dist, rootDir src.

    Add vitest project { name: "ld-adapter", root: "./packages/ld-adapter", include: ["src/**/*.test.ts"] } to vitest.config.ts.

    Update .env.example with LD_ACCESS_TOKEN=, LD_BASE_URL=https://app.launchdarkly.com, LD_API_VERSION=20240415, LD_PROJECT_KEY=default — document EU override https://app.eu.launchdarkly.com per RESEARCH.

    Placeholder packages/ld-adapter/src/index.ts exports createLaunchDarklyClient only (expanded in later plans).

    Run pnpm install at repo root. Verify launchdarkly-api resolves to 20.0.0.
  </action>
  <acceptance_criteria>
    - @ff-promo/ld-adapter exists under packages/ld-adapter
    - launchdarkly-api@20.0.0, bottleneck@2.19.5, p-retry@8.0.0 in dependencies
    - vitest.config.ts includes ld-adapter project
    - .env.example documents LD_BASE_URL and LD_API_VERSION
    - No @launchdarkly/node-server-sdk in any package.json
  </acceptance_criteria>
  <verify>
    <automated>pnpm install && pnpm --filter @ff-promo/ld-adapter exec node -e "const p=require('./package.json'); if(p.dependencies['launchdarkly-api']!=='20.0.0') process.exit(1)" && grep -q 'ld-adapter' vitest.config.ts</automated>
  </verify>
  <done>ld-adapter package installs; vitest project registered; env vars documented.</done>
</task>

<task type="auto">
  <name>Task 3: LD client factory, FlagProvider interface, error types</name>
  <read_first>
    - .planning/phases/02-launchdarkly-adapter/02-RESEARCH.md — Configure launchdarkly-api Client, Security Domain
    - .planning/phases/02-launchdarkly-adapter/02-PATTERNS.md — client.ts factory analog from db/client.ts
    - packages/db/src/client.ts — factory DI pattern (config param, no process.env inside)
  </read_first>
  <files>packages/ld-adapter/src/client/ld-api-client.ts, packages/ld-adapter/src/errors/ld-adapter-error.ts, packages/ld-adapter/src/provider/flag-provider.ts, packages/ld-adapter/src/index.ts, packages/ld-adapter/src/__tests__/ld-api-client.test.ts</files>
  <action>
    Implement createLaunchDarklyClient(config) in ld-api-client.ts:

    - Parse config with LaunchDarklyClientConfigSchema from contracts
    - Set LaunchDarklyApi.ApiClient.instance.authentications['ApiKey'].apiKey from config.accessToken
    - Set defaultHeaders['LD-API-Version'] = config.apiVersion ?? '20240415'
    - Set basePath/base URL from config.baseUrl ?? process.env.LD_BASE_URL ?? 'https://app.launchdarkly.com' (caller may pass explicit baseUrl in tests; document env for EU)
    - Export typed wrapper { flagsApi: FeatureFlagsApi, config } — do not read accessToken from process.env inside factory (testability)
    - Define SEMANTIC_PATCH_CONTENT_TYPE constant for use in plan 03 writes

    Create FlagProvider interface in provider/flag-provider.ts with getFlagState and applyTargeting method signatures using contracts types — implementation deferred to plans 02-03.

    Create errors/ld-adapter-error.ts with base LdAdapterError and subclasses: UnresolvedVariationError, UnresolvedRuleError, LdRateLimitError, LdApiError (status + body), ApprovalRequiredError (405 per RESEARCH — defer approval flow to v2, only typed error here).

    Write ld-api-client.test.ts verifying factory sets LD-API-Version header and respects baseUrl override via inspecting ApiClient.instance after createLaunchDarklyClient({ accessToken: 'test', baseUrl: 'https://app.eu.launchdarkly.com' }).

    Export createLaunchDarklyClient, FlagProvider type, and error classes from index.ts.
  </action>
  <acceptance_criteria>
    - createLaunchDarklyClient accepts config object; does not require live LD token in unit tests
    - LD-API-Version defaults to 20240415
    - LD_BASE_URL env documented and overridable via config.baseUrl
    - ApprovalRequiredError class exists for future 405 mapping
    - FlagProvider interface declares getFlagState and applyTargeting
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project ld-adapter src/__tests__/ld-api-client.test.ts && pnpm --filter @ff-promo/ld-adapter run build</automated>
  </verify>
  <done>Client factory and error hierarchy compile; smoke test passes; package builds via tsc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env/secret → ld-adapter | LD access token crosses trust boundary at client construction |
| npm registry → monorepo | External packages installed via pnpm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | LD access token | mitigate | Config param injection; never log Authorization header; .env.example uses empty token |
| T-02-02 | Tampering | Semantic patch payloads | mitigate | Zod schemas in contracts; validation at adapter boundaries in later plans |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | Package Legitimacy Audit in RESEARCH — launchdarkly-api, bottleneck, p-retry Approved; nock installed in plan 04 with human verify |
</threat_model>

<verification>
- pnpm --filter @ff-promo/ld-adapter run build
- pnpm exec vitest run --project ld-adapter
</verification>

<success_criteria>
- @ff-promo/ld-adapter package exists and builds
- Contracts define FlagState and semantic patch instruction types with correct LD instruction kind names
- Client factory pins API version and supports configurable base URL
</success_criteria>

<output>
Create `.planning/phases/02-launchdarkly-adapter/02-01-SUMMARY.md` when done
</output>
