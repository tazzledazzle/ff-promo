---
phase: 02-launchdarkly-adapter
plan: 02
type: execute
wave: 1
depends_on:
  - "02-01"
files_modified:
  - packages/ld-adapter/src/read/get-flag-state.ts
  - packages/ld-adapter/src/read/mappers.ts
  - packages/ld-adapter/src/resolve/variation-resolver.ts
  - packages/ld-adapter/src/resolve/rule-resolver.ts
  - packages/ld-adapter/src/__tests__/fixtures/flag-boolean.json
  - packages/ld-adapter/src/__tests__/fixtures/flag-multivariate.json
  - packages/ld-adapter/src/__tests__/get-flag-state.test.ts
  - packages/ld-adapter/src/__tests__/mappers.test.ts
  - packages/ld-adapter/src/__tests__/variation-resolver.test.ts
  - packages/ld-adapter/src/__tests__/rule-resolver.test.ts
  - packages/ld-adapter/src/index.ts
autonomous: true
requirements:
  - PROV-01
  - PROV-03
must_haves:
  truths:
    - "Operator can read flag variations, targeting rules, and environment on/off state from LD GET responses"
    - "Variation refs resolve to exactly one project-scoped _id or fail closed"
    - "Rule refs resolve from target environment rules only, never cross-environment IDs"
    - "Ambiguous or missing variation/rule refs throw typed errors before any PATCH"
  artifacts:
    - path: "packages/ld-adapter/src/read/get-flag-state.ts"
      provides: "GET flag + normalize to FlagState"
      exports: ["getFlagState"]
    - path: "packages/ld-adapter/src/resolve/variation-resolver.ts"
      provides: "PROV-03 variation ID resolution"
      exports: ["resolveVariationId"]
    - path: "packages/ld-adapter/src/resolve/rule-resolver.ts"
      provides: "Env-scoped rule ID resolution"
      exports: ["resolveRuleId"]
  key_links:
    - from: "packages/ld-adapter/src/read/mappers.ts"
      to: "@ff-promo/contracts FlagStateSchema"
      via: "FlagStateSchema.parse after map"
      pattern: "FlagStateSchema\\.parse"
    - from: "packages/ld-adapter/src/resolve/rule-resolver.ts"
      to: "flag.environments[envKey].rules"
      via: "target env only"
      pattern: "environmentKey"
---

<objective>
Implement PROV-01 read path (GET flag state normalization) and PROV-03 ID resolution (variation + env-scoped rule resolvers) with unit tests using JSON fixtures — no live LaunchDarkly required.

Purpose: All writes in plan 03 depend on fresh GET state and fail-closed ID resolution; resolvers must be pure and testable before semantic patch work.
Output: getFlagState, mappers, resolveVariationId, resolveRuleId with Vitest coverage.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-launchdarkly-adapter/02-RESEARCH.md
@.planning/phases/02-launchdarkly-adapter/02-PATTERNS.md
@packages/contracts/src/launchdarkly.ts
@packages/ld-adapter/src/client/ld-api-client.ts
@packages/ld-adapter/src/errors/ld-adapter-error.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LD response mappers and getFlagState read path (PROV-01)</name>
  <read_first>
    - .planning/phases/02-launchdarkly-adapter/02-RESEARCH.md — Pattern 1 Read-Before-Write, Read Flag State code example
    - packages/contracts/src/launchdarkly.ts — FlagStateSchema shape
    - packages/ld-adapter/src/client/ld-api-client.ts — flagsApi injection pattern
  </read_first>
  <files>packages/ld-adapter/src/read/mappers.ts, packages/ld-adapter/src/read/get-flag-state.ts, packages/ld-adapter/src/__tests__/fixtures/flag-boolean.json, packages/ld-adapter/src/__tests__/fixtures/flag-multivariate.json, packages/ld-adapter/src/__tests__/mappers.test.ts, packages/ld-adapter/src/__tests__/get-flag-state.test.ts</files>
  <behavior>
    - Test: boolean flag fixture maps variations with id/value, environment on/off, rules array with rule ids, fallthrough rollout
    - Test: multivariate flag fixture maps named variations correctly
    - Test: getFlagState calls flagsApi.getFeatureFlag and returns FlagStateSchema-valid object
    - Test: missing environment key in LD response throws LdApiError or domain error with projectKey/flagKey/environmentKey context
  </behavior>
  <action>
    Implement mapLdFlagToFlagState(rawLdFlag, projectKey, flagKey) in mappers.ts:

    - Map flag.variations[] to { id: _id, name, value }
    - Map flag.environments entries to FlagEnvironmentState with on, rules (id from rule._id), fallthrough, offVariation, targets
    - Validate output with FlagStateSchema.parse before return

    Implement getFlagState({ client, projectKey, flagKey, environmentKey }) in get-flag-state.ts:

    - Parse input with GetFlagStateInputSchema
    - Call client.flagsApi.getFeatureFlag(projectKey, flagKey)
    - Map full flag; expose helper getEnvironmentState(flagState, environmentKey) that returns slice for one env
    - Do NOT cache across calls in v1 adapter (RESEARCH anti-pattern: stale IDs)

    Add fixtures flag-boolean.json and flag-multivariate.json derived from LD doc examples in RESEARCH (realistic _id shapes).

    Mock FeatureFlagsApi.getFeatureFlag in get-flag-state.test.ts — no nock yet.
  </action>
  <acceptance_criteria>
    - PROV-01: mapped FlagState includes variations, rules, environment on/off
    - Output always passes FlagStateSchema.parse
    - Fixtures cover boolean and multivariate flags
    - getFlagState uses injected client, not global singleton
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project ld-adapter src/__tests__/mappers.test.ts src/__tests__/get-flag-state.test.ts</automated>
  </verify>
  <done>Read path maps LD GET JSON to contracts FlagState; PROV-01 unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VariationResolver and RuleResolver (PROV-03)</name>
  <read_first>
    - .planning/phases/02-launchdarkly-adapter/02-RESEARCH.md — Pattern 3 VariationResolver + RuleResolver, Resolve Variation by Value example
    - .planning/phases/02-launchdarkly-adapter/02-PATTERNS.md — variation-resolver validation-before-persist analog
    - packages/ld-adapter/src/errors/ld-adapter-error.ts — UnresolvedVariationError, UnresolvedRuleError
  </read_first>
  <files>packages/ld-adapter/src/resolve/variation-resolver.ts, packages/ld-adapter/src/resolve/rule-resolver.ts, packages/ld-adapter/src/__tests__/variation-resolver.test.ts, packages/ld-adapter/src/__tests__/rule-resolver.test.ts, packages/ld-adapter/src/index.ts</files>
  <behavior>
    - Test: resolve by value returns single _id when exactly one match
    - Test: resolve by name and by id work on fixture FlagState
    - Test: zero matches throws UnresolvedVariationError
    - Test: two matches (ambiguous value) throws UnresolvedVariationError
    - Test: rule resolved by id from staging env rules succeeds
    - Test: staging rule id passed with prod environmentKey throws UnresolvedRuleError (fail closed — never use cross-env rule IDs per RESEARCH Pitfall 2)
  </behavior>
  <action>
    Implement resolveVariationId(flagState, ref: VariationRef) in variation-resolver.ts:

    - Match by id, name, or deep-equal value (JSON.stringify compare for objects)
    - Exactly one match required; else throw UnresolvedVariationError with ref details
    - Variation _id is project-scoped (same across envs) per RESEARCH

    Implement resolveRuleId(flagState, environmentKey, ref: RuleRef) in rule-resolver.ts:

    - Read ONLY flagState.environments[environmentKey].rules
    - Match by rule id or description (exactly one match)
    - Throw UnresolvedRuleError if env missing or match count !== 1
    - Never accept rule IDs from a different environment's rule list

    Export resolveVariationId and resolveRuleId from package index.

    Pure functions — no HTTP; tests use FlagState fixtures from plan 02 task 1.
  </action>
  <acceptance_criteria>
    - PROV-03: variation IDs resolved before write path consumes them
    - Rule IDs resolved from target environment only
    - Ambiguous/unresolved refs throw typed errors, not generic Error
    - Resolvers exported from @ff-promo/ld-adapter index
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project ld-adapter src/__tests__/variation-resolver.test.ts src/__tests__/rule-resolver.test.ts</automated>
  </verify>
  <done>PROV-03 resolvers fail closed; env-scoped rule resolution enforced by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LD GET response → mappers | External JSON normalized to internal types |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | LD response mapping | mitigate | FlagStateSchema.parse on every map output; reject malformed payloads |
| T-02-04 | Elevation | Wrong rule ID in prod | mitigate | RuleResolver scopes to environmentKey; tests assert cross-env rejection |
</threat_model>

<verification>
- pnpm exec vitest run --project ld-adapter
</verification>

<success_criteria>
- PROV-01 read mapping complete with fixture tests
- PROV-03 variation and rule resolvers fail closed on ambiguous/missing IDs
</success_criteria>

<output>
Create `.planning/phases/02-launchdarkly-adapter/02-02-SUMMARY.md` when done
</output>
