---
phase: 02-launchdarkly-adapter
plan: 04
type: execute
wave: 3
depends_on:
  - "02-03"
files_modified:
  - packages/ld-adapter/package.json
  - packages/ld-adapter/src/index.ts
  - packages/ld-adapter/src/__tests__/launch-darkly-provider.test.ts
  - packages/ld-adapter/src/__tests__/fixtures/patch-canary-success.json
  - packages/ld-adapter/src/__tests__/fixtures/patch-422-invalid.json
  - packages/ld-adapter/src/__tests__/fixtures/patch-429-retry.json
  - pnpm-lock.yaml
  - README.md
autonomous: false
requirements:
  - PROV-01
  - PROV-02
  - PROV-03
user_setup: []
must_haves:
  truths:
    - "Full provider flow verified with HTTP-level mocks (no live LD account in CI)"
    - "422 invalid patch returns error with zero successful mutations (no follow-up PATCH)"
    - "429 retry sequence completes without corrupting state"
    - "Package exports createLaunchDarklyProvider and all public adapter surface from index"
  artifacts:
    - path: "packages/ld-adapter/src/__tests__/launch-darkly-provider.test.ts"
      provides: "End-to-end adapter tests with nock"
      contains: "nock"
    - path: "packages/ld-adapter/src/index.ts"
      provides: "Public API barrel"
      exports: ["createLaunchDarklyProvider", "LaunchDarklyProvider"]
  key_links:
    - from: "packages/ld-adapter/src/__tests__/launch-darkly-provider.test.ts"
      to: "https://app.launchdarkly.com/api/v2"
      via: "nock HTTP intercept"
      pattern: "nock\\("
---

<objective>
Wire public exports and prove PROV-01/02/03 end-to-end with nock HTTP mocking — validating read, resolve, patch, and rate-limit behavior without a live LaunchDarkly account.

Purpose: Unit tests with vi.fn mocks are insufficient for header/body contract verification; HTTP-level tests gate phase completion and CI confidence.
Output: nock-based integration tests green, complete package exports, README env documentation updated.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-launchdarkly-adapter/02-RESEARCH.md
@.planning/phases/02-launchdarkly-adapter/02-PATTERNS.md
@packages/ld-adapter/src/provider/launch-darkly-provider.ts
@packages/ld-adapter/src/__tests__/fixtures/flag-boolean.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify nock package legitimacy before install</name>
  <read_first>
    - .planning/phases/02-launchdarkly-adapter/02-RESEARCH.md — Package Legitimacy Audit (nock marked [ASSUMED])
  </read_first>
  <action>Pause for human verification of nock@14.0.15 on npm before adding devDependency. Executor must not run pnpm add nock until user sends resume signal "approved".</action>
  <decision>Approve nock@14.0.15 as devDependency for HTTP mocking in ld-adapter tests</decision>
  <context>RESEARCH Package Legitimacy Audit flags nock as [ASSUMED] — standard Node HTTP mock from github.com/nock/nock. Required for CI tests without live LD credentials.</context>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/nock and confirm publisher/maintenance status
    2. Confirm version 14.0.15 exists and matches RESEARCH pin
    3. Type "approved" to proceed with pnpm add -D nock@14.0.15 in @ff-promo/ld-adapter
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install nock and add HTTP integration fixtures</name>
  <read_first>
    - .planning/phases/02-launchdarkly-adapter/02-RESEARCH.md — Validation Architecture, Phase Requirements → Test Map
    - packages/ld-adapter/src/__tests__/fixtures/flag-boolean.json
  </read_first>
  <files>packages/ld-adapter/package.json, packages/ld-adapter/src/__tests__/fixtures/patch-canary-success.json, packages/ld-adapter/src/__tests__/fixtures/patch-422-invalid.json, packages/ld-adapter/src/__tests__/fixtures/patch-429-retry.json, pnpm-lock.yaml</files>
  <action>
    After checkpoint approval, run pnpm add -D nock@14.0.15 --filter @ff-promo/ld-adapter.

    Add JSON fixtures for PATCH responses:
    - patch-canary-success.json: 200 response body after semantic patch
    - patch-422-invalid.json: LD error body for invalid instruction kind (simulates updatePercentageRollout rejection)
    - patch-429-retry.json: 429 response with Retry-After: 1 header for rate limit test

    Ensure fixtures align with flag-boolean.json variation _ids used in GET fixture.
  </action>
  <acceptance_criteria>
    - nock@14.0.15 in ld-adapter devDependencies
    - Three PATCH fixtures exist under src/__tests__/fixtures/
    - pnpm-lock.yaml updated
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @ff-promo/ld-adapter exec node -e "const p=require('./package.json'); if(!p.devDependencies?.nock) process.exit(1)" && test -f packages/ld-adapter/src/__tests__/fixtures/patch-422-invalid.json</automated>
  </verify>
  <done>nock installed; PATCH fixtures ready for HTTP tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: nock integration tests and public exports (PROV-01/02/03)</name>
  <read_first>
    - .planning/phases/02-launchdarkly-adapter/02-RESEARCH.md — Validation Architecture test map, Environment Availability
    - packages/ld-adapter/src/provider/launch-darkly-provider.ts
    - packages/ld-adapter/src/index.ts
    - README.md — existing dev workflow section
  </read_first>
  <files>packages/ld-adapter/src/__tests__/launch-darkly-provider.test.ts, packages/ld-adapter/src/index.ts, README.md</files>
  <behavior>
    - Test PROV-01: nock GET /api/v2/flags/{project}/{flag} returns fixture; provider.getFlagState yields variations, rules, on/off for target env
    - Test PROV-02: nock PATCH intercepted; request body includes semantic patch Content-Type, turnFlagOn + updateFallthroughVariationOrRollout, rolloutWeights summing to 100000
    - Test PROV-03: PATCH body uses variation _ids resolved from GET fixture (not hardcoded wrong ids)
    - Test: 422 response — assert exactly one PATCH attempt (no retry); error propagates
    - Test: 429 then 200 — assert retry occurred (nock sequence); final state success
    - Test: 405 response maps to ApprovalRequiredError
  </behavior>
  <action>
    Write launch-darkly-provider.test.ts using nock against base URL https://app.launchdarkly.com (match LD_BASE_URL default):

    - beforeEach: nock.cleanAll(); create provider with test token and baseUrl
    - afterEach: assert nock.isDone() for happy paths
    - Use createLaunchDarklyProvider factory with config.accessToken 'test-token'
    - Intercept GET and PATCH paths per launchdarkly-api client URL patterns

    Finalize packages/ld-adapter/src/index.ts exports:
    - createLaunchDarklyClient, createLaunchDarklyProvider, LaunchDarklyProvider
    - FlagProvider type, getFlagState, resolveVariationId, resolveRuleId
    - All error classes from errors module
    - Do NOT export internal rate limiter unless needed — prefer factory

    Update README.md with brief ld-adapter section: env vars (LD_ACCESS_TOKEN, LD_BASE_URL, LD_API_VERSION), run tests via pnpm exec vitest run --project ld-adapter, note CI uses nock not live LD.

    No Prisma or worker wiring in this phase.
  </action>
  <acceptance_criteria>
    - PROV-01/02/03 covered by launch-darkly-provider.test.ts with nock
    - 422 test proves no second PATCH (atomic failure, no corruption retry)
    - 429 retry test passes with Retry-After fixture
    - index.ts exports complete public API
    - README documents LD env vars and test command
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project ld-adapter</automated>
  </verify>
  <done>Full ld-adapter test suite green; public exports wired; README updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → nock | Simulated LD API responses in CI |
| npm registry → devDependency | nock install after human verify |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | Malicious nock package | mitigate | Human verify checkpoint before nock install per [ASSUMED] audit |
| T-02-10 | Repudiation | Untested PATCH contract | mitigate | nock asserts request headers/body shape in integration tests |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | nock human-approved; version pinned 14.0.15 |
</threat_model>

<verification>
- pnpm exec vitest run --project ld-adapter
- pnpm test (full monorepo suite)
- pnpm --filter @ff-promo/ld-adapter run build
</verification>

<success_criteria>
- All PROV-01/02/03 behaviors verified with HTTP mocks
- Phase 2 success criteria #1-4 satisfied by automated test suite
- Package ready for Phase 4 worker activity wiring
</success_criteria>

<output>
Create `.planning/phases/02-launchdarkly-adapter/02-04-SUMMARY.md` when done
</output>
