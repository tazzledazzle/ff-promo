---
phase: 02-launchdarkly-adapter
plan: 03
type: execute
wave: 2
depends_on:
  - "02-02"
files_modified:
  - packages/ld-adapter/src/client/rate-limited-client.ts
  - packages/ld-adapter/src/write/semantic-patch.ts
  - packages/ld-adapter/src/write/apply-targeting.ts
  - packages/ld-adapter/src/provider/launch-darkly-provider.ts
  - packages/ld-adapter/src/__tests__/semantic-patch-builder.test.ts
  - packages/ld-adapter/src/__tests__/rate-limited-client.test.ts
  - packages/ld-adapter/src/__tests__/apply-targeting.test.ts
  - packages/ld-adapter/src/index.ts
autonomous: true
requirements:
  - PROV-02
  - PROV-03
must_haves:
  truths:
    - "System applies targeting updates via semantic patch with correct Content-Type header"
    - "Canary rollouts use updateFallthroughVariationOrRollout with rolloutWeights summing to 100000"
    - "Every write path GETs fresh flag state and resolves variation IDs before PATCH (PROV-03)"
    - "429 responses retry with Retry-After backoff without tight loops; non-429 4xx fail fast"
    - "All LD HTTP calls (GET and PATCH) route through rateLimitedClient.schedule()"
    - "405 approval-required environments surface ApprovalRequiredError without partial flag mutation"
  artifacts:
    - path: "packages/ld-adapter/src/write/semantic-patch.ts"
      provides: "Atomic instruction batch builders"
      exports: ["buildTargetingPatchBody", "buildRolloutWeights"]
    - path: "packages/ld-adapter/src/client/rate-limited-client.ts"
      provides: "bottleneck + p-retry 429 handling"
      exports: ["createRateLimitedLdClient"]
    - path: "packages/ld-adapter/src/provider/launch-darkly-provider.ts"
      provides: "FlagProvider implementation"
      exports: ["LaunchDarklyProvider"]
  key_links:
    - from: "packages/ld-adapter/src/write/apply-targeting.ts"
      to: "getFlagState"
      via: "GET-before-write via rateLimitedClient.schedule()"
      pattern: "getFlagState"
    - from: "packages/ld-adapter/src/write/apply-targeting.ts"
      to: "resolveRuleId"
      via: "rule mode rollout"
      pattern: "resolveRuleId"
    - from: "packages/ld-adapter/src/provider/launch-darkly-provider.ts"
      to: "patchFeatureFlag"
      via: "semantic patch Content-Type via schedule()"
      pattern: "launchdarkly\\.semanticpatch"
---

<objective>
Implement PROV-02 semantic patch writes with read-before-write ID resolution, fallthrough rollout builders, and centralized rate limiting — satisfying ROADMAP success criteria #2, #3, and #4.

Purpose: Adapter must mutate LD targeting safely: atomic semantic patches, correct instruction kinds, env-scoped IDs from fresh GET, and 429 handling that never corrupts flag state via blind retries.
Output: RateLimitedLdClient, semantic patch builders, applyTargeting, LaunchDarklyProvider implementing FlagProvider.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-launchdarkly-adapter/02-RESEARCH.md
@packages/ld-adapter/src/read/get-flag-state.ts
@packages/ld-adapter/src/resolve/variation-resolver.ts
@packages/ld-adapter/src/resolve/rule-resolver.ts
@packages/ld-adapter/src/client/ld-api-client.ts
@packages/contracts/src/launchdarkly.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RateLimitedLdClient with bottleneck and p-retry (429 handling)</name>
  <read_first>
    - .planning/phases/02-launchdarkly-adapter/02-RESEARCH.md — Pattern 4 Centralized Rate Limiting, Pitfall 5 429 Retry Storm
    - packages/ld-adapter/src/errors/ld-adapter-error.ts — LdRateLimitError
  </read_first>
  <files>packages/ld-adapter/src/client/rate-limited-client.ts, packages/ld-adapter/src/__tests__/rate-limited-client.test.ts</files>
  <behavior>
    - Test: successful call passes through limiter without retry
    - Test: 429 on first attempt retries after honoring Retry-After header (use vi.useFakeTimers or mock delay fn injectable for test speed)
    - Test: 422 on PATCH does NOT retry (fail fast — atomic patch already rejected, no state corruption)
    - Test: 405 maps to ApprovalRequiredError per RESEARCH open Q3 (defer approval workflow to v2)
    - Test: after max retries exhausted on 429, throws LdRateLimitError with retryAfterMs when present
  </behavior>
  <action>
    Implement createRateLimitedLdClient(rawClient, opts?) wrapping all LD HTTP calls:

    - Single Bottleneck instance per client: configure minTime and maxConcurrent conservatively (e.g. maxConcurrent 2) per RESEARCH
    - Export schedule(fn) that runs fn inside bottleneck queue wrapped with p-retry (retries: 4)
    - Retry ONLY on 429 and transient 5xx; immediately rethrow other 4xx including 422
    - Parse Retry-After (seconds) and X-Ratelimit-Reset (epoch ms) from error response headers; sleep before retry with 0-500ms jitter
    - Map 405 to ApprovalRequiredError with environmentKey context when available
    - Map repeated 429 exhaustion to LdRateLimitError
    - Never retry with modified patch body — same atomic request retries only

    Unit test with mocked fn throwing structured errors mimicking launchdarkly-api client shape (status + headers object).
  </action>
  <acceptance_criteria>
    - ROADMAP criterion #4: 429 handled with backoff, not tight loop
    - 422 and other non-429 4xx fail without retry
    - 405 surfaces ApprovalRequiredError (v2 approval flow deferred)
    - Bottleneck serializes concurrent calls through shared limiter
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project ld-adapter src/__tests__/rate-limited-client.test.ts</automated>
  </verify>
  <done>Rate limit wrapper honors Retry-After; fail-fast on non-retryable errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Semantic patch builders and applyTargeting with GET-before-write (PROV-02, PROV-03)</name>
  <read_first>
    - .planning/phases/02-launchdarkly-adapter/02-RESEARCH.md — Pattern 2 Atomic Semantic Patch Batching, Apply Canary Rollout example, Pitfalls 1-4
    - packages/ld-adapter/src/read/get-flag-state.ts
    - packages/ld-adapter/src/resolve/variation-resolver.ts
    - packages/contracts/src/launchdarkly.ts — TargetingIntentSchema, RolloutIntentSchema
  </read_first>
  <files>packages/ld-adapter/src/write/semantic-patch.ts, packages/ld-adapter/src/write/apply-targeting.ts, packages/ld-adapter/src/provider/launch-darkly-provider.ts, packages/ld-adapter/src/__tests__/semantic-patch-builder.test.ts, packages/ld-adapter/src/__tests__/apply-targeting.test.ts, packages/ld-adapter/src/index.ts</files>
  <behavior>
    - Test: buildRolloutWeights(10000, treatmentId, controlId) returns weights summing to 100000
    - Test: buildTargetingPatchBody for fallthrough mode emits turnFlagOn + updateFallthroughVariationOrRollout with rolloutBucketBy and rolloutContextKind
    - Test: instruction kinds never include updatePercentageRollout
    - Test: applyTargeting calls getFeatureFlag before patchFeatureFlag (GET-before-write order asserted via mock call order)
    - Test: getFlagState GET and patchFeatureFlag PATCH both route through rate-limited client schedule() wrapper (not raw flagsApi)
    - Test: applyTargeting passes Content-Type application/json; domain-model=launchdarkly.semanticpatch on PATCH
    - Test: unresolved variation ref throws UnresolvedVariationError before patchFeatureFlag invoked
    - Test: when rollout.mode is rule, applyTargeting calls resolveRuleId and emits updateRuleVariationOrRollout with resolved ruleId
  </behavior>
  <action>
    Implement semantic-patch.ts:

    - buildRolloutWeights(treatmentThousandths, treatmentVariationId, controlVariationId) with integer math; assert sum === 100_000 or throw
    - buildTargetingPatchBody(intent: TargetingIntent, resolvedIds) producing { environmentKey, comment, instructions[] }
    - v1 default: fallthrough rollout via updateFallthroughVariationOrRollout (RESEARCH open Q1 locked); optional rule path uses updateRuleVariationOrRollout when intent.rollout.mode is rule and ruleId resolved via RuleResolver
    - Require rolloutBucketBy and rolloutContextKind on rollout intents (RESEARCH Pitfall 4)
    - Batch turnFlagOn with rollout in single instructions array (RESEARCH anti-pattern: separate PATCH)

    Implement apply-targeting.ts:

    - Parse ApplyTargetingInputSchema
    - Accept rate-limited client wrapper (createRateLimitedLdClient result) — all LD HTTP must go through schedule(), not raw flagsApi
    - GET fresh flag via getFlagState using rateLimitedClient.schedule(() => flagsApi.getFeatureFlag(...)) — same limiter/retry path as PATCH (RESEARCH Pattern 4: every LD HTTP call)
    - Resolve treatment/control variation IDs via resolveVariationId (PROV-03)
    - When intent.rollout.mode is rule: resolve ruleId via resolveRuleId(flagState, environmentKey, intent.rollout.ruleRef) before building patch; throw UnresolvedRuleError if unresolved (PROV-03)
    - Build patch body; validate instruction shapes against SemanticPatchInstructionSchema array
    - PATCH via rateLimitedClient.schedule(() => flagsApi.patchFeatureFlag(...)) with SEMANTIC_PATCH_CONTENT_TYPE header

    Implement LaunchDarklyProvider in launch-darkly-provider.ts implementing FlagProvider:

    - Constructor injects createLaunchDarklyClient result + createRateLimitedLdClient wrapper
    - getFlagState delegates to read module with rate-limited client passed through (GET via schedule())
    - applyTargeting delegates to apply-targeting module with same rate-limited client
    - Export createLaunchDarklyProvider(config) factory from index.ts alongside createLaunchDarklyClient

    Mock flagsApi in apply-targeting.test.ts; assert single PATCH with batched instructions on happy path.
  </action>
  <acceptance_criteria>
    - PROV-02: semantic patch writes use official instruction kinds only
    - PROV-03: variation IDs resolved from fresh GET before every PATCH
    - PROV-03: rule IDs resolved via resolveRuleId when rollout.mode is rule
    - Rollout weights use thousandths scale summing to 100000
    - PATCH uses semantic patch Content-Type header
    - GET-before-write enforced in applyTargeting call sequence
    - Both GET and PATCH route through rateLimitedClient.schedule()
    - LaunchDarklyProvider exported as FlagProvider implementation
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project ld-adapter src/__tests__/semantic-patch-builder.test.ts src/__tests__/apply-targeting.test.ts</automated>
  </verify>
  <done>PROV-02 write path complete with atomic batched patches and read-before-write resolution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ld-adapter → LD REST PATCH | Targeting mutations cross to external provider |
| promotion intent → semantic patch | Internal domain intent becomes LD instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | Partial flag state on failed write | mitigate | Atomic semantic patch batch; LD all-or-nothing; no retry on 422 |
| T-02-06 | Denial of Service | 429 rate limit storms | mitigate | bottleneck queue + Retry-After + p-retry cap |
| T-02-07 | Tampering | Wrong variation ID in rollout | mitigate | GET-before-write + resolveVariationId fail-closed |
| T-02-08 | Elevation | Approval bypass on protected env | mitigate | Map 405 to ApprovalRequiredError; no silent skip |
</threat_model>

<verification>
- pnpm exec vitest run --project ld-adapter
- pnpm --filter @ff-promo/ld-adapter run build
</verification>

<success_criteria>
- PROV-02 semantic patch writes implemented with correct headers and instruction kinds
- PROV-03 enforced on every write via GET + resolvers
- Rate limit handling satisfies ROADMAP criterion #4
</success_criteria>

<output>
Create `.planning/phases/02-launchdarkly-adapter/02-03-SUMMARY.md` when done
</output>
