---
phase: 07-guardrails-self-service
plan: 04
type: execute
wave: 3
depends_on:
  - "07-03"
files_modified:
  - apps/web/src/lib/api-client.ts
  - apps/web/src/lib/api-errors.ts
  - apps/web/src/hooks/use-pipelines.ts
  - apps/web/src/hooks/use-pipeline-mutations.ts
  - apps/web/src/hooks/use-pipeline-detail.ts
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/pipelines/page.tsx
  - apps/web/src/app/pipelines/new/page.tsx
  - apps/web/src/app/pipelines/[id]/page.tsx
  - apps/web/src/components/pipelines/pipelines-page-header.tsx
  - apps/web/src/components/pipelines/pipelines-table.tsx
  - apps/web/src/components/pipelines/pipeline-form.tsx
  - apps/web/src/components/pipelines/pipeline-detail.tsx
  - apps/web/src/__tests__/integration/pipeline-form.test.tsx
  - apps/web/src/__tests__/mocks/handlers.ts
  - README.md
autonomous: true
requirements:
  - UI-04
user_setup: []
must_haves:
  truths:
    - "Platform engineer can view /pipelines list with stage count and active/inactive badge (D-13, UI-04)"
    - "Platform engineer can create pipeline at /pipelines/new with fixed dev/staging/prod rows and gate policy editors (D-14, TELE-01, TELE-02)"
    - "Platform engineer can view /pipelines/[id] read-only detail with thresholds and deactivate pipeline (D-15)"
    - "api-client exposes createPipeline, updatePipeline, getPipeline with typed contracts (D-16)"
    - "MSW tests verify form validation and successful submit navigates to detail (D-20)"
  artifacts:
    - path: "apps/web/src/app/pipelines/page.tsx"
      provides: "Pipeline list page (UI-04, D-13)"
    - path: "apps/web/src/app/pipelines/new/page.tsx"
      provides: "Pipeline create form (D-14)"
    - path: "apps/web/src/app/pipelines/[id]/page.tsx"
      provides: "Pipeline detail + deactivate (D-15)"
    - path: "apps/web/src/components/pipelines/pipeline-form.tsx"
      provides: "3-stage gate policy editors"
    - path: "apps/web/src/lib/api-client.ts"
      provides: "createPipeline, updatePipeline, getPipeline"
      pattern: "createPipeline"
  key_links:
    - from: "apps/web/src/app/pipelines/new/page.tsx"
      to: "POST /v1/pipelines"
      via: "api.createPipeline with dashboardActor platform"
      pattern: "createPipeline"
    - from: "apps/web/src/components/pipelines/pipeline-detail.tsx"
      to: "PATCH /v1/pipelines/:id"
      via: "deactivate mutation isActive false"
      pattern: "updatePipeline|deactivatePipeline"
    - from: "apps/web/src/app/pipelines/page.tsx"
      to: "GET /v1/pipelines"
      via: "usePipelines hook"
      pattern: "listPipelines"
---

<objective>
Deliver dashboard pipeline configuration UI: list, create form with gate policy editors, read-only detail with deactivate, api-client mutations, MSW integration tests, and README Phase 7 section.

Purpose: UI-04 and D-13–D-16 complete the v1 configuration layer for platform engineers.
Output: Passing pipeline-form MSW test; navigable /pipelines routes; README documents Phase 7 capabilities.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-guardrails-self-service/07-CONTEXT.md
@.planning/phases/07-guardrails-self-service/07-PATTERNS.md
@apps/web/src/app/runs/page.tsx
@apps/web/src/app/runs/new/page.tsx
@apps/web/src/components/runs/runs-table.tsx
@apps/web/src/hooks/use-run-mutations.ts
@apps/web/src/lib/api-client.ts
@apps/web/src/__tests__/integration/create-run.test.tsx
@apps/web/src/__tests__/mocks/handlers.ts
@packages/db/src/seed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: api-client, hooks, and nav for pipeline CRUD (D-16)</name>
  <files>apps/web/src/lib/api-client.ts, apps/web/src/lib/api-errors.ts, apps/web/src/hooks/use-pipelines.ts, apps/web/src/hooks/use-pipeline-mutations.ts, apps/web/src/hooks/use-pipeline-detail.ts, apps/web/src/app/layout.tsx</files>
  <action>
    Extend api-client.ts per PATTERNS Pattern 4:
    createPipeline(body: PipelineCreateRequest) POST /v1/pipelines return PipelineResponse.
    updatePipeline(id, body: PipelineUpdateRequest) PATCH /v1/pipelines/:id.
    deactivatePipeline(id, actor) convenience wrapper PATCH { isActive: false, actor }.
    getPipeline(id) GET /v1/pipelines/:id return PipelineDetailResponse — add if not present.
    Extend listPipelines return type to include isActive, version, projectKey from updated PipelineListItemSchema.
    Add isGuardrailError(error) to api-errors.ts for 403/422 ApiClientError (PATTERNS).
    Create use-pipeline-mutations.ts mirroring use-run-mutations: create and deactivate mutations with query invalidation on queryKeys.pipelines.list and detail(id); use dashboardActor('platform') per D-18.
    Create use-pipeline-detail.ts: useQuery queryKeys.pipelines.detail(id) fetching getPipeline.
    Ensure usePipelines({ activeOnly }) from 07-03 defaults activeOnly false for platform list.
    Add nav link to /pipelines in layout.tsx header or shared nav component alongside /runs (Claude discretion: minimal header links).
  </action>
  <verify>
    <automated>pnpm exec vitest run --project web api-client && pnpm --filter @ff-promo/web run build</automated>
  </verify>
  <done>api-client exports pipeline write methods; hooks invalidate queries; layout links to /pipelines.</done>
</task>

<task type="auto">
  <name>Task 2: Pipeline list, create form, and detail pages (D-13, D-14, D-15, UI-04)</name>
  <files>apps/web/src/app/pipelines/page.tsx, apps/web/src/app/pipelines/new/page.tsx, apps/web/src/app/pipelines/[id]/page.tsx, apps/web/src/components/pipelines/pipelines-page-header.tsx, apps/web/src/components/pipelines/pipelines-table.tsx, apps/web/src/components/pipelines/pipeline-form.tsx, apps/web/src/components/pipelines/pipeline-detail.tsx</files>
  <action>
    /pipelines page (D-13): mirror runs/page.tsx — PipelinesPageHeader with link to /pipelines/new; PipelinesTable showing name, flagKey, stageCount, Active/Inactive Badge, View link to /pipelines/[id]; usePipelines() without activeOnly filter.
    pipeline-form.tsx (D-14, discretion: single-page fixed 3-row template per RESEARCH A4):
    Fields: name, flagKey, projectKey; three fixed stages dev/staging/prod with orderIndex 0/1/2 (non-reorderable environments); per stage: displayName, serviceName, error_rate threshold, latency_p95 threshold.
    Default gate policy values from seed.ts (error_rate 0.01, latency_p95 500, serviceName demo-service).
    Client-side validation: all fields required, thresholds positive numbers — surface errors in role=alert region; server remains authoritative.
    /pipelines/new/page.tsx: thin wrapper using PipelineForm; on submit call api.createPipeline with stages built from form state and actor dashboardActor('platform'); router.push `/pipelines/${pipeline.id}` on success; display isGuardrailError messages.
    /pipelines/[id]/page.tsx (D-15): usePipelineDetail; PipelineDetail read-only table of stages with error_rate and latency_p95 thresholds; show isActive badge, version, flagKey, projectKey; Deactivate button (confirm via alert-dialog) calling deactivate mutation when isActive; after deactivate refresh detail and show Inactive badge. v1: no in-place stage edit — note in UI helper text that structural changes require new pipeline via /pipelines/new (D-04).
    Use shadcn Table, Badge, Button, Input, Label, AlertDialog — match runs-table styling (PATTERNS).
  </action>
  <verify>
    <automated>pnpm --filter @ff-promo/web run build</automated>
  </verify>
  <done>Three pipeline routes render; form submits 3-stage payload; detail shows gate policies and deactivate works against API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MSW integration tests and README Phase 7 (D-20)</name>
  <files>apps/web/src/__tests__/integration/pipeline-form.test.tsx, apps/web/src/__tests__/mocks/handlers.ts, README.md</files>
  <behavior>
    - Form shows validation error when name empty on submit
    - Successful submit calls POST /v1/pipelines and navigates to /pipelines/[id]
    - MSW handler rejects payload missing gate policies with 422
  </behavior>
  <action>
    Extend handlers.ts: POST /api/ff-promo/v1/pipelines validates body.stages each have >=2 gatePolicies; return 422 if not; return 201 mock PipelineResponse with id pipeline-new.
    Add PATCH handler for deactivate returning isActive false.
    Extend GET pipeline detail mock to include gatePolicies per stage for detail page tests if needed.
    Create pipeline-form.test.tsx mirroring create-run.test.tsx: render NewPipelinePage with MSW; test validation prevents submit when name empty; fill name, flagKey, projectKey, stage thresholds; click Create pipeline; assert router.push /pipelines/pipeline-new.
    Update README.md Phase 7 section: document POST/PATCH /v1/pipelines, GuardrailService enforcement on promotion runs, dashboard /pipelines routes, requirement IDs satisfied (PIPE-01, TELE-01/02, GRD-01–03, API-03, UI-04); add curl example for pipeline create; note v2 deferred GRD-04/API-04.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project web -t pipeline</automated>
  </verify>
  <done>MSW pipeline form tests pass; README documents Phase 7 configuration and guardrails.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → BFF → API | Pipeline config submitted from dashboard |
| Form validation | UX only; GRD-03 enforced API-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Information Disclosure | api-client | mitigate | No API_KEY in client; BFF proxy unchanged from Phase 6 |
| T-07-13 | Tampering | pipeline-form | accept | Client validation UX; API Zod + GuardrailService authoritative |
| T-07-14 | Repudiation | Deactivate action | mitigate | PATCH sends actor dashboardActor platform; server audits (D-12) |
| T-07-15 | Elevation | Config UI visible to all | accept | RBAC deferred GRD-04; all authenticated callers can configure v1 (D-17) |
</threat_model>

<verification>
- pnpm --filter @ff-promo/web run build
- pnpm exec vitest run --project web -t pipeline
</verification>

<success_criteria>
- Dashboard pipeline list, create, and detail pages ship (UI-04, D-13–D-15)
- Typed api-client pipeline CRUD methods (D-16)
- MSW form tests pass (D-20)
- README updated with Phase 7 documentation
</success_criteria>

<output>
Create `.planning/phases/07-guardrails-self-service/07-04-SUMMARY.md` when done
</output>
