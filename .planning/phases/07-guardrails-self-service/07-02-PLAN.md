---
phase: 07-guardrails-self-service
plan: 02
type: execute
wave: 1
depends_on:
  - "07-01"
files_modified:
  - packages/db/prisma/schema.prisma
  - packages/db/prisma/migrations/
  - packages/db/src/repositories/pipeline-audit.repository.ts
  - packages/db/src/index.ts
  - apps/api/src/services/pipeline.service.ts
  - apps/api/src/routes/pipelines.ts
  - apps/api/src/app.ts
  - apps/api/src/__tests__/pipelines.create.test.ts
  - apps/api/src/__tests__/pipelines.list.test.ts
autonomous: true
requirements:
  - PIPE-01
  - TELE-01
  - TELE-02
  - GRD-01
  - API-03
user_setup: []
must_haves:
  truths:
    - "Platform engineer can POST /v1/pipelines to create a 3-environment pipeline with error_rate and latency_p95 gate policies per stage (PIPE-01, TELE-01, TELE-02, D-05)"
    - "Platform engineer can PATCH /v1/pipelines/:id to deactivate or update description (D-06, D-04)"
    - "GET /v1/pipelines lists all pipelines with isActive badge fields; GET /v1/pipelines/:id returns gate policies per stage (D-07, D-13)"
    - "Pipeline create and deactivate append PipelineConfigAudit records with actor (D-12)"
  artifacts:
    - path: "apps/api/src/routes/pipelines.ts"
      provides: "POST and PATCH /v1/pipelines; extended GET list/detail (API-03)"
      contains: "app.post"
    - path: "apps/api/src/services/pipeline.service.ts"
      provides: "createPipeline, updatePipeline with guardrail pre-check and audit"
    - path: "packages/db/prisma/schema.prisma"
      provides: "PipelineConfigAudit model"
      contains: "PipelineConfigAudit"
    - path: "packages/db/src/repositories/pipeline-audit.repository.ts"
      provides: "append pipeline config audit events"
  key_links:
    - from: "apps/api/src/services/pipeline.service.ts"
      to: "apps/api/src/services/guardrail.service.ts"
      via: "validatePipelineConfig before repos.pipeline.create"
      pattern: "validatePipelineConfig"
    - from: "apps/api/src/routes/pipelines.ts"
      to: "apps/api/src/services/pipeline.service.ts"
      via: "POST / PATCH handlers return PipelineResponseSchema"
      pattern: "pipelineService"
    - from: "apps/api/src/services/pipeline.service.ts"
      to: "packages/db/src/repositories/pipeline-audit.repository.ts"
      via: "append on create and deactivate"
      pattern: "pipeline_created|pipeline_deactivated"
---

<objective>
Deliver pipeline configuration REST API: POST create with nested stages and gate policies, PATCH deactivate/metadata, extended GET list/detail, PipelineConfigAudit persistence, and integration tests.

Purpose: API-03, PIPE-01, TELE-01, TELE-02 require write endpoints; D-12 requires config audit separate from promotion AuditEvent.
Output: Passing pipelines.create and updated pipelines.list API tests; migration applied for PipelineConfigAudit.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-guardrails-self-service/07-CONTEXT.md
@.planning/phases/07-guardrails-self-service/07-RESEARCH.md
@.planning/phases/07-guardrails-self-service/07-PATTERNS.md
@apps/api/src/routes/pipelines.ts
@apps/api/src/routes/promotion-runs.ts
@apps/api/src/app.ts
@packages/db/prisma/schema.prisma
@packages/db/src/seed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: PipelineConfigAudit schema, migration, and repository (D-12)</name>
  <files>packages/db/prisma/schema.prisma, packages/db/prisma/migrations/, packages/db/src/repositories/pipeline-audit.repository.ts, packages/db/src/index.ts</files>
  <action>
    Add PipelineConfigAudit model to schema.prisma per RESEARCH config audit design — do NOT extend AuditEvent (requires promotionRunId):
    Fields: id cuid, pipelineId FK to Pipeline, action String (pipeline_created | pipeline_deactivated | pipeline_updated), actorType ActorType, actorId String, displayName String optional, metadata Json default {}, occurredAt DateTime default now; index on [pipelineId, occurredAt].
    Add Pipeline.configAudits relation on Pipeline model.
    Generate and apply Prisma migration.
    Create PipelineAuditRepository with append({ pipelineId, action, actorType, actorId, displayName?, metadata? }) using existing ActorType enum.
    Export repository from packages/db/src/index.ts alongside PipelineRepository.
  </action>
  <verify>
    <automated>pnpm --filter @ff-promo/db exec prisma migrate deploy && pnpm --filter @ff-promo/db run build</automated>
  </verify>
  <done>Migration applies cleanly; PipelineAuditRepository.append persists rows linked to pipelineId.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API integration tests for pipeline CRUD (D-19 partial, PIPE-01, TELE-01, TELE-02)</name>
  <files>apps/api/src/__tests__/pipelines.create.test.ts, apps/api/src/__tests__/pipelines.list.test.ts</files>
  <behavior>
    - creates pipeline with dev/staging/prod stages each having error_rate and latency_p95 policies
    - POST returns 422 when stage missing latency_p95
    - PATCH deactivate sets isActive false and pipeline appears in list with isActive false
    - GET detail returns gate policy thresholds per stage
  </behavior>
  <action>
    Create apps/api/src/__tests__/pipelines.create.test.ts using testcontainers harness from pipelines.list.test.ts (startTestDatabase, buildApp with pipelineService).
    Seed helper createValidPipelinePayload(): 3 stages orderIndex 0-2, environments dev/staging/prod, each with error_rate threshold 0.01 and latency_p95 threshold 500, serviceName demo-service, actor actorType user actorId platform.
    Test POST 201 returns nested stages with gatePolicies; verify PipelineConfigAudit row exists for pipeline_created.
    Test POST 422 when only error_rate provided (missing latency_p95).
    Test PATCH deactivate returns isActive false; audit pipeline_deactivated appended.
    Update pipelines.list.test.ts: expect isActive and version on list items; list includes deactivated pipeline after PATCH; extend detail test to assert gatePolicies array length 2 per stage.
  </action>
  <verify>
    <automated>test -f apps/api/src/__tests__/pipelines.create.test.ts</automated>
  </verify>
  <done>Test files exist with failing scenarios defined; tests fail until Task 3 implementation lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PipelineService and POST/PATCH/GET route extensions (D-05, D-06, D-07, API-03)</name>
  <files>apps/api/src/services/pipeline.service.ts, apps/api/src/routes/pipelines.ts, apps/api/src/app.ts</files>
  <behavior>
    - POST /v1/pipelines with valid 3-stage payload returns 201 PipelineResponse
    - POST rejects pipeline missing latency_p95 with 422
    - PATCH /v1/pipelines/:id with isActive false deactivates pipeline
    - GET /v1/pipelines returns inactive pipelines with isActive field
    - GET /v1/pipelines/:id includes gatePolicies on each stage
  </behavior>
  <action>
    Create apps/api/src/services/pipeline.service.ts mirroring promotion-run.service request-scoped DB pattern (createRequestDb try/finally):
    createPipeline(body: PipelineCreateRequest): call validatePipelineConfig from guardrail.service — if violations, throw forbidden or unprocessableEntity based on violation.httpStatus; call repos.pipeline.create with description from body; append pipeline_created audit with actor from body (D-12, D-18 actorType user); return mapPipelineResponse.
    updatePipeline(id, body: PipelineUpdateRequest): findById or notFound; if isActive === false call repos.pipeline.deactivate and append pipeline_deactivated; if description provided update pipeline description in-place (metadata-only edit allowed per D-04 discretion); reject any attempt to mutate stages/flagKey at service layer; return mapPipelineResponse.
    mapPipelineResponse: map DB entity to PipelineResponseSchema including stage gate policy ids and timestamps as ISO strings.
    Refactor apps/api/src/routes/pipelines.ts:
    Inject pipelineService via factory pattern — extend buildApp to accept optional pipelineService or construct default from env DATABASE_URL (mirror promotionRunRoutes(service) pattern).
    POST / with body PipelineCreateRequestSchema, response 201 PipelineResponseSchema — delegate to pipelineService.createPipeline.
    PATCH /:id with params id, body PipelineUpdateRequestSchema, response 200 PipelineResponseSchema.
    GET /: switch listActive to listAll (D-13 platform view); map isActive, version, projectKey, stageCount per PipelineListItemSchema.
    GET /:id: use PipelineDetailResponseSchema from contracts; include isActive, version, description, gatePolicies per stage — replace inline z.object response schema.
    Handle Prisma P2002 unique name+version as conflict() 409 when active duplicate name without version bump (Pitfall 4).
    Wire pipelineService in app.ts: createPipelineService({ databaseUrl }) and pass to pipelineRoutes(pipelineService).
  </action>
  <verify>
    <automated>pnpm exec vitest run --project api pipelines.create pipelines.list</automated>
  </verify>
  <done>POST/PATCH routes work; GET list/detail extended; Task 2 integration tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST/PATCH /v1/pipelines | Untrusted pipeline config input |
| API → PostgreSQL | Nested create for stages/gatePolicies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | PATCH /v1/pipelines/:id | mitigate | Allowlist isActive + description only in PipelineUpdateRequestSchema (D-06) |
| T-07-05 | Tampering | POST gate thresholds | mitigate | Zod positive numbers + MetricTypeSchema; validatePipelineConfig before write |
| T-07-06 | Repudiation | Config changes | mitigate | PipelineConfigAudit append on create/deactivate with actor (D-12) |
| T-07-07 | Elevation | Pipeline CRUD | accept | API_KEY auth same as Phase 5; RBAC deferred GRD-04 (D-17) |
| T-07-SC | Tampering | npm/prisma migrate | accept | No new npm packages; Prisma migration only |
</threat_model>

<verification>
- pnpm --filter @ff-promo/db exec prisma migrate deploy
- pnpm exec vitest run --project api pipelines.create pipelines.list
</verification>

<success_criteria>
- POST /v1/pipelines creates multi-env pipeline with SLO thresholds (PIPE-01, TELE-01, TELE-02, D-05)
- PATCH deactivates pipeline immutably (D-04, D-06)
- Config audit records pipeline create/deactivate (D-12)
- GET list/detail support platform UI (D-07, API-03)
</success_criteria>

<output>
Create `.planning/phases/07-guardrails-self-service/07-02-SUMMARY.md` when done
</output>
