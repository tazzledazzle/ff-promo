---
phase: 07-guardrails-self-service
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/contracts/src/pipeline.ts
  - packages/contracts/src/api.ts
  - packages/contracts/src/index.ts
  - packages/db/src/repositories/pipeline.repository.ts
  - packages/db/src/__tests__/pipeline.integration.test.ts
  - apps/api/src/errors/api-error.ts
  - apps/api/src/services/guardrail.service.ts
  - apps/api/src/__tests__/guardrail.service.test.ts
autonomous: true
requirements:
  - GRD-01
  - GRD-03
user_setup: []
must_haves:
  truths:
    - "Shared contracts define pipeline create/update/response shapes and guardrail violation codes (D-03)"
    - "GuardrailService rejects inactive pipelines, flagKey mismatches, empty stages, duplicate environments, bad stage order, and missing error_rate/latency_p95 policies (D-10, D-11)"
    - "API error helpers return consistent 403 forbidden and 422 unprocessable_entity bodies"
    - "PipelineRepository supports deactivate, listAll, and resolveNextVersion for immutable versioning (D-04)"
  artifacts:
    - path: "packages/contracts/src/pipeline.ts"
      provides: "MetricTypeSchema, PipelineCreateRequestSchema, PipelineUpdateRequestSchema, PipelineResponseSchema, GuardrailViolationSchema (D-03)"
      contains: "MetricTypeSchema"
    - path: "apps/api/src/services/guardrail.service.ts"
      provides: "validatePipelineConfig and validatePromotionRequest (D-08, GRD-03 logic)"
      exports: ["validatePipelineConfig", "validatePromotionRequest"]
    - path: "apps/api/src/errors/api-error.ts"
      provides: "forbidden() and unprocessableEntity() helpers"
      contains: "forbidden"
    - path: "packages/db/src/repositories/pipeline.repository.ts"
      provides: "deactivate, listAll, resolveNextVersion"
      contains: "deactivate"
  key_links:
    - from: "apps/api/src/services/guardrail.service.ts"
      to: "packages/contracts/src/pipeline.ts"
      via: "MetricTypeSchema and GuardrailViolationSchema types"
      pattern: "error_rate.*latency_p95"
    - from: "packages/db/src/repositories/pipeline.repository.ts"
      to: "PipelineCreateInputSchema"
      via: "Zod parse at repository boundary"
      pattern: "PipelineCreateInputSchema.parse"
---

<objective>
Extend shared pipeline contracts, implement GuardrailService validation logic with unit tests, add API error helpers, and extend PipelineRepository for deactivate/listAll/version resolution — foundation for CRUD API and promotion enforcement.

Purpose: D-03, D-08, D-10, D-11 require typed contracts and centralized guardrail rules before POST/PATCH routes or createRun hooks can ship.
Output: Passing guardrail unit tests, repository deactivate/listAll tests, contracts build with new schemas exported.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-guardrails-self-service/07-CONTEXT.md
@.planning/phases/07-guardrails-self-service/07-RESEARCH.md
@.planning/phases/07-guardrails-self-service/07-PATTERNS.md
@packages/contracts/src/pipeline.ts
@packages/contracts/src/api.ts
@packages/db/src/repositories/pipeline.repository.ts
@packages/db/src/__tests__/pipeline.integration.test.ts
@apps/api/src/errors/api-error.ts
@packages/telemetry/src/query/build-promql.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend pipeline contracts and API response schemas (D-03)</name>
  <files>packages/contracts/src/pipeline.ts, packages/contracts/src/api.ts, packages/contracts/src/index.ts</files>
  <behavior>
    - MetricTypeSchema accepts only error_rate and latency_p95
    - GatePolicyInputSchema uses MetricTypeSchema and z.number().positive() for threshold
    - PipelineCreateInputSchema refinement rejects stages missing either required metric
    - PipelineCreateRequestSchema extends create input with optional description and required actor (D-12 prep)
    - PipelineUpdateRequestSchema allows only isActive, description, actor — no stage mutation (D-04, D-06 discretion: PATCH fields)
    - GuardrailViolationSchema includes code, message, httpStatus
    - PipelineResponseSchema includes nested stages with gatePolicies ids
  </behavior>
  <action>
    In packages/contracts/src/pipeline.ts per D-03 and RESEARCH Pattern 1:
    Add MetricTypeSchema z.enum(['error_rate', 'latency_p95']) aligned with packages/telemetry build-promql metric types.
    Tighten GatePolicyInputSchema.metricType to MetricTypeSchema; threshold z.number().positive(); keep serviceName, windowSeconds, minSampleSize optional with sensible defaults documented in JSDoc only if needed.
    Add GuardrailPolicySchema per D-02 as optional Zod object (allowedEnvironments, requirePreflightPass, maxConcurrentRunsPerFlag) — export type but do NOT add DB column in this plan; v1 enforcement derives from pipeline fields only.
    Add PipelineCreateRequestSchema = PipelineCreateInputSchema.extend({ description: z.string().optional(), actor: ActorSchema }) importing ActorSchema from promotion-run.ts.
    Add PipelineUpdateRequestSchema with isActive optional boolean, description optional string, actor required — no name/flagKey/stages fields (immutable config per D-04).
    Add PipelineResponseSchema and PipelineStageResponseSchema with gate policy ids; export inferred types.
    Add GuardrailViolationSchema { code: z.string(), message: z.string(), httpStatus: z.union([z.literal(403), z.literal(422)]) }.
    Add .superRefine on PipelineCreateInputSchema: each stage must include both error_rate and latency_p95 gate policies; stages.length >= 1; no duplicate environment values; orderIndex 0..n-1 with monotonic dev→staging→prod by orderIndex (D-10, D-11).
    In packages/contracts/src/api.ts extend PipelineListItemSchema with isActive, version, projectKey (D-07, D-13).
    Add PipelineStageDetailSchema with gatePolicies array (GatePolicyInputSchema + id); extend PipelineDetailResponseSchema with isActive, version, description nullable optional, stages using PipelineStageDetailSchema.
    Re-export all new schemas/types from index.ts.
  </action>
  <verify>
    <automated>pnpm --filter @ff-promo/contracts run build</automated>
  </verify>
  <done>Contracts compile; MetricTypeSchema and guardrail refinements exported; list/detail schemas include isActive and gatePolicies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GuardrailService and API error helpers (D-08, D-10, D-11, GRD-01, GRD-03)</name>
  <files>apps/api/src/errors/api-error.ts, apps/api/src/services/guardrail.service.ts, apps/api/src/__tests__/guardrail.service.test.ts</files>
  <behavior>
    - validatePipelineConfig returns violations for empty stages, duplicate environment, invalid stage order, missing error_rate, missing latency_p95
    - validatePromotionRequest returns pipeline_not_found when pipeline null
    - validatePromotionRequest returns pipeline_inactive (403) when isActive false
    - validatePromotionRequest returns flag_key_mismatch (403) when flagKey differs
    - validatePromotionRequest returns structural violations (422) for empty stages or missing gate policies
    - forbidden() creates ApiError 403 with code forbidden; unprocessableEntity() creates 422 with code unprocessable_entity
  </behavior>
  <action>
    Add to apps/api/src/errors/api-error.ts alongside conflict/notFound: forbidden(message) returns ApiError(403, message, 'forbidden'); unprocessableEntity(message) returns ApiError(422, message, 'unprocessable_entity') per RESEARCH guardrail HTTP mapping.
    Create apps/api/src/services/guardrail.service.ts as pure validation module (no DB calls in validatePromotionRequest — caller passes pipeline | null per RESEARCH Pattern 2):
    Export REQUIRED_METRICS and ENV_ORDER constants; validateStageGatePolicies, validateStageOrder, validatePipelineConfig(input: PipelineCreateInput): GuardrailViolation[].
    Export validatePromotionRequest({ pipeline, flagKey }): GuardrailViolation[] covering D-10 rules: pipeline_not_found (404 hint in httpStatus or separate code — map to notFound at call site), pipeline_inactive 403, flag_key_mismatch 403, pipeline_empty 422, duplicate_environment 422, invalid_stage_order 422, missing_error_rate 422, missing_latency_p95 422.
    Use GuardrailViolation type from contracts; first violation wins at HTTP layer (callers throw on violations[0]).
    Do NOT wire into promotion-run.service yet — that is 07-03.
    Write apps/api/src/__tests__/guardrail.service.test.ts covering every rule in D-10/D-11 with table-driven cases; use inline pipeline fixtures (3-stage dev/staging/prod with both metrics) copied from seed pattern.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project api guardrail.service</automated>
  </verify>
  <done>All guardrail unit tests pass; forbidden/unprocessableEntity helpers exist; validatePipelineConfig and validatePromotionRequest exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PipelineRepository deactivate, listAll, resolveNextVersion (D-04, D-21)</name>
  <files>packages/db/src/repositories/pipeline.repository.ts, packages/db/src/__tests__/pipeline.integration.test.ts</files>
  <behavior>
    - deactivate(id) sets isActive false and returns pipeline with stages + gatePolicies
    - listAll() returns all pipelines ordered by name asc with stage id count
    - resolveNextVersion(name) returns max version + 1 for name or 1 if none
    - create with explicit version uses resolveNextVersion when version omitted (optional enhancement: accept version in create data)
  </behavior>
  <action>
    Extend PipelineRepository per D-04 immutable versioning:
    deactivate(id): update isActive false; include stages orderBy orderIndex asc with gatePolicies — mirror findById include shape.
    listAll(): findMany orderBy name asc; include stages select id only for stageCount mapping.
    resolveNextVersion(name): aggregate max version for name; return (max ?? 0) + 1.
    Update create() to set version from input.version ?? await resolveNextVersion(data.name) — pass version into prisma create data (schema has version Int @default(1); override when bumping).
    Extend packages/db/src/__tests__/pipeline.integration.test.ts per D-21: seed 3-stage pipeline with both metrics per stage; test deactivate sets isActive false; test listAll includes deactivated row; test resolveNextVersion increments after second create with same name different version.
    Update existing integration test fixtures to include both error_rate and latency_p95 per stage (Pitfall 5 in RESEARCH).
  </action>
  <verify>
    <automated>pnpm exec vitest run --project db pipeline.integration</automated>
  </verify>
  <done>Repository methods exist with passing db integration tests; fixtures use both required metrics per stage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contracts → API/DB | Shared Zod schemas are validation source of truth |
| GuardrailService | Pure logic; no I/O — callers enforce at trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | GatePolicyInputSchema | mitigate | MetricTypeSchema enum + positive threshold (V5 input validation) |
| T-07-02 | Elevation | Guardrail rules | mitigate | Centralize D-10/D-11 in GuardrailService; no ad-hoc checks in routes |
| T-07-03 | Tampering | PipelineUpdateRequestSchema | mitigate | Allowlist isActive + description only; no stage/flagKey mutation (D-04) |
| T-07-SC | Tampering | npm installs | accept | No new packages in this plan; existing workspace deps only |
</threat_model>

<verification>
- pnpm --filter @ff-promo/contracts run build
- pnpm exec vitest run --project api guardrail.service
- pnpm exec vitest run --project db pipeline.integration
</verification>

<success_criteria>
- Contracts extended with pipeline CRUD and guardrail types (D-03)
- GuardrailService validates all D-10/D-11 rules with unit tests (GRD-01, GRD-03 logic)
- Repository supports deactivate, listAll, version resolution (D-04, D-21)
</success_criteria>

<output>
Create `.planning/phases/07-guardrails-self-service/07-01-SUMMARY.md` when done
</output>
