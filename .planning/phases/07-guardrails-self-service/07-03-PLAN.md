---
phase: 07-guardrails-self-service
plan: 03
type: execute
wave: 2
depends_on:
  - "07-02"
files_modified:
  - apps/api/src/services/promotion-run.service.ts
  - apps/api/src/app.ts
  - apps/api/src/__tests__/guardrails.integration.test.ts
  - apps/web/src/hooks/use-pipelines.ts
  - apps/web/src/app/runs/new/page.tsx
autonomous: true
requirements:
  - GRD-02
  - GRD-03
user_setup: []
must_haves:
  truths:
    - "createRun rejects wrong flagKey with 403 before PromotionRun is persisted (GRD-03, D-08, D-19)"
    - "createRun rejects inactive pipeline with 403"
    - "startRun re-validates pipeline active and flagKey match before workflow start (D-08, Pitfall 3)"
    - "Valid self-service createRun + startRun succeeds when pipeline active and flagKey matches (GRD-02, D-09)"
    - "/runs/new pipeline picker shows only active pipelines despite listAll API (RESEARCH A5)"
  artifacts:
    - path: "apps/api/src/services/promotion-run.service.ts"
      provides: "GuardrailService hook in createRun and startRun"
      pattern: "validatePromotionRequest"
    - path: "apps/api/src/__tests__/guardrails.integration.test.ts"
      provides: "Rejection and acceptance integration tests (D-19)"
    - path: "apps/web/src/hooks/use-pipelines.ts"
      provides: "Active-only filter for developer picker"
  key_links:
    - from: "apps/api/src/services/promotion-run.service.ts"
      to: "apps/api/src/services/guardrail.service.ts"
      via: "validatePromotionRequest before repos.promotionRun.create"
      pattern: "validatePromotionRequest"
    - from: "apps/api/src/services/promotion-run.service.ts"
      to: "apps/api/src/errors/api-error.ts"
      via: "forbidden/unprocessableEntity/notFound on violation code"
      pattern: "forbidden|unprocessableEntity"
---

<objective>
Wire GuardrailService into promotion run create and start flows so out-of-bounds requests are rejected server-side; add integration tests proving GRD-02 self-service and GRD-03 enforcement; filter inactive pipelines from developer create-run picker.

Purpose: D-08, D-09, GRD-02, GRD-03 require API enforcement — not client-only checks. Existing POST /v1/promotion-runs surface unchanged.
Output: guardrails.integration.test.ts green; createRun/startRun reject violations; /runs/new filters active pipelines only.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-guardrails-self-service/07-CONTEXT.md
@.planning/phases/07-guardrails-self-service/07-RESEARCH.md
@apps/api/src/services/promotion-run.service.ts
@apps/api/src/services/guardrail.service.ts
@apps/api/src/__tests__/promotion-runs.control.test.ts
@apps/web/src/app/runs/new/page.tsx
@apps/web/src/hooks/use-pipelines.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guardrail integration tests (D-19, GRD-02, GRD-03)</name>
  <files>apps/api/src/__tests__/guardrails.integration.test.ts</files>
  <behavior>
    - rejects createRun when flagKey does not match pipeline with 403 forbidden
    - rejects createRun when pipeline is inactive with 403
    - rejects createRun when pipeline structurally invalid (e.g. missing gate policy) with 422
    - accepts valid createRun with matching flagKey returns 201
    - rejects startRun on deactivated pipeline after pending run created before deactivation
    - valid self-service flow: createRun 201 then startRun 200
  </behavior>
  <action>
    Create apps/api/src/__tests__/guardrails.integration.test.ts using testcontainers + buildApp pattern from promotion-runs.control.test.ts.
    Reuse seed helper from pipelines.create.test.ts or inline PipelineRepository.create with valid 3-stage both-metrics payload.
    Test wrong flagKey: POST /v1/promotion-runs expect 403, body.error forbidden; verify no PromotionRun row for wrong flagKey (query DB count).
    Test inactive: PATCH pipeline isActive false then POST createRun expect 403 pipeline_inactive message.
    Test valid self-service (GRD-02): POST createRun with pipeline.flagKey actorId dev expect 201; POST /v1/promotion-runs/:id/start expect 200.
    Test startRun guard: create pending run, deactivate pipeline, POST start expect 403.
    Use actor actorType user with distinct actorIds platform vs dev in payloads (D-18 audit distinction on promotion side unchanged).
  </action>
  <verify>
    <automated>test -f apps/api/src/__tests__/guardrails.integration.test.ts</automated>
  </verify>
  <done>Integration test file exists with all D-19 scenarios defined; tests fail until Task 2 wiring lands.</done>
</task>

<task type="auto">
  <name>Task 2: Wire guardrails into createRun and startRun (D-08, GRD-03)</name>
  <files>apps/api/src/services/promotion-run.service.ts, apps/api/src/app.ts</files>
  <action>
    In promotion-run.service.ts replace bare pipeline lookup in createRun with guardrail enforcement per RESEARCH Pattern 2:
    After createRequestDb, load pipeline via repos.pipeline.findById(input.pipelineId).
    Call validatePromotionRequest({ pipeline, flagKey: input.flagKey }) from guardrail.service.
    Map violations to ApiError: pipeline_not_found → notFound(v.message); httpStatus 403 → forbidden(v.message); 422 → unprocessableEntity(v.message). Throw on first violation.
    Only after clean validation call repos.promotionRun.create — ensure no run row exists on rejection (GRD-03).
    In startRun: after loading existing run and pending status check, load pipeline by existing.pipelineId; validatePromotionRequest with existing.flagKey before repos.audit.append run_started — prevents starting run against deactivated pipeline or post-deactivation race (Pitfall 3).
    Optionally inject guardrail helpers as module imports (pure functions) rather than DB-calling service factory — keep promotion-run.service owning pipeline fetch.
    No new routes — GRD-02 uses existing POST /v1/promotion-runs and POST .../start (D-09).
  </action>
  <verify>
    <automated>pnpm exec vitest run --project api guardrails.integration</automated>
  </verify>
  <done>createRun and startRun call validatePromotionRequest; Task 1 integration tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Filter active pipelines in /runs/new picker (RESEARCH A5)</name>
  <files>apps/web/src/hooks/use-pipelines.ts, apps/web/src/app/runs/new/page.tsx</files>
  <action>
    Update usePipelines to accept optional option { activeOnly?: boolean } default false for platform /pipelines list pages.
    When activeOnly true, filter pipelines where isActive === true after fetch (list API returns all per 07-02 listAll).
    In runs/new/page.tsx use usePipelines({ activeOnly: true }) so developers cannot pick deactivated pipelines in UI (GRD-02 UX); server still enforces 403 if bypassed (GRD-03).
    No API change required — client-side filter only for picker; guardrails remain authoritative.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project web create-run && pnpm --filter @ff-promo/web run build</automated>
  </verify>
  <done>Existing create-run tests pass; inactive pipelines excluded from picker when isActive false in mock data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /v1/promotion-runs | Untrusted flagKey and pipelineId |
| createRun/startRun | Last server-side gate before Temporal workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Elevation | POST /v1/promotion-runs | mitigate | validatePromotionRequest before persist (GRD-03, D-08) |
| T-07-09 | Elevation | POST .../start | mitigate | Re-validate pipeline on startRun (inactive bypass prevention) |
| T-07-10 | Spoofing | flagKey override | mitigate | flag_key_mismatch → 403 when input.flagKey !== pipeline.flagKey |
| T-07-11 | Tampering | Client picker filter | accept | UI filter is UX only; API enforcement is authoritative |
</threat_model>

<verification>
- pnpm exec vitest run --project api guardrails.integration
- pnpm exec vitest run --project web create-run
</verification>

<success_criteria>
- Out-of-bounds promotion requests rejected server-side (GRD-03)
- Valid developer self-service create + start works without new endpoints (GRD-02, D-09)
- Integration tests cover wrong flagKey, inactive pipeline, valid flow (D-19)
</success_criteria>

<output>
Create `.planning/phases/07-guardrails-self-service/07-03-SUMMARY.md` when done
</output>
