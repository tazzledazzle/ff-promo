---
phase: 10-telemetry-adapter-kotlin
plan: 02
type: execute
wave: 1
depends_on:
  - "10-01"
files_modified:
  - kotlin/modules/telemetry/src/main/kotlin/com/ffpromo/telemetry/query/BuildPromql.kt
  - kotlin/modules/telemetry/src/main/kotlin/com/ffpromo/telemetry/query/ParseResponse.kt
  - kotlin/modules/telemetry/src/test/kotlin/com/ffpromo/telemetry/query/PromqlBuilderTest.kt
  - kotlin/modules/telemetry/src/test/kotlin/com/ffpromo/telemetry/query/ParseResponseTest.kt
autonomous: true
requirements:
  - TELE-03
user_setup: []
must_haves:
  truths:
    - "buildErrorRateQuery and buildLatencyP95Query produce identical strings to v1 for same inputs"
    - "escapePromqlLabelValue escapes quotes and backslashes"
    - "buildMetricQuery throws UnsupportedMetricTypeError for unknown types"
    - "parseInstantQueryResult handles vector, scalar, empty, NaN fail-closed"
  artifacts:
    - path: "kotlin/modules/telemetry/src/main/kotlin/com/ffpromo/telemetry/query/BuildPromql.kt"
      provides: "PromQL builders TELE-03"
      contains: "buildErrorRateQuery"
    - path: "kotlin/modules/telemetry/src/main/kotlin/com/ffpromo/telemetry/query/ParseResponse.kt"
      provides: "Fail-closed response parser"
      contains: "parseInstantQueryResult"
  key_links:
    - from: "packages/telemetry/src/query/build-promql.ts"
      to: "BuildPromql.kt"
      via: "golden string parity"
      pattern: "ld_context_kind"
---

<objective>
Port PromQL builders and Prometheus response parser with golden-string unit tests — the core TELE-03 query contract.

Purpose: Gate evaluation (plan 03) depends on exact PromQL strings and fail-closed parsing.
Output: All promql-builder and parse-response v1 test scenarios ported.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-telemetry-adapter-kotlin/10-RESEARCH.md
@.planning/phases/10-telemetry-adapter-kotlin/10-PATTERNS.md
@.planning/phases/10-telemetry-adapter-kotlin/10-01-SUMMARY.md
@packages/telemetry/src/query/build-promql.ts
@packages/telemetry/src/query/parse-response.ts
@packages/telemetry/src/__tests__/promql-builder.test.ts
@packages/telemetry/src/__tests__/parse-response.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PromQL builders</name>
  <read_first>
    - packages/telemetry/src/query/build-promql.ts — full file
    - packages/telemetry/src/__tests__/promql-builder.test.ts
  </read_first>
  <files>kotlin/modules/telemetry/src/main/kotlin/com/ffpromo/telemetry/query/BuildPromql.kt, kotlin/modules/telemetry/src/test/kotlin/com/ffpromo/telemetry/query/PromqlBuilderTest.kt</files>
  <behavior>
    - Test: escapePromqlLabelValue for quotes/backslashes
    - Test: error rate query contains ld_context_kind, variation id, [300s], status=~"5.."
    - Test: latency p95 contains histogram_quantile(0.95 and * 1000
    - Test: sample count uses sum(increase(...))
    - Test: unsupported metric type throws UnsupportedMetricTypeError
  </behavior>
  <action>
    Port escapePromqlLabelValue, buildErrorRateQuery, buildLatencyP95Query, buildSampleCountQuery, buildMetricQuery.

    Use GatePolicyInput and GateRunContext from contracts (Pipeline.kt + Telemetry.kt).

    Cohort enum: treatment | control.

    Port all promql-builder.test.ts cases. Add optional golden full-string assertions copied from v1 if present.
  </action>
  <acceptance_criteria>
    - All v1 promql-builder tests ported
    - Query strings match v1 for canonical fixture inputs
  </acceptance_criteria>
  <verify>
    <automated>cd kotlin && ./gradlew :telemetry:test --tests "*PromqlBuilder*"</automated>
  </verify>
  <done>PromQL builders at parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Response parser</name>
  <read_first>
    - packages/telemetry/src/query/parse-response.ts
    - packages/telemetry/src/__tests__/parse-response.test.ts
    - packages/telemetry/src/__tests__/fixtures/prometheus-*.json
  </read_first>
  <files>kotlin/modules/telemetry/src/main/kotlin/com/ffpromo/telemetry/query/ParseResponse.kt, kotlin/modules/telemetry/src/test/kotlin/com/ffpromo/telemetry/query/ParseResponseTest.kt, kotlin/modules/telemetry/src/test/resources/fixtures/</files>
  <action>
    Port parseInstantQueryResult with ParseResult sealed type (ok value / fail reason).

    Handle resultType vector and scalar; no_data for empty/missing; non_finite_value for NaN/Inf.

    Copy prometheus-scalar-pass.json, prometheus-vector-pass.json, prometheus-vector-empty.json, prometheus-nan-value.json fixtures.

    Port parse-response.test.ts scenarios.
  </action>
  <acceptance_criteria>
    - All parse-response tests pass
    - Fixtures copied verbatim
  </acceptance_criteria>
  <verify>
    <automated>cd kotlin && ./gradlew :telemetry:test --tests "*ParseResponse*"</automated>
  </verify>
  <done>Fail-closed parser at parity.</done>
</task>

</tasks>

<verification>
```bash
cd kotlin && ./gradlew :telemetry:test --tests "*PromqlBuilder*" --tests "*ParseResponse*"
```
</verification>
