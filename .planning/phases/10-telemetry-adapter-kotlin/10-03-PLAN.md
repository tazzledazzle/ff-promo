---
phase: 10-telemetry-adapter-kotlin
plan: 03
type: execute
wave: 2
depends_on:
  - "10-02"
files_modified:
  - kotlin/modules/telemetry/src/main/kotlin/com/ffpromo/telemetry/evaluate/EvaluateGatePolicy.kt
  - kotlin/modules/telemetry/src/main/kotlin/com/ffpromo/telemetry/evaluate/EvaluateStageGates.kt
  - kotlin/modules/telemetry/src/main/kotlin/com/ffpromo/telemetry/preflight/RunPreflight.kt
  - kotlin/modules/telemetry/src/test/kotlin/com/ffpromo/telemetry/evaluate/EvaluateGatePolicyTest.kt
  - kotlin/modules/telemetry/src/test/kotlin/com/ffpromo/telemetry/evaluate/EvaluateStageGatesTest.kt
  - kotlin/modules/telemetry/src/test/kotlin/com/ffpromo/telemetry/preflight/RunPreflightTest.kt
autonomous: true
requirements:
  - TELE-03
  - TELE-04
  - TELE-01
  - TELE-02
user_setup: []
must_haves:
  truths:
    - "evaluateGatePolicy compares treatment-control delta against policy.threshold"
    - "Insufficient samples fail with metadata reason insufficient_samples"
    - "Prometheus errors fail-closed with prometheus_error metadata"
    - "evaluateStageGates returns pass only when all policies pass"
    - "runPreflightChecks emits metric_flow, min_sample, context_kind_user checks"
  artifacts:
    - path: "kotlin/modules/telemetry/src/main/kotlin/com/ffpromo/telemetry/evaluate/EvaluateGatePolicy.kt"
      provides: "TELE-03 gate evaluation"
      contains: "evaluateGatePolicy"
    - path: "kotlin/modules/telemetry/src/main/kotlin/com/ffpromo/telemetry/preflight/RunPreflight.kt"
      provides: "TELE-04 preflight"
      contains: "runPreflightChecks"
  key_links:
    - from: "EvaluateGatePolicy.kt"
      to: "BuildPromql.kt"
      via: "buildMetricQuery + buildSampleCountQuery"
      pattern: "buildMetricQuery"
---

<objective>
Implement gate policy evaluation, stage gate aggregation, and preflight health checks with unit tests ported from v1.

Purpose: Phase 11 worker activities call evaluateStageGates and runPreflightChecks before promotion advances.
Output: evaluate-gate-policy, evaluate-stage-gates, run-preflight test suites green with fixture-backed mock client.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-telemetry-adapter-kotlin/10-RESEARCH.md
@.planning/phases/10-telemetry-adapter-kotlin/10-PATTERNS.md
@.planning/phases/10-telemetry-adapter-kotlin/10-02-SUMMARY.md
@packages/telemetry/src/evaluate/evaluate-gate-policy.ts
@packages/telemetry/src/evaluate/evaluate-stage-gates.ts
@packages/telemetry/src/preflight/run-preflight.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: evaluateGatePolicy and evaluateStageGates</name>
  <read_first>
    - packages/telemetry/src/evaluate/evaluate-gate-policy.ts
    - packages/telemetry/src/evaluate/evaluate-stage-gates.ts
    - packages/telemetry/src/__tests__/evaluate-gate-policy.test.ts
    - packages/telemetry/src/__tests__/evaluate-stage-gates.test.ts
    - packages/telemetry/src/__tests__/fixtures/prometheus-treatment-error-rate.json
    - packages/telemetry/src/__tests__/fixtures/prometheus-control-error-rate.json
  </read_first>
  <files>kotlin/modules/telemetry/src/main/kotlin/com/ffpromo/telemetry/evaluate/EvaluateGatePolicy.kt, kotlin/modules/telemetry/src/main/kotlin/com/ffpromo/telemetry/evaluate/EvaluateStageGates.kt, kotlin/modules/telemetry/src/test/kotlin/com/ffpromo/telemetry/evaluate/EvaluateGatePolicyTest.kt, kotlin/modules/telemetry/src/test/kotlin/com/ffpromo/telemetry/evaluate/EvaluateStageGatesTest.kt</files>
  <behavior>
    - Test: pass when treatment-control delta <= threshold
    - Test: fail when delta exceeds threshold with threshold_exceeded metadata
    - Test: fail insufficient_samples for treatment/control cohorts
    - Test: fail on no_data parse results
    - Test: evaluateStageGates pass/fail aggregation across error_rate + latency_p95 policies (TELE-01/02)
  </behavior>
  <action>
  Port evaluateGatePolicy: 4 parallel queryInstant calls (treatment metric, control metric, treatment sample, control sample).

  Compare observedDelta = treatment - control; fail if > threshold.

  Port evaluateStageGates: sequential evaluateGatePolicy per policy; stage verdict pass iff all pass.

  Unit tests use fake PrometheusClient returning fixture JSON per query (or MockWebServer with enqueued responses).

  Copy remaining prometheus-* fixtures needed for gate tests.
  </action>
  <acceptance_criteria>
    - All v1 evaluate-gate-policy scenarios covered
    - Stage gates test with error_rate + latency_p95 policies
  </acceptance_criteria>
  <verify>
    <automated>cd kotlin && ./gradlew :telemetry:test --tests "*EvaluateGate*" --tests "*EvaluateStage*"</automated>
  </verify>
  <done>Gate evaluation logic at parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: runPreflightChecks</name>
  <read_first>
    - packages/telemetry/src/preflight/run-preflight.ts
    - packages/telemetry/src/__tests__/run-preflight.test.ts
    - packages/telemetry/src/__tests__/fixtures/prometheus-sample-count-high.json
    - packages/telemetry/src/__tests__/fixtures/prometheus-sample-count-low.json
  </read_first>
  <files>kotlin/modules/telemetry/src/main/kotlin/com/ffpromo/telemetry/preflight/RunPreflight.kt, kotlin/modules/telemetry/src/test/kotlin/com/ffpromo/telemetry/preflight/RunPreflightTest.kt</files>
  <behavior>
    - Test: all checks pass when samples above minSampleSize
    - Test: metric_flow fails when no data
    - Test: min_sample fails when below required
    - Test: context_kind_user check tied to treatment sample ok
    - Test: Prometheus error produces all-fail checks with blockReason
  </behavior>
  <action>
    Port runPreflightChecks with check ids: metric_flow_treatment, metric_flow_control, min_sample_treatment, min_sample_control, context_kind_user.

    requiredSampleSize = max of policy minSampleSize values.

    Return PreflightReport with status pass/fail and blockReason summary.

    Port run-preflight.test.ts scenarios.
  </action>
  <acceptance_criteria>
    - All v1 preflight tests ported
    - TELE-04 check ids match v1
  </acceptance_criteria>
  <verify>
    <automated>cd kotlin && ./gradlew :telemetry:test --tests "*RunPreflight*"</automated>
  </verify>
  <done>Preflight checks at parity.</done>
</task>

</tasks>

<verification>
```bash
cd kotlin && ./gradlew :telemetry:test --tests "*Evaluate*" --tests "*RunPreflight*"
```
</verification>
