---
phase: 10-telemetry-adapter-kotlin
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - kotlin/settings.gradle.kts
  - kotlin/modules/telemetry/build.gradle.kts
  - kotlin/modules/contracts/src/main/kotlin/com/ffpromo/contracts/Telemetry.kt
  - kotlin/modules/telemetry/src/main/kotlin/com/ffpromo/telemetry/client/PrometheusClient.kt
  - kotlin/modules/telemetry/src/main/kotlin/com/ffpromo/telemetry/errors/TelemetryAdapterError.kt
  - kotlin/modules/telemetry/src/main/kotlin/com/ffpromo/telemetry/TelemetryModule.kt
autonomous: true
requirements:
  - TELE-03
user_setup: []
must_haves:
  truths:
    - "kotlin/modules/telemetry builds as Gradle module depending on :contracts"
    - "Telemetry.kt mirrors packages/contracts/src/telemetry.ts"
    - "PrometheusClient queries /api/v1/query with optional bearer token"
    - "TelemetryAdapterError hierarchy matches v1"
  artifacts:
    - path: "kotlin/modules/contracts/src/main/kotlin/com/ffpromo/contracts/Telemetry.kt"
      provides: "GateRunContext, GateEvaluationResult, PreflightReport"
      contains: "GateRunContext"
    - path: "kotlin/modules/telemetry/src/main/kotlin/com/ffpromo/telemetry/client/PrometheusClient.kt"
      provides: "Prometheus instant query client"
      contains: "queryInstant"
  key_links:
    - from: "kotlin/settings.gradle.kts"
      to: "kotlin/modules/telemetry"
      via: "include telemetry"
      pattern: "telemetry"
---

<objective>
Scaffold Kotlin telemetry module, port telemetry contract types, and implement Prometheus HTTP client with error types.

Purpose: Foundation for PromQL builders and gate evaluation in plans 02–03; shared types for worker (Phase 11).
Output: `./gradlew :telemetry:compileKotlin` succeeds; PrometheusClient tested with MockWebServer.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-telemetry-adapter-kotlin/10-RESEARCH.md
@.planning/phases/10-telemetry-adapter-kotlin/10-PATTERNS.md
@packages/contracts/src/telemetry.ts
@packages/telemetry/src/client/prometheus-client.ts
@packages/telemetry/src/errors/telemetry-adapter-error.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: telemetry module and Telemetry.kt contracts</name>
  <read_first>
    - packages/contracts/src/telemetry.ts
    - .planning/phases/10-telemetry-adapter-kotlin/10-RESEARCH.md
  </read_first>
  <files>kotlin/settings.gradle.kts, kotlin/modules/telemetry/build.gradle.kts, kotlin/modules/contracts/src/main/kotlin/com/ffpromo/contracts/Telemetry.kt</files>
  <action>
    Add `:telemetry` to settings.gradle.kts.

    telemetry build.gradle.kts: depends on :contracts; OkHttp 4.12.0, kotlinx-coroutines, kotlinx-serialization; test: junit5, mockwebserver.

    Port telemetry.ts to Telemetry.kt:
    - PrometheusClientConfig, GateRunContext, GateEvaluationResult
    - PreflightCheck, PreflightReport, StageGateEvaluation
    - GateVerdictResult enum (pass/fail)

    Note: GatePolicyInput remains in Pipeline.kt (Phase 8) — do not duplicate.

    Add ContractsJsonTest golden decode for GateRunContext sample JSON.
  </action>
  <acceptance_criteria>
    - settings includes telemetry module
    - Telemetry.kt compiles
    - ./gradlew :contracts:build :telemetry:compileKotlin passes
  </acceptance_criteria>
  <verify>
    <automated>cd kotlin && ./gradlew :contracts:build :telemetry:compileKotlin</automated>
  </verify>
  <done>Module scaffold and telemetry DTOs ready.</done>
</task>

<task type="auto">
  <name>Task 2: PrometheusClient and errors</name>
  <read_first>
    - packages/telemetry/src/client/prometheus-client.ts
    - packages/telemetry/src/__tests__/prometheus-client.test.ts
    - packages/telemetry/src/errors/telemetry-adapter-error.ts
  </read_first>
  <files>kotlin/modules/telemetry/src/main/kotlin/com/ffpromo/telemetry/client/PrometheusClient.kt, kotlin/modules/telemetry/src/main/kotlin/com/ffpromo/telemetry/errors/TelemetryAdapterError.kt, kotlin/modules/telemetry/src/test/kotlin/com/ffpromo/telemetry/client/PrometheusClientTest.kt, kotlin/modules/telemetry/src/main/kotlin/com/ffpromo/telemetry/TelemetryModule.kt</files>
  <action>
    Port TelemetryAdapterError, TelemetryApiError, UnsupportedMetricTypeError.

    Implement createPrometheusClient(config):
    - DEFAULT_PROMETHEUS_BASE_URL = http://localhost:9090
    - Env: PROMETHEUS_BASE_URL, PROMETHEUS_BEARER_TOKEN
    - assertHttpOrHttpsUrl validation
    - suspend queryInstant(query, timeout?) → PrometheusInstantQueryData
    - Parse envelope: status success + data or throw TelemetryApiError
    - Retry on 503 only (2 retries) matching v1 p-retry

    PrometheusClientTest: MockWebServer returns vector JSON fixture; assert query param and Authorization header when bearer set.

    TelemetryModule.kt exports public API mirroring packages/telemetry/src/index.ts.
  </action>
  <acceptance_criteria>
    - Error types match v1 names
    - queryInstant parses success envelope
    - PrometheusClientTest passes
  </acceptance_criteria>
  <verify>
    <automated>cd kotlin && ./gradlew :telemetry:test --tests "*PrometheusClient*"</automated>
  </verify>
  <done>Prometheus client factory ready.</done>
</task>

</tasks>

<verification>
```bash
cd kotlin && ./gradlew :telemetry:compileKotlin :telemetry:test --tests "*PrometheusClient*"
```
</verification>
