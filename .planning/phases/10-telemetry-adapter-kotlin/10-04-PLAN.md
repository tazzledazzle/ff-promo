---
phase: 10-telemetry-adapter-kotlin
plan: 04
type: execute
wave: 3
depends_on:
  - "10-03"
files_modified:
  - kotlin/modules/telemetry/src/test/kotlin/com/ffpromo/telemetry/TelemetryIntegrationTest.kt
  - kotlin/modules/telemetry/src/test/resources/fixtures/
  - kotlin/README.md
  - README.md
autonomous: true
requirements:
  - TELE-03
  - TELE-04
  - TELE-01
  - TELE-02
user_setup: []
must_haves:
  truths:
    - "telemetry-integration.test.ts scenarios pass via MockWebServer E2E test"
    - "All prometheus fixtures from v1 copied"
    - "kotlin/README documents telemetry module"
    - "./gradlew :telemetry:test all green"
  artifacts:
    - path: "kotlin/modules/telemetry/src/test/kotlin/com/ffpromo/telemetry/TelemetryIntegrationTest.kt"
      provides: "E2E gate + preflight HTTP test"
      contains: "evaluateGatePolicy"
    - path: "kotlin/README.md"
      provides: "telemetry dev docs"
      contains: ":telemetry:test"
  key_links:
    - from: "packages/telemetry/src/__tests__/telemetry-integration.test.ts"
      to: "TelemetryIntegrationTest.kt"
      via: "scenario port"
      pattern: "queryInstant"
---

<objective>
Port telemetry integration tests, copy all v1 fixtures, and document telemetry module — closing TELE-03/04 verification.

Purpose: Phase 11 wires real PrometheusClient into worker activities; integration tests prove end-to-end query→parse→evaluate flow.
Output: Full `:telemetry:test` green; README updated.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-telemetry-adapter-kotlin/10-RESEARCH.md
@.planning/phases/10-telemetry-adapter-kotlin/10-PATTERNS.md
@.planning/phases/10-telemetry-adapter-kotlin/10-03-SUMMARY.md
@packages/telemetry/src/__tests__/telemetry-integration.test.ts
@packages/telemetry/src/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integration tests and fixtures</name>
  <read_first>
    - packages/telemetry/src/__tests__/telemetry-integration.test.ts
    - packages/telemetry/src/__tests__/fixtures/ (all JSON files)
  </read_first>
  <files>kotlin/modules/telemetry/src/test/kotlin/com/ffpromo/telemetry/TelemetryIntegrationTest.kt, kotlin/modules/telemetry/src/test/resources/fixtures/</files>
  <behavior>
    - Test: full evaluateGatePolicy flow with MockWebServer returning treatment/control/sample fixtures
    - Test: preflight pass/fail E2E
    - Test: PromQL query param matches expected builder output on wire
  </behavior>
  <action>
    Copy all 11 prometheus-*.json fixtures from v1 to src/test/resources/fixtures/.

    Port telemetry-integration.test.ts scenarios to TelemetryIntegrationTest.kt using createPrometheusClient pointed at MockWebServer.

    Verify TelemetryModule.kt exports match packages/telemetry/src/index.ts public API.
  </action>
  <acceptance_criteria>
    - All fixtures present
    - Integration tests pass
    - ./gradlew :telemetry:test fully green
  </acceptance_criteria>
  <verify>
    <automated>cd kotlin && ./gradlew :telemetry:test</automated>
  </verify>
  <done>Full telemetry test suite at parity.</done>
</task>

<task type="auto">
  <name>Task 2: Documentation</name>
  <read_first>
    - kotlin/README.md — ld-adapter section from Phase 9
  </read_first>
  <files>kotlin/README.md, README.md</files>
  <action>
    Add telemetry module section to kotlin/README.md:
    - Purpose: PromQL gates + preflight (TELE-03/04)
    - Env: PROMETHEUS_BASE_URL, PROMETHEUS_BEARER_TOKEN
    - Test: `./gradlew :telemetry:test`
    - Parity note: ports packages/telemetry

    Update root README v2 Kotlin section to mention telemetry module.

    Add to smoke checklist if present.
  </action>
  <acceptance_criteria>
    - kotlin/README documents telemetry
    - ./gradlew :telemetry:build passes
  </acceptance_criteria>
  <verify>
    <automated>cd kotlin && ./gradlew :telemetry:build</automated>
  </verify>
  <done>Phase 10 documented and build-integrated.</done>
</task>

</tasks>

<verification>
```bash
cd kotlin && ./gradlew :telemetry:test build
pnpm run build:kotlin
```
</verification>
