---
phase: 09-launchdarkly-adapter-kotlin
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - kotlin/settings.gradle.kts
  - kotlin/modules/ld-adapter/build.gradle.kts
  - kotlin/modules/contracts/src/main/kotlin/com/ffpromo/contracts/LaunchDarkly.kt
  - kotlin/modules/ld-adapter/src/main/kotlin/com/ffpromo/ldadapter/client/LdApiClient.kt
  - kotlin/modules/ld-adapter/src/main/kotlin/com/ffpromo/ldadapter/errors/LdAdapterError.kt
  - kotlin/modules/ld-adapter/src/main/kotlin/com/ffpromo/ldadapter/provider/FlagProvider.kt
  - kotlin/modules/ld-adapter/src/main/kotlin/com/ffpromo/ldadapter/LdAdapterModule.kt
autonomous: true
requirements:
  - PROV-01
user_setup: []
must_haves:
  truths:
    - "kotlin/modules/ld-adapter builds as Gradle module depending on :contracts"
    - "LaunchDarkly.kt DTOs mirror packages/contracts/src/launchdarkly.ts field names"
    - "LdApiClient configures Authorization and LD-API-Version headers"
    - "LdAdapterError hierarchy matches v1 exception types"
  artifacts:
    - path: "kotlin/modules/contracts/src/main/kotlin/com/ffpromo/contracts/LaunchDarkly.kt"
      provides: "FlagState, TargetingIntent, VariationRef DTOs"
      contains: "FlagState"
    - path: "kotlin/modules/ld-adapter/src/main/kotlin/com/ffpromo/ldadapter/client/LdApiClient.kt"
      provides: "LD REST client factory"
      contains: "LD-API-Version"
  key_links:
    - from: "kotlin/settings.gradle.kts"
      to: "kotlin/modules/ld-adapter"
      via: "include ld-adapter"
      pattern: "ld-adapter"
---

<objective>
Scaffold the Kotlin ld-adapter module, port LaunchDarkly contract types to kotlinx-serialization, and implement the LD REST client factory with error types.

Purpose: Foundation for read/write/resolver work in plans 02–03; contracts shared by worker (Phase 11) and API (Phase 12).
Output: `./gradlew :ld-adapter:compileKotlin` succeeds; LdApiClient unit-tested with MockWebServer GET stub.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-launchdarkly-adapter-kotlin/09-RESEARCH.md
@.planning/phases/09-launchdarkly-adapter-kotlin/09-PATTERNS.md
@packages/contracts/src/launchdarkly.ts
@packages/ld-adapter/src/client/ld-api-client.ts
@packages/ld-adapter/src/errors/ld-adapter-error.ts
@packages/ld-adapter/src/provider/flag-provider.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ld-adapter module and contracts LD types</name>
  <read_first>
    - .planning/phases/09-launchdarkly-adapter-kotlin/09-RESEARCH.md — Pattern 1 module layout
    - packages/contracts/src/launchdarkly.ts — all schemas
  </read_first>
  <files>kotlin/settings.gradle.kts, kotlin/modules/ld-adapter/build.gradle.kts, kotlin/modules/contracts/src/main/kotlin/com/ffpromo/contracts/LaunchDarkly.kt</files>
  <action>
    Add `:ld-adapter` to settings.gradle.kts with projectDir under modules/ld-adapter.

    ld-adapter build.gradle.kts: depends on :contracts; `com.launchdarkly:api-client:22.0.0` for GET; OkHttp 4.12.0 for semantic PATCH (per 09-RESEARCH hybrid stack); kotlinx-serialization-json + kotlinx-coroutines; test: junit5, mockwebserver.

    Add LdAdapterModule.kt barrel exports mirroring packages/ld-adapter/src/index.ts.

    Implement LdApiClient per 09-RESEARCH Pattern 2:
    - createLaunchDarklyClient configures api-client ApiClient (Authorization, LD-API-Version: 20240415)
    - LaunchDarklyRawClient holds config + FeatureFlagsApi + shared OkHttpClient for PATCH
    - Constants: SEMANTIC_PATCH_CONTENT_TYPE, DEFAULT_LD_BASE_URL

    Port launchdarkly.ts to LaunchDarkly.kt:
    - FlagVariation, FlagRule, FlagEnvironmentState, FlagState
    - VariationRef, RuleRef (sealed class + @SerialName discriminators)
    - RolloutIntent, TargetingIntent, GetFlagStateInput, ApplyTargetingInput
    - LaunchDarklyClientConfig
    - SemanticPatchInstruction sealed hierarchy (turnFlagOn, turnFlagOff, updateFallthrough..., updateRule...)

    Add ContractsJsonTest cases for VariationRef and TargetingIntent golden JSON if not covered.

    Define FlagProvider interface in ld-adapter matching v1: suspend getFlagState, suspend applyTargeting.
  </action>
  <acceptance_criteria>
    - settings.gradle.kts includes ld-adapter
    - LaunchDarkly.kt compiles with all v1 enum/string wire values
    - ./gradlew :contracts:build :ld-adapter:compileKotlin passes
  </acceptance_criteria>
  <verify>
    <automated>cd kotlin && ./gradlew :contracts:build :ld-adapter:compileKotlin</automated>
  </verify>
  <done>Module scaffold and shared LD contracts ready.</done>
</task>

<task type="auto">
  <name>Task 2: LdApiClient and error types</name>
  <read_first>
    - packages/ld-adapter/src/client/ld-api-client.ts
    - packages/ld-adapter/src/errors/ld-adapter-error.ts
    - packages/ld-adapter/src/__tests__/ld-api-client.test.ts
  </read_first>
  <files>kotlin/modules/ld-adapter/src/main/kotlin/com/ffpromo/ldadapter/client/LdApiClient.kt, kotlin/modules/ld-adapter/src/main/kotlin/com/ffpromo/ldadapter/errors/LdAdapterError.kt, kotlin/modules/ld-adapter/src/test/kotlin/com/ffpromo/ldadapter/client/LdApiClientTest.kt</files>
  <action>
    Implement LdAdapterError, UnresolvedVariationError, UnresolvedRuleError, LdRateLimitError, LdApiError, ApprovalRequiredError as Kotlin exception classes with same properties as v1.

    Implement LdApiClient(config: LaunchDarklyClientConfig):
    - Constants: SEMANTIC_PATCH_CONTENT_TYPE, DEFAULT_LD_BASE_URL, DEFAULT_LD_API_VERSION (20240415)
    - createLaunchDarklyClient: configure com.launchdarkly.api.ApiClient with accessToken, basePath, LD-API-Version header
    - getFeatureFlag(projectKey, flagKey): via FeatureFlagsApi (api-client), return raw flag JSON/model for mappers
    - patchFeatureFlag(projectKey, flagKey, body): OkHttp PATCH with semantic patch Content-Type (NOT api-client patchFeatureFlag)

    LdApiClientTest: MockWebServer base URL injected into api-client; assert Authorization and LD-API-Version on GET.
  </action>
  <acceptance_criteria>
    - Error class names match v1 exports
    - GET sets Authorization header
    - LdApiClientTest passes
  </acceptance_criteria>
  <verify>
    <automated>cd kotlin && ./gradlew :ld-adapter:test --tests "*LdApiClient*"</automated>
  </verify>
  <done>HTTP client factory and error taxonomy in place.</done>
</task>

</tasks>

<threat_model>
## Threat Model: Plan 09-01

| Threat | Mitigation |
|--------|------------|
| LD token logged | Never log Authorization header |
| DTO drift from v1 JSON | Golden decode tests in contracts |
| Wrong API version header | Assert LD-API-Version in client test |
</threat_model>

<verification>
```bash
cd kotlin && ./gradlew :ld-adapter:compileKotlin :ld-adapter:test --tests "*LdApiClient*"
```
</verification>
