---
phase: 09-launchdarkly-adapter-kotlin
plan: 04
type: execute
wave: 3
depends_on:
  - "09-03"
files_modified:
  - kotlin/modules/ld-adapter/src/main/kotlin/com/ffpromo/ldadapter/provider/LaunchDarklyProvider.kt
  - kotlin/modules/ld-adapter/src/test/kotlin/com/ffpromo/ldadapter/write/ApplyTargetingTest.kt
  - kotlin/modules/ld-adapter/src/test/kotlin/com/ffpromo/ldadapter/provider/LaunchDarklyProviderTest.kt
  - kotlin/modules/ld-adapter/src/test/resources/fixtures/patch-canary-success.json
  - kotlin/modules/ld-adapter/src/test/resources/fixtures/patch-429-retry.json
  - kotlin/modules/ld-adapter/src/test/resources/fixtures/patch-422-invalid.json
  - kotlin/README.md
autonomous: true
requirements:
  - PROV-01
  - PROV-02
  - PROV-03
user_setup: []
must_haves:
  truths:
    - "LaunchDarklyProvider exposes getFlagState and applyTargeting via rate limiter"
    - "createLaunchDarklyProvider factory matches v1 public API"
    - "ApplyTargeting integration test: GET → PATCH → GET with MockWebServer"
    - "429 retry fixture test passes"
    - "kotlin/README documents ld-adapter module and test commands"
  artifacts:
    - path: "kotlin/modules/ld-adapter/src/main/kotlin/com/ffpromo/ldadapter/provider/LaunchDarklyProvider.kt"
      provides: "FlagProvider facade"
      contains: "LaunchDarklyProvider"
    - path: "kotlin/modules/ld-adapter/src/test/kotlin/com/ffpromo/ldadapter/write/ApplyTargetingTest.kt"
      provides: "PROV-02 E2E HTTP test"
      contains: "applyTargeting"
  key_links:
    - from: "LaunchDarklyProvider.kt"
      to: "packages/ld-adapter/src/index.ts"
      via: "public exports parity"
      pattern: "createLaunchDarklyProvider"
---

<objective>
Wire LaunchDarklyProvider facade, port nock-style integration tests to MockWebServer, and document ld-adapter usage — closing PROV-01/02/03 parity verification.

Purpose: Phase 11 worker activities import ld-adapter provider; Phase 12 may expose LD state via API.
Output: `./gradlew :ld-adapter:test` all green; public API documented in kotlin/README.md.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-launchdarkly-adapter-kotlin/09-RESEARCH.md
@.planning/phases/09-launchdarkly-adapter-kotlin/09-PATTERNS.md
@.planning/phases/09-launchdarkly-adapter-kotlin/09-03-SUMMARY.md
@packages/ld-adapter/src/provider/launch-darkly-provider.ts
@packages/ld-adapter/src/__tests__/apply-targeting.test.ts
@packages/ld-adapter/src/__tests__/launch-darkly-provider.test.ts
@packages/ld-adapter/src/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LaunchDarklyProvider and applyTargeting integration tests</name>
  <read_first>
    - packages/ld-adapter/src/__tests__/apply-targeting.test.ts
    - packages/ld-adapter/src/__tests__/fixtures/patch-canary-success.json
    - packages/ld-adapter/src/__tests__/fixtures/patch-429-retry.json
    - packages/ld-adapter/src/__tests__/fixtures/patch-422-invalid.json
  </read_first>
  <files>kotlin/modules/ld-adapter/src/main/kotlin/com/ffpromo/ldadapter/provider/LaunchDarklyProvider.kt, kotlin/modules/ld-adapter/src/test/kotlin/com/ffpromo/ldadapter/write/ApplyTargetingTest.kt, kotlin/modules/ld-adapter/src/test/kotlin/com/ffpromo/ldadapter/provider/LaunchDarklyProviderTest.kt, kotlin/modules/ld-adapter/src/test/resources/fixtures/</files>
  <behavior>
    - Test: applyTargeting sends PATCH with semantic patch content type and correct instruction body
    - Test: 429 response retried then succeeds (patch-429-retry fixture sequence)
    - Test: 422 does not retry (patch-422-invalid)
    - Test: LaunchDarklyProvider delegates to rate limiter
  </behavior>
  <action>
    Implement LaunchDarklyProvider(rawClient, rateLimitedClient) and createLaunchDarklyProvider(config) factory.

    Copy remaining patch fixtures to test resources.

    ApplyTargetingTest: MockWebServer enqueues GET flag, PATCH success, GET flag responses — mirror v1 nock intercept order.

    Port launch-darkly-provider.test.ts smoke test.

    Optional: package-level exports documented in KDoc (match v1 index.ts export list).
  </action>
  <acceptance_criteria>
    - All ld-adapter tests pass
    - applyTargeting E2E test validates PATCH body against golden instruction
  </acceptance_criteria>
  <verify>
    <automated>cd kotlin && ./gradlew :ld-adapter:test</automated>
  </verify>
  <done>Full ld-adapter test suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Documentation and root build integration</name>
  <read_first>
    - kotlin/README.md — Phase 8 section
    - .planning/ROADMAP.md — Phase 9 success criteria
  </read_first>
  <files>kotlin/README.md, README.md</files>
  <action>
    Update kotlin/README.md with ld-adapter section:
    - Module purpose and dependency on contracts
    - Env vars: LD_ACCESS_TOKEN, LD_BASE_URL
    - Test command: `./gradlew :ld-adapter:test`
    - Parity note: ports packages/ld-adapter

    Add brief mention in root README v2 Kotlin section linking ld-adapter.

    Verify `./gradlew build` includes ld-adapter module (all kotlin tests).

    Add Phase 9 smoke item: `:ld-adapter:test` in kotlin/scripts/smoke.sh if present.
  </action>
  <acceptance_criteria>
    - kotlin/README documents ld-adapter
    - ./gradlew build succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd kotlin && ./gradlew build</automated>
  </verify>
  <done>Phase 9 adapter documented and integrated into build.</done>
</task>

</tasks>

<threat_model>
## Threat Model: Plan 09-04

| Threat | Mitigation |
|--------|------------|
| Tests hit real LD API | MockWebServer only; no live token in CI |
| Fixture drift | Copy JSON verbatim from v1 |
</threat_model>

<verification>
```bash
cd kotlin && ./gradlew :ld-adapter:test build
pnpm run build:kotlin
```
</verification>
