---
phase: 09-launchdarkly-adapter-kotlin
plan: 03
type: execute
wave: 2
depends_on:
  - "09-02"
files_modified:
  - kotlin/modules/ld-adapter/src/main/kotlin/com/ffpromo/ldadapter/write/SemanticPatch.kt
  - kotlin/modules/ld-adapter/src/main/kotlin/com/ffpromo/ldadapter/write/ApplyTargeting.kt
  - kotlin/modules/ld-adapter/src/main/kotlin/com/ffpromo/ldadapter/client/RateLimitedLdClient.kt
  - kotlin/modules/ld-adapter/src/test/kotlin/com/ffpromo/ldadapter/write/SemanticPatchBuilderTest.kt
  - kotlin/modules/ld-adapter/src/test/kotlin/com/ffpromo/ldadapter/client/RateLimitedClientTest.kt
autonomous: true
requirements:
  - PROV-02
  - PROV-03
user_setup: []
must_haves:
  truths:
    - "buildRolloutWeights sums to 100000 thousandths"
    - "buildTargetingPatchBody emits turnFlagOn/Off and rollout instructions"
    - "applyTargeting resolves variations, PATCHes LD, re-fetches flag state"
    - "RateLimitedLdClient retries 429/5xx with Retry-After backoff; no retry on 422/405"
  artifacts:
    - path: "kotlin/modules/ld-adapter/src/main/kotlin/com/ffpromo/ldadapter/write/ApplyTargeting.kt"
      provides: "PROV-02 semantic patch write"
      contains: "applyTargeting"
    - path: "kotlin/modules/ld-adapter/src/main/kotlin/com/ffpromo/ldadapter/client/RateLimitedLdClient.kt"
      provides: "Rate limit + retry"
      contains: "computeRetryDelayMs"
  key_links:
    - from: "write/ApplyTargeting.kt"
      to: "resolve/VariationResolver.kt"
      via: "resolveVariationId before patch"
      pattern: "resolveVariationId"
---

<objective>
Implement semantic patch builder, applyTargeting orchestration, and rate-limited client — completing PROV-02 writes and wiring PROV-03 resolution into the write path.

Purpose: Promotion engine Phase 11 calls applyTargeting to advance flag rollouts per environment.
Output: semantic-patch-builder and rate-limited-client tests ported from v1; applyTargeting ready for integration tests in plan 04.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-launchdarkly-adapter-kotlin/09-RESEARCH.md
@.planning/phases/09-launchdarkly-adapter-kotlin/09-PATTERNS.md
@.planning/phases/09-launchdarkly-adapter-kotlin/09-02-SUMMARY.md
@packages/ld-adapter/src/write/semantic-patch.ts
@packages/ld-adapter/src/write/apply-targeting.ts
@packages/ld-adapter/src/client/rate-limited-client.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Semantic patch builder</name>
  <read_first>
    - packages/ld-adapter/src/write/semantic-patch.ts
    - packages/ld-adapter/src/__tests__/semantic-patch-builder.test.ts
  </read_first>
  <files>kotlin/modules/ld-adapter/src/main/kotlin/com/ffpromo/ldadapter/write/SemanticPatch.kt, kotlin/modules/ld-adapter/src/test/kotlin/com/ffpromo/ldadapter/write/SemanticPatchBuilderTest.kt</files>
  <behavior>
    - Test: buildRolloutWeights valid/invalid thousandths
    - Test: fallthrough rollout instruction shape
    - Test: rule rollout requires ruleId
    - Test: turnFlagOn/Off instructions included per intent
  </behavior>
  <action>
    Port buildRolloutWeights, buildTargetingPatchBody, rolloutInstruction with ResolvedRolloutIds data class.

    Reject updatePercentageRollout kind if present (v1 guard).

    Port all semantic-patch-builder.test.ts cases — assert instruction JSON structure matches v1 golden outputs.
  </action>
  <acceptance_criteria>
    - Weight sum invariant enforced
    - All v1 builder tests ported
  </acceptance_criteria>
  <verify>
    <automated>cd kotlin && ./gradlew :ld-adapter:test --tests "*SemanticPatch*"</automated>
  </verify>
  <done>Patch body builder matches v1 contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RateLimitedLdClient</name>
  <read_first>
    - packages/ld-adapter/src/client/rate-limited-client.ts
    - packages/ld-adapter/src/__tests__/rate-limited-client.test.ts
  </read_first>
  <files>kotlin/modules/ld-adapter/src/main/kotlin/com/ffpromo/ldadapter/client/RateLimitedLdClient.kt, kotlin/modules/ld-adapter/src/test/kotlin/com/ffpromo/ldadapter/client/RateLimitedClientTest.kt</files>
  <behavior>
    - Test: computeRetryDelayMs from Retry-After header
    - Test: 429 retried up to max; 422 not retried
    - Test: 405 maps to ApprovalRequiredError
    - Test: maxConcurrent limits parallel schedule calls
  </behavior>
  <action>
    Port createRateLimitedLdClient with schedule { fn } suspend API.

    Port computeRetryDelayMs, shouldRetry, mapHttpError, normalizeThrownError logic.

    Use coroutines delay for sleep; configurable clock/sleep for tests.

    Port rate-limited-client.test.ts scenarios (use MockWebServer or mock fn injection).
  </action>
  <acceptance_criteria>
    - Retry behavior matches v1 test expectations
    - LdRateLimitError includes retryAfterMs when applicable
  </acceptance_criteria>
  <verify>
    <automated>cd kotlin && ./gradlew :ld-adapter:test --tests "*RateLimited*"</automated>
  </verify>
  <done>Rate limiting parity with v1.</done>
</task>

<task type="auto">
  <name>Task 3: applyTargeting orchestration</name>
  <read_first>
    - packages/ld-adapter/src/write/apply-targeting.ts
    - packages/ld-adapter/src/__tests__/apply-targeting.test.ts (structure for plan 04)
  </read_first>
  <files>kotlin/modules/ld-adapter/src/main/kotlin/com/ffpromo/ldadapter/write/ApplyTargeting.kt</files>
  <action>
    Implement suspend applyTargeting(deps, input): ApplyTargetingInput validation.

    Flow matching v1:
    1. rateLimited schedule → getFlagState
    2. If rollout: resolve variation IDs (+ ruleId if mode rule)
    3. buildTargetingPatchBody
    4. rateLimited schedule → patchFeatureFlag
    5. rateLimited schedule → getFlagState (return fresh state)

    Use LdApiClient from deps.rateLimitedClient.rawClient.

    Do not duplicate HTTP logic — delegate to LdApiClient.patchFeatureFlag.
  </action>
  <acceptance_criteria>
    - applyTargeting compiles and links resolvers + patch builder
    - Integration tests deferred to 09-04
  </acceptance_criteria>
  <verify>
    <automated>cd kotlin && ./gradlew :ld-adapter:compileKotlin</automated>
  </verify>
  <done>Write orchestration ready for E2E tests.</done>
</task>

</tasks>

<threat_model>
## Threat Model: Plan 09-03

| Threat | Mitigation |
|--------|------------|
| Partial patch on retry | LD semantic patch is atomic per request; idempotent reads after |
| Rate limit storm | maxConcurrent=2, backoff with jitter |
| Wrong rollout weights | buildRolloutWeights sum check |
</threat_model>

<verification>
```bash
cd kotlin && ./gradlew :ld-adapter:test --tests "*SemanticPatch*" --tests "*RateLimited*"
```
</verification>
