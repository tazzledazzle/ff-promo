---
phase: 03-telemetry-adapter
plan: 04
type: execute
wave: 3
depends_on:
  - "03-03"
files_modified:
  - packages/telemetry/src/__tests__/telemetry-integration.test.ts
  - packages/telemetry/src/__tests__/fixtures/prometheus-treatment-error-rate.json
  - packages/telemetry/src/__tests__/fixtures/prometheus-control-error-rate.json
  - packages/telemetry/src/__tests__/fixtures/prometheus-treatment-latency.json
  - packages/telemetry/src/__tests__/fixtures/prometheus-control-latency.json
  - packages/telemetry/src/__tests__/fixtures/prometheus-sample-count-low.json
  - packages/telemetry/src/index.ts
  - docker-compose.yml
  - prometheus/prometheus.yml
  - README.md
autonomous: true
requirements:
  - TELE-03
  - TELE-04
user_setup: []
must_haves:
  truths:
    - "Full gate evaluation and pre-flight flow verified with HTTP-level nock mocks (no live Prometheus in CI)"
    - "TELE-03 pass/fail/missing-data/min-sample scenarios covered at HTTP boundary"
    - "TELE-04 pre-flight pass and block scenarios covered at HTTP boundary"
    - "Package exports complete public adapter surface from index.ts"
  artifacts:
    - path: "packages/telemetry/src/__tests__/telemetry-integration.test.ts"
      provides: "End-to-end adapter tests with nock"
      contains: "nock"
    - path: "packages/telemetry/src/index.ts"
      provides: "Public API barrel"
      exports: ["createPrometheusClient", "evaluateGatePolicy", "evaluateStageGates", "runPreflightChecks"]
  key_links:
    - from: "packages/telemetry/src/__tests__/telemetry-integration.test.ts"
      to: "http://localhost:9090/api/v1/query"
      via: "nock HTTP intercept"
      pattern: "nock\\("
---

<objective>
Wire public exports and prove TELE-03/TELE-04 end-to-end with nock HTTP mocking — validating PromQL query contract, response parsing, gate evaluation, and pre-flight at the HTTP boundary without a live Prometheus server.

Purpose: Mocked client unit tests in plan 03 are insufficient for query param and multi-request sequence verification; HTTP-level tests gate phase completion and CI confidence per D-13.
Output: nock-based integration tests green, complete package exports, README and optional docker Prometheus profile documented.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-telemetry-adapter/03-RESEARCH.md
@.planning/phases/03-telemetry-adapter/03-PATTERNS.md
@packages/telemetry/src/evaluate/evaluate-gate-policy.ts
@packages/telemetry/src/preflight/run-preflight.ts
@packages/ld-adapter/src/__tests__/launch-darkly-provider.test.ts
@packages/db/src/seed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTTP integration fixtures for cohort gate scenarios</name>
  <read_first>
    - .planning/phases/03-telemetry-adapter/03-RESEARCH.md — Pattern 5 nock HTTP Tests, Validation Architecture test map
    - packages/ld-adapter/src/__tests__/fixtures/flag-boolean.json — fixture loading pattern
    - packages/telemetry/src/__tests__/fixtures/prometheus-vector-pass.json — existing fixture shape from plan 02
    - packages/db/src/seed.ts — demo-service thresholds error_rate 0.01, latency_p95 500
  </read_first>
  <files>packages/telemetry/src/__tests__/fixtures/prometheus-treatment-error-rate.json, packages/telemetry/src/__tests__/fixtures/prometheus-control-error-rate.json, packages/telemetry/src/__tests__/fixtures/prometheus-treatment-latency.json, packages/telemetry/src/__tests__/fixtures/prometheus-control-latency.json, packages/telemetry/src/__tests__/fixtures/prometheus-sample-count-low.json</files>
  <action>
    Add Prometheus API data-envelope fixtures (resultType + result) for nock replies:

    - prometheus-treatment-error-rate.json / prometheus-control-error-rate.json — values producing pass delta (treatment 0.02, control 0.01) and fail delta (treatment 0.05, control 0.01) scenarios used in separate tests
    - prometheus-treatment-latency.json / prometheus-control-latency.json — p95 ms values for pass and fail delta cases
    - prometheus-sample-count-low.json — sample count below typical minSampleSize (e.g. 50 when required 100)

    Align flagKey, serviceName demo-service, and variation IDs with constants used in integration test runContext.

    Reuse plan 02 empty/NaN fixtures where applicable — do not duplicate.
  </action>
  <acceptance_criteria>
    - Five cohort-specific fixture files exist
    - Fixtures use vector or scalar shapes compatible with parseInstantQueryResult
    - Values support both pass and fail test scenarios documented in test file
  </acceptance_criteria>
  <verify>
    <automated>test -f packages/telemetry/src/__tests__/fixtures/prometheus-treatment-error-rate.json && test -f packages/telemetry/src/__tests__/fixtures/prometheus-sample-count-low.json && ls packages/telemetry/src/__tests__/fixtures/prometheus-*.json | wc -l | grep -qv '^0$'</automated>
  </verify>
  <done>Cohort gate fixtures ready for nock integration tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: nock integration tests (TELE-03, TELE-04)</name>
  <read_first>
    - .planning/phases/03-telemetry-adapter/03-RESEARCH.md — Validation Architecture Phase Requirements → Test Map
    - packages/ld-adapter/src/__tests__/launch-darkly-provider.test.ts — nock lifecycle, nock.isDone(), fixture loading
    - packages/telemetry/src/evaluate/evaluate-gate-policy.ts
    - packages/telemetry/src/evaluate/evaluate-stage-gates.ts
    - packages/telemetry/src/preflight/run-preflight.ts
  </read_first>
  <files>packages/telemetry/src/__tests__/telemetry-integration.test.ts</files>
  <behavior>
    - TELE-03: nock GET /api/v1/query — error rate delta pass returns verdict pass with observedDelta
    - TELE-03: threshold exceeded returns verdict fail with reason threshold_exceeded
    - TELE-03: empty vector fixture returns verdict fail with reason no_data
    - TELE-03: low sample count returns verdict fail with reason insufficient_samples
    - TELE-03: latency_p95 pass and fail scenarios
    - TELE-03: evaluateStageGates with error_rate pass + latency_p95 fail → stage fail (D-09)
    - TELE-04: runPreflightChecks pass when treatment/control/sample/context checks succeed
    - TELE-04: runPreflightChecks fail with blockReason when treatment series missing
    - All happy-path tests assert nock.isDone()
  </behavior>
  <action>
    Write telemetry-integration.test.ts using nock against baseUrl http://localhost:9090 (match PROMETHEUS_BASE_URL default):

    - beforeEach: nock.cleanAll()
    - afterEach: nock.cleanAll(); assert nock.isDone() on happy paths
    - createPrometheusClient({ baseUrl: 'http://localhost:9090' })
    - Intercept GET /api/v1/query with .query(true) — reply based on query string matching treatment vs control vs sample PromQL fragments (ld_variation_id label distinguishes cohort)
    - Use seed-aligned GatePolicyInput fixtures: error_rate threshold 0.01, latency_p95 threshold 500, serviceName demo-service, windowSeconds 300, minSampleSize as needed per test
    - GateRunContext with flagKey demo-feature-flag and distinct treatment/control variation IDs

    Tag describe/it names with TELE-03 or TELE-04 requirement IDs.

    Do NOT require live Prometheus. Do NOT wire worker activities.
  </action>
  <acceptance_criteria>
    - TELE-03 and TELE-04 covered in telemetry-integration.test.ts
    - nock.isDone() asserted on happy paths
    - Empty vector and insufficient sample scenarios fail closed
    - Stage gates test proves D-09 one-fail-fails-stage
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project telemetry telemetry-integration</automated>
  </verify>
  <done>HTTP integration tests pass for gate evaluation and pre-flight.</done>
</task>

<task type="auto">
  <name>Task 3: Public exports, README, optional Prometheus docker profile (D-13)</name>
  <read_first>
    - .planning/phases/03-telemetry-adapter/03-CONTEXT.md — D-13, D-14, D-15
    - .planning/phases/03-telemetry-adapter/03-PATTERNS.md — index.ts barrel pattern
    - packages/ld-adapter/src/index.ts — public export surface
    - docker-compose.yml — existing service profile pattern
    - README.md — ld-adapter section if present
  </read_first>
  <files>packages/telemetry/src/index.ts, docker-compose.yml, prometheus/prometheus.yml, README.md</files>
  <action>
    Finalize packages/telemetry/src/index.ts exports:

    - createPrometheusClient
    - evaluateGatePolicy, evaluateStageGates, runPreflightChecks
    - escapePromqlLabelValue, buildErrorRateQuery, buildLatencyP95Query, buildSampleCountQuery, parseInstantQueryResult (public for testing/forensics)
    - All error classes from errors module
    - Re-export key types from @ff-promo/contracts (PrometheusClientConfig, GateRunContext, GateEvaluationResult, PreflightReport, StageGateEvaluation)

    Update README.md with telemetry section per D-14:

    - Env vars PROMETHEUS_BASE_URL, PROMETHEUS_BEARER_TOKEN
    - Test command: pnpm exec vitest run --project telemetry
    - Note CI uses nock not live Prometheus
    - Document metric label contract: service, ld_flag_key, ld_variation_id, ld_context_kind=user, http_requests_total, http_request_duration_seconds_bucket
    - Optional manual e2e: docker compose --profile prometheus up (D-13)

    Add docker-compose prometheus profile per Claude's discretion (D-13):

    - services.prometheus with profiles: [prometheus], image prom/prometheus, mount prometheus/prometheus.yml minimal scrape config
    - Not required for CI; document in README only

    No worker or API wiring in this phase.
  </action>
  <acceptance_criteria>
    - index.ts exports evaluateGatePolicy, evaluateStageGates, runPreflightChecks, createPrometheusClient
    - README documents Prometheus env vars, test command, and metric label contract
    - docker compose --profile prometheus defined OR README states optional profile path if deferred — prefer adding profile per RESEARCH RESOLVED
    - Full telemetry test suite green
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project telemetry && pnpm --filter @ff-promo/telemetry run build</automated>
  </verify>
  <done>Public exports wired; README updated; full test suite green; optional Prometheus profile documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → nock | Simulated Prometheus API responses in CI |
| docker profile → host | Optional manual Prometheus for operator validation only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Repudiation | Untested queryInstant HTTP contract | mitigate | nock integration tests assert request paths and multi-query sequences |
| T-03-10 | Information disclosure | Bearer token in README examples | mitigate | README uses empty PROMETHEUS_BEARER_TOKEN= placeholder |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | nock Approved in RESEARCH Package Legitimacy Audit; installed in plan 01 |
</threat_model>

<verification>
- pnpm exec vitest run --project telemetry
- pnpm test (full monorepo suite)
- pnpm --filter @ff-promo/telemetry run build
</verification>

<success_criteria>
- All TELE-03/TELE-04 behaviors verified with HTTP mocks
- Phase 3 ROADMAP success criteria #1-#4 satisfied by automated test suite
- Package ready for Phase 4 worker activity wiring (evaluateGate + preflight activities)
</success_criteria>

<output>
Create `.planning/phases/03-telemetry-adapter/03-04-SUMMARY.md` when done
</output>
