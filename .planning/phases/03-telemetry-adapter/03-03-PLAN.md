---
phase: 03-telemetry-adapter
plan: 03
type: execute
wave: 2
depends_on:
  - "03-02"
files_modified:
  - packages/telemetry/src/evaluate/evaluate-gate-policy.ts
  - packages/telemetry/src/evaluate/evaluate-stage-gates.ts
  - packages/telemetry/src/preflight/run-preflight.ts
  - packages/telemetry/src/__tests__/evaluate-gate-policy.test.ts
  - packages/telemetry/src/__tests__/evaluate-stage-gates.test.ts
  - packages/telemetry/src/__tests__/run-preflight.test.ts
  - packages/telemetry/src/index.ts
autonomous: true
requirements:
  - TELE-03
  - TELE-04
must_haves:
  truths:
    - "System evaluates error rate SLO as delta treatment minus control against policy threshold (D-02, TELE-03)"
    - "System evaluates latency p95 SLO as delta in milliseconds against policy threshold (TELE-03)"
    - "Gate fails when sample count below minSampleSize for treatment or control (D-07)"
    - "Stage with multiple GatePolicies fails if any single policy fails (D-09)"
    - "Pre-flight verifies metric flow, min sample size, and user context kind before promotion start (D-10, TELE-04)"
    - "Pre-flight failure returns blockReason and status fail — blocks promotion start (D-11)"
  artifacts:
    - path: "packages/telemetry/src/evaluate/evaluate-gate-policy.ts"
      provides: "Single GatePolicy SLO evaluation"
      exports: ["evaluateGatePolicy"]
    - path: "packages/telemetry/src/evaluate/evaluate-stage-gates.ts"
      provides: "All stage policies must pass"
      exports: ["evaluateStageGates"]
    - path: "packages/telemetry/src/preflight/run-preflight.ts"
      provides: "Pre-flight health report"
      exports: ["runPreflightChecks"]
  key_links:
    - from: "packages/telemetry/src/evaluate/evaluate-gate-policy.ts"
      to: "createPrometheusClient.queryInstant"
      via: "treatment + control + sample count queries per metric"
      pattern: "queryInstant"
    - from: "packages/telemetry/src/preflight/run-preflight.ts"
      to: "packages/telemetry/src/query/build-promql.ts"
      via: "reuses builders and parser"
      pattern: "buildSampleCountQuery"
---

<objective>
Implement TELE-03 SLO gate evaluation (delta-vs-control per policy) and TELE-04 pre-flight health checks — the core adapter logic Phase 4 worker activities will call.

Purpose: ROADMAP success criteria #1-#4 require real gate verdicts and pre-flight blocking; adapter returns structured DTOs without persisting GateResult (D-12).
Output: evaluateGatePolicy, evaluateStageGates, runPreflightChecks with unit tests using mocked client.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-telemetry-adapter/03-CONTEXT.md
@.planning/phases/03-telemetry-adapter/03-RESEARCH.md
@.planning/phases/03-telemetry-adapter/03-PATTERNS.md
@packages/contracts/src/telemetry.ts
@packages/telemetry/src/query/build-promql.ts
@packages/telemetry/src/query/parse-response.ts
@packages/ld-adapter/src/resolve/variation-resolver.ts
@apps/worker/src/activities/evaluate-gate.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: evaluateGatePolicy — delta-vs-control SLO (TELE-03, D-02, D-06, D-07, D-08)</name>
  <read_first>
    - .planning/phases/03-telemetry-adapter/03-CONTEXT.md — D-02, D-06, D-07, D-08
    - .planning/phases/03-telemetry-adapter/03-RESEARCH.md — Pattern 3 fail-closed table, RESOLVED two-query delta approach, A3 threshold units
    - packages/telemetry/src/query/build-promql.ts — builder exports
    - packages/telemetry/src/query/parse-response.ts — ParseResult shape
    - packages/ld-adapter/src/resolve/variation-resolver.ts — fail-closed posture (structured fail vs throw)
  </read_first>
  <files>packages/telemetry/src/evaluate/evaluate-gate-policy.ts, packages/telemetry/src/__tests__/evaluate-gate-policy.test.ts</files>
  <behavior>
    - Pass: treatment error rate 0.02, control 0.01, threshold 0.01, delta 0.01 → verdict pass (delta not greater than threshold — use fail when delta > threshold)
    - Fail threshold_exceeded: treatment 0.05, control 0.01, threshold 0.01 → verdict fail, reason threshold_exceeded
    - Fail no_data: client returns empty vector for treatment → verdict fail, reason no_data
    - Fail insufficient_samples: sample count below minSampleSize → verdict fail, reason insufficient_samples
    - Latency_p95: delta in ms compared to threshold (500ms seed baseline)
    - Returns treatmentValue, controlValue, observedDelta in result for Phase 5 forensics
  </behavior>
  <action>
    Create evaluate-gate-policy.ts:

    - export async function evaluateGatePolicy(client, policy: GatePolicyInput, runContext: GateRunContext): Promise&lt;GateEvaluationResult&gt;
    - Parse runContext with GateRunContextSchema at entry
    - For metricType error_rate or latency_p95: run three queryInstant calls via client — treatment metric, control metric, treatment sample count, control sample count (four queries total; sample counts use buildSampleCountQuery)
    - Parse each response with parseInstantQueryResult; any parse failure → verdict fail with metadata.reason from parser
    - Enforce minSampleSize (default 0) on both cohorts independently per D-07 before delta comparison
    - Compute observedDelta = treatmentValue - controlValue per D-02; verdict fail when observedDelta > policy.threshold OR any prerequisite fail
    - Verdict pass when observedDelta <= threshold and all checks pass
    - Ignore policy.comparisonMode — always delta-vs-control
    - Do NOT persist GateResult; do NOT read process.env
    - Do NOT modify apps/worker evaluate-gate stub (Phase 4)

    Write evaluate-gate-policy.test.ts with mocked client object { queryInstant: vi.fn() } returning canned envelopes — no nock yet (plan 04). Cover pass, threshold_exceeded, no_data, insufficient_samples, latency_p95 pass/fail.

    Export evaluateGatePolicy from index.ts.
  </action>
  <acceptance_criteria>
    - Delta computed as treatment minus control per D-02
    - Fail closed on empty/missing data per D-06
    - minSampleSize enforced per D-07
    - GateEvaluationResult includes treatmentValue, controlValue, observedDelta, metadata.reason
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project telemetry evaluate-gate-policy</automated>
  </verify>
  <done>Single-policy evaluation passes unit tests for error_rate and latency_p95 scenarios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: evaluateStageGates — all policies must pass (D-09, TELE-03)</name>
  <read_first>
    - .planning/phases/03-telemetry-adapter/03-CONTEXT.md — D-09
    - .planning/phases/03-telemetry-adapter/03-RESEARCH.md — evaluateStageGates pattern, seed dual policies error_rate + latency_p95
    - packages/db/src/seed.ts — stage with two GatePolicies
  </read_first>
  <files>packages/telemetry/src/evaluate/evaluate-stage-gates.ts, packages/telemetry/src/__tests__/evaluate-stage-gates.test.ts</files>
  <behavior>
    - Two policies both pass → stage verdict pass, results length 2
    - One policy pass one fail → stage verdict fail
    - Empty policies array → stage verdict pass with empty results (or document as pass — no gates to evaluate)
    - Results array preserves order matching input policies
  </behavior>
  <action>
    Create evaluate-stage-gates.ts:

    - export async function evaluateStageGates(client, policies: GatePolicyInput[], runContext: GateRunContext): Promise&lt;StageGateEvaluation&gt; per contracts
    - Loop policies calling evaluateGatePolicy sequentially (concurrency optional; keep simple)
    - Stage verdict fail if ANY result.verdict === 'fail' per D-09
    - Stage verdict pass only when all policies pass
    - Return { verdict, results } — no DB persistence

    Write evaluate-stage-gates.test.ts mocking evaluateGatePolicy or client to return mixed pass/fail — verify D-09 all-must-pass semantics with error_rate + latency_p95 pair mirroring seed.

    Export evaluateStageGates from index.ts.
  </action>
  <acceptance_criteria>
    - Single failing policy fails entire stage per D-09
    - Results array contains one GateEvaluationResult per policy
    - StageGateEvaluation matches contracts schema
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project telemetry evaluate-stage-gates</automated>
  </verify>
  <done>Stage-level evaluation enforces all-must-pass semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: runPreflightChecks — health report before promotion (TELE-04, D-10, D-11, D-12)</name>
  <read_first>
    - .planning/phases/03-telemetry-adapter/03-CONTEXT.md — D-10, D-11, D-12
    - .planning/phases/03-telemetry-adapter/03-RESEARCH.md — Pattern 4 PreflightReport shape, max minSampleSize across policies
    - packages/ld-adapter/src/provider/launch-darkly-provider.ts — orchestration pattern
  </read_first>
  <files>packages/telemetry/src/preflight/run-preflight.ts, packages/telemetry/src/__tests__/run-preflight.test.ts</files>
  <behavior>
    - Pass when treatment and control sample queries return data, counts meet max(minSampleSize), user context kind series present
    - Fail when treatment metric flow missing → metric_flow_treatment fail, status fail, blockReason set
    - Fail when control metric flow missing → metric_flow_control fail
    - Fail when samples below max minSampleSize across policies
    - Fail when no user-scoped series (context_kind_user check)
    - Does not write GateResult rows (D-12)
  </behavior>
  <action>
    Create run-preflight.ts:

    - export async function runPreflightChecks(client, policies: GatePolicyInput[], runContext: GateRunContext): Promise&lt;PreflightReport&gt;
    - Reuse buildSampleCountQuery and buildErrorRateQuery (or lightweight metric flow probe — sample count > 0 suffices for metric_flow checks per D-10) + parseInstantQueryResult
    - Compute requiredSampleSize = max of policy.minSampleSize ?? 0 across policies for preflight per RESEARCH RESOLVED
    - Run checks per D-10: metric_flow_treatment, metric_flow_control (non-empty parse ok), min_sample_treatment, min_sample_control (count >= requiredSampleSize), context_kind_user (user-scoped series present — fail if treatment sample query with ld_context_kind=user returns no_data)
    - status pass only when all checks pass; set blockReason human-readable summary on fail per D-11
    - Return PreflightReport — caller blocks promotion start on status fail; adapter does not call Temporal or DB

    Write run-preflight.test.ts with mocked client covering pass, missing treatment, missing control, insufficient samples, missing user context kind.

    Export runPreflightChecks from index.ts.
  </action>
  <acceptance_criteria>
    - PreflightReport includes all five check ids from D-10
    - status fail sets blockReason per D-11
    - Uses max minSampleSize across policies for preflight sample checks
    - No GateResult persistence (D-12)
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project telemetry run-preflight</automated>
  </verify>
  <done>Pre-flight health checks pass unit tests; TELE-04 behavior verified with mocked client.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → evaluateGatePolicy | GatePolicy thresholds and run context from orchestrator |
| adapter → Prometheus | Read-only instant queries; no mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | Missing data treated as pass | mitigate | Fail closed at parse and evaluate layers per D-06; unit tests assert no_data → fail |
| T-03-07 | Denial of Service | Expensive parallel queries | mitigate | Pass timeout opt to queryInstant; sequential queries acceptable for v1 |
| T-03-08 | Repudiation | Gate verdict without forensics | mitigate | GateEvaluationResult includes treatmentValue, controlValue, observedDelta, metadata.reason |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
- pnpm exec vitest run --project telemetry
- pnpm --filter @ff-promo/telemetry run build
</verification>

<success_criteria>
- TELE-03: error rate and latency p95 delta gates evaluate with fail-closed missing data and minSampleSize
- TELE-04: pre-flight returns structured report that blocks promotion start on failure
- D-09: stage fails when any policy fails
- Worker evaluate-gate stub unchanged (Phase 4 wiring)
</success_criteria>

<output>
Create `.planning/phases/03-telemetry-adapter/03-03-SUMMARY.md` when done
</output>
