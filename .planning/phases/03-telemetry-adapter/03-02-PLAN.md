---
phase: 03-telemetry-adapter
plan: 02
type: execute
wave: 1
depends_on:
  - "03-01"
files_modified:
  - packages/telemetry/src/query/build-promql.ts
  - packages/telemetry/src/query/parse-response.ts
  - packages/telemetry/src/__tests__/promql-builder.test.ts
  - packages/telemetry/src/__tests__/parse-response.test.ts
  - packages/telemetry/src/__tests__/fixtures/prometheus-vector-pass.json
  - packages/telemetry/src/__tests__/fixtures/prometheus-vector-empty.json
  - packages/telemetry/src/__tests__/fixtures/prometheus-scalar-pass.json
  - packages/telemetry/src/__tests__/fixtures/prometheus-error-envelope.json
  - packages/telemetry/src/__tests__/fixtures/prometheus-nan-value.json
  - packages/db/src/__tests__/pipeline.integration.test.ts
  - packages/db/src/__tests__/gate-result.integration.test.ts
  - packages/telemetry/src/index.ts
autonomous: true
requirements:
  - TELE-03
must_haves:
  truths:
    - "Built-in PromQL generated from GatePolicy + run context without raw templates in DB (D-04)"
    - "Cohort queries filter service, ld_flag_key, ld_variation_id, ld_context_kind=user (D-01, D-03)"
    - "Window-bound instant queries use GatePolicy.windowSeconds as [Ns] suffix (D-08)"
    - "Empty Prometheus vectors and non-finite values fail closed before numeric comparison (D-06)"
    - "Canonical metric types error_rate and latency_p95 only; unknown types rejected"
  artifacts:
    - path: "packages/telemetry/src/query/build-promql.ts"
      provides: "Built-in PromQL builders with label escaping"
      exports: ["escapePromqlLabelValue", "buildErrorRateQuery", "buildLatencyP95Query", "buildSampleCountQuery"]
    - path: "packages/telemetry/src/query/parse-response.ts"
      provides: "Fail-closed instant query result parser"
      exports: ["parseInstantQueryResult"]
  key_links:
    - from: "packages/telemetry/src/query/build-promql.ts"
      to: "GatePolicyInput + GateRunContext"
      via: "serviceName, windowSeconds, variation IDs"
      pattern: "ld_context_kind=\"user\""
    - from: "packages/telemetry/src/query/parse-response.ts"
      to: "Prometheus API envelope"
      via: "vector and scalar resultType handling"
      pattern: "resultType"
---

<objective>
Implement built-in PromQL query builders and fail-closed Prometheus response parsing — pure functions with no HTTP or env access that gate evaluation and pre-flight will consume.

Purpose: D-04 locks PromQL in version-controlled code; D-06/D-08 require centralized parsing before any pass/fail verdict. Evaluators in plan 03 depend on these contracts.
Output: build-promql.ts, parse-response.ts, JSON fixtures, unit tests; metricType normalization in db integration tests.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-telemetry-adapter/03-CONTEXT.md
@.planning/phases/03-telemetry-adapter/03-RESEARCH.md
@.planning/phases/03-telemetry-adapter/03-PATTERNS.md
@packages/contracts/src/telemetry.ts
@packages/contracts/src/pipeline.ts
@packages/db/src/seed.ts
@packages/ld-adapter/src/write/semantic-patch.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PromQL builders with label escaping (D-01, D-04, D-05, D-08)</name>
  <read_first>
    - .planning/phases/03-telemetry-adapter/03-CONTEXT.md — D-01, D-04, D-05, D-08
    - .planning/phases/03-telemetry-adapter/03-RESEARCH.md — Pattern 2 Built-in PromQL, RESOLVED error_rate and latency_p95 expressions, Pitfall 6 metricType drift
    - packages/db/src/seed.ts — canonical error_rate and latency_p95 policies for demo-service
    - packages/ld-adapter/src/write/semantic-patch.ts — pure builder validation pattern
  </read_first>
  <files>packages/telemetry/src/query/build-promql.ts, packages/telemetry/src/__tests__/promql-builder.test.ts</files>
  <behavior>
    - escapePromqlLabelValue escapes backslash and double-quote in flag keys and variation IDs
    - buildErrorRateQuery produces ratio with status=~"5.." and [windowSeconds]s window for treatment variation
    - buildLatencyP95Query produces histogram_quantile(0.95, ...) * 1000 for ms output
    - buildSampleCountQuery produces sum(increase(http_requests_total{...}[window])) for minSampleSize checks
    - All queries include ld_context_kind="user" label selector per D-03
    - Window suffix uses policy.windowSeconds defaulting to 300 when omitted
    - Unknown metricType in helper switch throws UnsupportedMetricTypeError
  </behavior>
  <action>
    Create packages/telemetry/src/query/build-promql.ts as pure functions (no process.env, no fetch):

    - export escapePromqlLabelValue(value: string): string — escape \ and " per RESEARCH Don't Hand-Roll table
    - export buildErrorRateQuery(policy: GatePolicyInput, runContext: GateRunContext, cohort: 'treatment' | 'control'): string — use RESEARCH RESOLVED error_rate PromQL; variation id from treatmentVariationId or controlVariationId; service from policy.serviceName; flag from runContext.flagKey
    - export buildLatencyP95Query(...): string — RESEARCH RESOLVED latency_p95 PromQL with http_request_duration_seconds_bucket and by (le)
    - export buildSampleCountQuery(...): string — RESEARCH RESOLVED sample count PromQL
    - export buildMetricQuery(metricType, policy, runContext, cohort): string — switch on canonical types error_rate and latency_p95 only; reject others with UnsupportedMetricTypeError (D-04, Pitfall 6)
    - Ignore policy.comparisonMode at runtime — v1 always delta-vs-control per D-02

    Write promql-builder.test.ts covering label escaping, window suffix [300s], cohort label selectors, both canonical metric types, and UnsupportedMetricTypeError for p95_latency_ms or unknown strings.

    Export builders from index.ts.
  </action>
  <acceptance_criteria>
    - PromQL includes service, ld_flag_key, ld_variation_id, ld_context_kind="user" selectors per D-01
    - Window uses [${windowSeconds}s] from GatePolicy per D-08
    - Only error_rate and latency_p95 supported in v1
    - Label values with quotes/backslashes produce valid PromQL
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project telemetry promql-builder</automated>
  </verify>
  <done>PromQL builders pass unit tests; canonical metric types enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail-closed response parser (D-06, D-07)</name>
  <read_first>
    - .planning/phases/03-telemetry-adapter/03-RESEARCH.md — Pattern 3 Fail-Closed Result Parsing, Pitfalls 1-4 scalar vs vector
    - .planning/phases/03-telemetry-adapter/03-PATTERNS.md — parse-response.ts analog from ld-adapter mappers
    - packages/ld-adapter/src/read/mappers.ts — response to domain mapping style
  </read_first>
  <files>packages/telemetry/src/query/parse-response.ts, packages/telemetry/src/__tests__/parse-response.test.ts, packages/telemetry/src/__tests__/fixtures/prometheus-vector-pass.json, packages/telemetry/src/__tests__/fixtures/prometheus-vector-empty.json, packages/telemetry/src/__tests__/fixtures/prometheus-scalar-pass.json, packages/telemetry/src/__tests__/fixtures/prometheus-error-envelope.json, packages/telemetry/src/__tests__/fixtures/prometheus-nan-value.json</files>
  <behavior>
    - Vector with single sample returns { ok: true, value: number }
    - Scalar resultType returns { ok: true, value: number }
    - Empty result array returns { ok: false, reason: 'no_data' }
    - Sample value "NaN", "+Inf", "-Inf" returns { ok: false, reason: 'non_finite_value' }
    - Missing or malformed data returns { ok: false, reason: 'no_data' }
  </behavior>
  <action>
    Create packages/telemetry/src/query/parse-response.ts:

    - export type ParseResult = { ok: true; value: number } | { ok: false; reason: 'no_data' | 'non_finite_value' }
    - export parseInstantQueryResult(data: { resultType: string; result: unknown }): ParseResult
    - Handle resultType vector: extract value[1] from first result entry; fail on empty result per D-06
    - Handle resultType scalar: extract value tuple [timestamp, valueString]
    - Parse numeric string to number; reject NaN/Inf string forms before comparison
    - Pure function — no throws for expected fail-closed paths (throws reserved for caller on HTTP errors)

    Add JSON fixtures under src/__tests__/fixtures/ per D-16 mimicking Prometheus API data envelope shapes (not full HTTP wrapper — parser accepts data sub-object).

    Write parse-response.test.ts loading fixtures via readFileSync pattern from ld-adapter tests.

    Export parseInstantQueryResult from index.ts.
  </action>
  <acceptance_criteria>
    - Empty vector maps to no_data fail per D-06
    - NaN sample maps to non_finite_value fail
    - Both vector and scalar result types handled
    - Five fixture files exist under src/__tests__/fixtures/
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --project telemetry parse-response</automated>
  </verify>
  <done>Parser passes unit tests with vector, scalar, empty, and NaN fixtures.</done>
</task>

<task type="auto">
  <name>Task 3: Normalize latency_p95 metricType in db integration tests</name>
  <read_first>
    - .planning/phases/03-telemetry-adapter/03-RESEARCH.md — Pitfall 6 metricType String Drift, RESOLVED canonical latency_p95
    - packages/db/src/seed.ts — GATE_POLICIES uses latency_p95
    - packages/db/src/__tests__/pipeline.integration.test.ts — locate p95_latency_ms usage
    - packages/db/src/__tests__/gate-result.integration.test.ts — locate p95_latency_ms usage
  </read_first>
  <files>packages/db/src/__tests__/pipeline.integration.test.ts, packages/db/src/__tests__/gate-result.integration.test.ts</files>
  <action>
    Replace p95_latency_ms with latency_p95 in db integration test fixtures per RESEARCH Pitfall 6 RESOLVED decision — align with seed.ts canonical metricType strings.

    Do not change Prisma schema or seed data (already uses latency_p95).

    No functional adapter code in this task — test string normalization only.
  </action>
  <acceptance_criteria>
    - No p95_latency_ms strings remain in pipeline.integration.test.ts or gate-result.integration.test.ts
    - Both files use latency_p95 consistent with seed.ts
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' packages/db/src/__tests__/pipeline.integration.test.ts packages/db/src/__tests__/gate-result.integration.test.ts | grep -c p95_latency_ms | grep -q '^0$' && pnpm exec vitest run --project db</automated>
  </verify>
  <done>Db integration tests use canonical latency_p95 metricType.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GatePolicy fields → PromQL string | User-influenced flagKey, variationId, serviceName interpolated into queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | PromQL injection via label values | mitigate | escapePromqlLabelValue() with unit tests for " and \ ; never accept raw PromQL from input (D-04) |
| T-03-05 | Tampering | Empty vector interpreted as zero | mitigate | parseInstantQueryResult fails closed with no_data before evaluators compare thresholds (D-06) |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
- pnpm exec vitest run --project telemetry
- pnpm exec vitest run --project db
- pnpm --filter @ff-promo/telemetry run build
</verification>

<success_criteria>
- PromQL builders generate cohort-scoped queries for error_rate and latency_p95 with user context kind
- Response parser fail-closed on empty vectors and non-finite values
- Db tests aligned on canonical metricType strings
</success_criteria>

<output>
Create `.planning/phases/03-telemetry-adapter/03-02-SUMMARY.md` when done
</output>
