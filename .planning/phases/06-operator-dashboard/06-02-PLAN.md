---
phase: 06-operator-dashboard
plan: 02
type: execute
wave: 1
depends_on:
  - "06-01"
files_modified:
  - apps/web/src/app/runs/page.tsx
  - apps/web/src/components/runs/run-status-badge.tsx
  - apps/web/src/components/runs/runs-table.tsx
  - apps/web/src/components/runs/runs-page-header.tsx
  - apps/web/src/hooks/use-promotion-runs.ts
  - apps/web/src/lib/query-keys.ts
  - apps/web/src/lib/actor.ts
  - apps/web/src/__tests__/components/run-status-badge.test.tsx
  - apps/web/src/__tests__/helpers/render.tsx
  - apps/web/src/__tests__/integration/runs-list.test.tsx
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - "Operator visiting /runs sees a table of active and historical promotion runs"
    - "Each row shows status badge, flag key, pipeline name, and current environment/stage"
    - "Rows link to /runs/[id] for detail navigation"
    - "Root / redirects to /runs (D-10)"
  artifacts:
    - path: "apps/web/src/app/runs/page.tsx"
      provides: "Runs list page (UI-01, D-10)"
      contains: "usePromotionRuns"
    - path: "apps/web/src/components/runs/runs-table.tsx"
      provides: "Sortable/filterable runs table"
    - path: "apps/web/src/components/runs/run-status-badge.tsx"
      provides: "Status → shadcn Badge mapping"
    - path: "apps/web/src/hooks/use-promotion-runs.ts"
      provides: "TanStack Query list fetch"
      pattern: "listPromotionRuns"
  key_links:
    - from: "apps/web/src/hooks/use-promotion-runs.ts"
      to: "apps/web/src/lib/api-client.ts"
      via: "createApiClient().listPromotionRuns"
      pattern: "listPromotionRuns"
    - from: "apps/web/src/components/runs/runs-table.tsx"
      to: "/runs/[id]"
      via: "Next.js Link"
      pattern: "href=.*runs/"
---

<objective>
Deliver the promotion runs list page (UI-01): operators view active and historical runs with environment stage context from the list API.

Purpose: UI-01 primary surface; D-10 requires /runs with status, flag key, pipeline, current stage/environment.
Output: `/runs` page with RunsTable, RunStatusBadge, query hook, component and integration tests.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-operator-dashboard/06-CONTEXT.md
@.planning/phases/06-operator-dashboard/06-PATTERNS.md
@apps/web/src/lib/api-client.ts
@packages/contracts/src/api.ts
@packages/contracts/src/promotion-run.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Query keys, actor constant, and usePromotionRuns hook</name>
  <files>apps/web/src/lib/query-keys.ts, apps/web/src/lib/actor.ts, apps/web/src/hooks/use-promotion-runs.ts</files>
  <action>
  query-keys.ts: factory keys promotionRuns.all, promotionRuns.list(status?), promotionRun.detail(id), promotionRun.gateResults(id), promotionRun.auditEvents(id), pipelines.list.
  actor.ts (D-16): export dashboardActor(actorId?) returning { actorType: 'user', actorId: process.env.NEXT_PUBLIC_DASHBOARD_ACTOR_ID ?? 'dashboard' } — used in later waves; define now for shared import.
  use-promotion-runs.ts (D-04): 'use client' hook wrapping useQuery with queryKey queryKeys.promotionRuns.list(statusFilter), queryFn calling createApiClient({ baseUrl: process.env.NEXT_PUBLIC_API_URL ?? '/api/ff-promo' }).listPromotionRuns(statusFilter). Optional status filter via searchParams later — v1 fetch all recent runs. refetchInterval: 30_000 for list page (light refresh; detail page handles active polling per D-13). staleTime 10_000.
  Export hook return { runs, isLoading, isError, error, refetch }.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project web 2>/dev/null || pnpm --filter @ff-promo/web run build</automated>
  </verify>
  <done>Hook compiles and uses api-client list endpoint with TanStack Query.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RunStatusBadge and RunsTable components (D-03, D-10)</name>
  <files>apps/web/src/components/runs/run-status-badge.tsx, apps/web/src/components/runs/runs-table.tsx, apps/web/src/components/runs/runs-page-header.tsx, apps/web/src/__tests__/components/run-status-badge.test.tsx, apps/web/src/__tests__/helpers/render.tsx</files>
  <behavior>
    - Test: RunStatusBadge renders 'paused' text with secondary/destructive variant
    - Test: RunStatusBadge maps all PromotionStatus values from contracts without local enum duplication
    - Test: RunsTable renders flagKey, pipelineName, currentEnvironment columns from fixture PromotionRunListItem
  </behavior>
  <action>
  render.tsx test helper: wrap UI in QueryClientProvider with retry: false (per 06-PATTERNS).
  run-status-badge.tsx: import PromotionStatus from @ff-promo/contracts; map pending→outline, active→default, paused→secondary, completed→outline, aborted→destructive using shadcn Badge (D-03).
  runs-table.tsx: shadcn Table with columns: Status (RunStatusBadge), Flag Key, Pipeline (pipelineName or pipelineId fallback), Stage (currentStageDisplayName + currentEnvironment), Updated (relative or ISO), Actions (Link to /runs/[id]). Handle empty state message "No promotion runs yet" with link to /runs/new. Loading: Skeleton rows.
  runs-page-header.tsx: title "Promotion Runs", subtitle, Button link "New Run" → /runs/new.
  Import PromotionRunListItem type from contracts — do not redefine list shape.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project web -t "RunStatusBadge"</automated>
  </verify>
  <done>Status badge and table components tested; table renders list item fields.</done>
</task>

<task type="auto">
  <name>Task 3: /runs page wiring and MSW list integration test (UI-01)</name>
  <files>apps/web/src/app/runs/page.tsx, apps/web/src/__tests__/integration/runs-list.test.tsx, apps/web/src/__tests__/mocks/handlers.ts</files>
  <action>
  runs/page.tsx: 'use client' page composing RunsPageHeader + RunsTable fed by usePromotionRuns(). Show error alert with message on isError. Page title in document via next/head or layout metadata if using server wrapper — prefer client page for query hook.
  Confirm app/page.tsx redirects to /runs (from 06-01).
  handlers.ts (MSW): http.get('/api/ff-promo/v1/promotion-runs', () => HttpResponse.json({ runs: [fixture paused run with pipelineName, currentEnvironment] })).
  runs-list.test.tsx (D-18): setup MSW server in test; render page with render helper; expect screen.getByText flagKey and environment column; expect link href /runs/run-1.
  Extend api-client.ts if listPromotionRuns return type not yet PromotionRunListResponse — align with contracts from 06-01.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project web -t "runs list"</automated>
  </verify>
  <done>Operator can open /runs and see mocked runs list with navigation to detail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → BFF list endpoint | Read-only; no mutation surface in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information Disclosure | RunsTable | accept | Run metadata is intended operator view; auth at API layer |
| T-06-05 | Tampering | XSS in flagKey/pipelineName | mitigate | React text escaping; no dangerouslySetInnerHTML |
</threat_model>

<verification>
- pnpm exec vitest run --project web
- Manual: pnpm --filter @ff-promo/web dev → visit http://localhost:3001/runs with API running
</verification>

<success_criteria>
- UI-01: operators view active/historical runs with current environment stage on /runs
- List consumes GET /v1/promotion-runs via api-client and TanStack Query (D-04, D-10)
</success_criteria>

<output>
Create `.planning/phases/06-operator-dashboard/06-02-SUMMARY.md` when done
</output>
