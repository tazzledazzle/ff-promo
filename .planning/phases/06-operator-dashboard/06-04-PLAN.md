---
phase: 06-operator-dashboard
plan: 04
type: execute
wave: 3
depends_on:
  - "06-03"
files_modified:
  - apps/web/src/app/runs/new/page.tsx
  - apps/web/src/app/runs/[id]/run-detail.tsx
  - apps/web/src/components/runs/run-control-bar.tsx
  - apps/web/src/components/runs/confirm-abort-dialog.tsx
  - apps/web/src/hooks/use-run-mutations.ts
  - apps/web/src/lib/api-client.ts
  - apps/web/src/__tests__/components/confirm-abort-dialog.test.tsx
  - apps/web/src/__tests__/components/run-control-bar.test.tsx
  - apps/web/src/__tests__/integration/control-actions.test.tsx
  - apps/web/src/__tests__/integration/create-run.test.tsx
  - apps/web/src/__tests__/mocks/handlers.ts
  - docker-compose.yml
  - README.md
  - .env.example
autonomous: true
requirements:
  - UI-03
  - SAFE-02
must_haves:
  truths:
    - "Operator can start pending runs, pause/resume active/paused runs from detail control bar (UI-03, D-09)"
    - "Abort requires confirmation dialog with destructive styling before POST (SAFE-02, D-14)"
    - "Abort hidden/disabled for pending runs; API 409 errors surfaced to operator without optimistic override"
    - "Operator can create pending run at /runs/new with pipeline picker and flag key, then navigate to detail (D-12)"
    - "Mutations invalidate both list and detail query keys"
  artifacts:
    - path: "apps/web/src/components/runs/run-control-bar.tsx"
      provides: "State-gated start/pause/resume/abort actions (UI-03)"
    - path: "apps/web/src/components/runs/confirm-abort-dialog.tsx"
      provides: "SAFE-02 confirmation gate"
    - path: "apps/web/src/hooks/use-run-mutations.ts"
      provides: "Control mutations with 409 handling"
    - path: "apps/web/src/app/runs/new/page.tsx"
      provides: "Create run flow (D-12)"
  key_links:
    - from: "apps/web/src/hooks/use-run-mutations.ts"
      to: "POST /v1/promotion-runs/:id/abort"
      via: "api.abortRun with dashboardActor"
      pattern: "abortRun"
    - from: "apps/web/src/components/runs/confirm-abort-dialog.tsx"
      to: "run-control-bar"
      via: "onConfirm triggers abort mutation"
      pattern: "Abort promotion"
---

<objective>
Complete operator control plane in the dashboard: promotion actions, emergency abort with confirmation, create-run flow, and test coverage (UI-03, SAFE-02).

Purpose: Operators control promotions from the web UI per project v1 requirements; D-14/D-16/D-17/D-18 close safety and testing decisions.
Output: RunControlBar, ConfirmAbortDialog, /runs/new, mutation hooks, RTL + MSW tests, README Phase 6 docs.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-operator-dashboard/06-CONTEXT.md
@.planning/phases/06-operator-dashboard/06-PATTERNS.md
@apps/api/src/__tests__/promotion-runs.control.test.ts
@apps/web/src/app/runs/[id]/run-detail.tsx
@apps/web/src/lib/actor.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Control mutations and RunControlBar with abort dialog (UI-03, SAFE-02, D-09, D-14, D-16)</name>
  <files>apps/web/src/hooks/use-run-mutations.ts, apps/web/src/components/runs/run-control-bar.tsx, apps/web/src/components/runs/confirm-abort-dialog.tsx, apps/web/src/app/runs/[id]/run-detail.tsx, apps/web/src/lib/api-client.ts, apps/web/src/__tests__/components/confirm-abort-dialog.test.tsx, apps/web/src/__tests__/components/run-control-bar.test.tsx</files>
  <behavior>
    - Test: ConfirmAbortDialog requires explicit confirm click before onConfirm fires (D-14)
    - Test: RunControlBar shows Start only when status pending; Pause+Abort when active; Resume+Abort when paused; no buttons when completed/aborted
    - Test: RunControlBar does not show Abort when status pending (pitfall 5)
    - Test: pause mutation onError displays 409 message via isConflictError
  </behavior>
  <action>
  Extend api-client: createPromotionRun, startRun(id, actor), pauseRun, resumeRun, abortRun — POST bodies with ControlActionRequest / actor per contracts. All mutations include dashboardActor() (D-16).
  use-run-mutations.ts: useMutation wrappers per action; onSuccess invalidate queryKeys.promotionRun.detail(id) AND queryKeys.promotionRuns.all (pitfall 3). onError: if isConflictError show toast or inline Alert with err.message — no optimistic status update (D-09).
  confirm-abort-dialog.tsx (SAFE-02, D-14): shadcn AlertDialog destructive variant; title "Abort promotion?"; description warns immediate stop; Cancel + "Abort promotion" confirm button.
  run-control-bar.tsx: action matrix per 06-PATTERNS — pending→Start; active→Pause+Abort; paused→Resume+Abort; terminal→read-only message. Abort opens ConfirmAbortDialog then calls abortRun mutation. Disable buttons while mutation pending. Use destructive Button variant for Abort.
  Wire RunControlBar into run-detail.tsx replacing placeholder.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project web -t "AbortConfirm|RunControlBar|confirm-abort"</automated>
  </verify>
  <done>Control bar state-gated; abort requires confirmation; 409 errors handled.</done>
</task>

<task type="auto">
  <name>Task 2: Create run page with pipeline picker (D-12, UI-03)</name>
  <files>apps/web/src/app/runs/new/page.tsx, apps/web/src/hooks/use-pipelines.ts, apps/web/src/lib/api-client.ts</files>
  <action>
  use-pipelines.ts: useQuery listPipelines via GET /v1/pipelines from 06-01; queryKey queryKeys.pipelines.list.
  runs/new/page.tsx: 'use client' form with Select/combobox of pipelines (name + flagKey label), flagKey Input pre-filled from selected pipeline default but editable, Submit calls createPromotionRun({ pipelineId, flagKey, actor: dashboardActor() }). On success router.push(/runs/${run.id}) — operator starts from detail Start button (or optional immediate start via second button; minimum is create then navigate per D-12). Validate required fields client-side with zod or HTML required. Error alert on API failure.
  Link from runs-page-header already points here from 06-02.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project web -t "create run" || pnpm --filter @ff-promo/web run build</automated>
  </verify>
  <done>/runs/new creates pending run and navigates to detail page.</done>
</task>

<task type="auto">
  <name>Task 3: MSW integration tests, docker-compose web service, README (D-17, D-18)</name>
  <files>apps/web/src/__tests__/integration/control-actions.test.tsx, apps/web/src/__tests__/integration/create-run.test.tsx, apps/web/src/__tests__/mocks/handlers.ts, docker-compose.yml, README.md, .env.example</files>
  <action>
  Expand MSW handlers for POST start/pause/resume/abort returning 200 or 409 fixtures mirroring promotion-runs.control.test.ts expectations.
  control-actions.test.tsx (D-18): render RunControlBar or RunDetail with active run; click Pause; assert handler called; render abort flow — open dialog, confirm, assert POST abort.
  create-run.test.tsx: mock pipelines list + POST create; submit form; assert navigation mock called with new run id.
  docker-compose.yml: add web service optional profile dev — build apps/web, ports 3001:3001, env API_URL=http://api:3000, depends_on api (Claude discretion per CONTEXT).
  README.md: add Phase 6 section — pnpm --filter @ff-promo/web dev, env vars table, pages /runs /runs/new /runs/[id], test command pnpm exec vitest run --project web. Document BFF proxy pattern and that API_KEY stays server-side.
  Ensure all D-17 widget tests from prior plans still pass in full web suite.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project web</automated>
  </verify>
  <done>Full web test suite green; README documents dashboard dev workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → BFF → API control POST | Destructive abort crosses trust boundary; requires confirmation + API auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Elevation | abort without confirm | mitigate | ConfirmAbortDialog blocking confirm (D-14, SAFE-02) |
| T-06-09 | Tampering | CSRF on control POST | mitigate | Same-origin BFF proxy; Phase 7 adds session CSRF |
| T-06-10 | Repudiation | control actions | transfer | API audit trail records actor from dashboardActor (D-16) |
| T-06-11 | Denial of Service | double-submit abort | mitigate | Disable buttons while mutation pending |
</threat_model>

<verification>
- pnpm exec vitest run --project web
- pnpm test (full monorepo suite)
- Manual: create run → start → pause → resume → abort with confirmation
</verification>

<success_criteria>
- UI-03: start, pause, resume, abort available from dashboard with API error surfacing
- SAFE-02: emergency abort reachable with confirmation dialog on active/paused runs
- D-12 create flow at /runs/new; D-17/D-18 tests in vitest web project
</success_criteria>

<output>
Create `.planning/phases/06-operator-dashboard/06-04-SUMMARY.md` when done
</output>
