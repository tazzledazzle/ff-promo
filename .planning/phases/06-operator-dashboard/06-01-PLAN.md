---
phase: 06-operator-dashboard
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/tsconfig.json
  - apps/web/postcss.config.mjs
  - apps/web/components.json
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/page.tsx
  - apps/web/src/app/globals.css
  - apps/web/src/app/api/ff-promo/[[...path]]/route.ts
  - apps/web/src/components/providers/query-provider.tsx
  - apps/web/src/lib/api-client.ts
  - apps/web/src/lib/api-errors.ts
  - apps/web/src/lib/env.ts
  - apps/web/src/__tests__/setup.ts
  - apps/web/src/__tests__/api-client.test.ts
  - vitest.config.ts
  - packages/contracts/src/api.ts
  - packages/contracts/src/index.ts
  - packages/db/src/repositories/promotion-run.repository.ts
  - packages/db/src/repositories/pipeline.repository.ts
  - apps/api/src/routes/promotion-runs.ts
  - apps/api/src/routes/pipelines.ts
  - apps/api/src/services/promotion-run.service.ts
  - apps/api/src/__tests__/promotion-runs.list.test.ts
  - apps/api/src/__tests__/pipelines.list.test.ts
  - .env.example
  - pnpm-lock.yaml
autonomous: true
requirements:
  - UI-01
user_setup: []
must_haves:
  truths:
    - "Next.js 16 App Router builds in apps/web on port 3001 with Tailwind v4 and shadcn/ui primitives"
    - "GET /v1/promotion-runs returns recent runs with pipeline stage metadata for list display (D-07)"
    - "GET /v1/pipelines returns active pipelines for create-run picker"
    - "Vitest web project runs; typed api-client fetches through same-origin BFF proxy without exposing API_KEY (D-05, D-15, D-19)"
  artifacts:
    - path: "apps/web/src/app/layout.tsx"
      provides: "Root layout with QueryClientProvider (D-01, D-04)"
      contains: "QueryProvider"
    - path: "apps/web/src/lib/api-client.ts"
      provides: "Typed REST client against @ff-promo/contracts (D-05)"
      exports: ["createApiClient"]
    - path: "apps/web/src/app/api/ff-promo/[[...path]]/route.ts"
      provides: "Server-side API proxy injecting X-API-Key (D-15)"
    - path: "packages/contracts/src/api.ts"
      provides: "PromotionRunListResponseSchema and PipelineListResponseSchema"
      contains: "PromotionRunListItemSchema"
    - path: "apps/api/src/routes/promotion-runs.ts"
      provides: "GET /v1/promotion-runs list route"
      contains: "promotion-runs"
  key_links:
    - from: "apps/web/src/lib/api-client.ts"
      to: "/api/ff-promo/v1/promotion-runs"
      via: "fetch with NEXT_PUBLIC_API_URL default /api/ff-promo"
      pattern: "v1/promotion-runs"
    - from: "apps/web/src/app/api/ff-promo/[[...path]]/route.ts"
      to: "apps/api"
      via: "server fetch to API_URL with X-API-Key"
      pattern: "API_KEY"
    - from: "apps/api/src/routes/promotion-runs.ts"
      to: "packages/db"
      via: "PromotionRunRepository.findRecent"
      pattern: "findRecent"
---

<objective>
Scaffold the Next.js 16 operator dashboard shell, close Phase 5 API list gaps, and establish the typed API client + vitest web project — unblocking list and detail pages.

Purpose: D-01–D-07 require a real web app and list endpoints deferred from Phase 5; executors need contracts, proxy auth, and test harness before UI waves.
Output: Runnable `pnpm --filter @ff-promo/web dev`, list API routes with tests, api-client lib, empty web vitest project passing smoke test.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-operator-dashboard/06-CONTEXT.md
@.planning/phases/06-operator-dashboard/06-RESEARCH.md
@.planning/phases/06-operator-dashboard/06-PATTERNS.md
@apps/api/src/routes/promotion-runs.ts
@apps/api/src/routes/pipelines.ts
@packages/contracts/src/api.ts
@apps/api/package.json
@apps/web/package.json
@vitest.config.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Next.js 16 app shell with Tailwind v4 and shadcn/ui</name>
  <files>apps/web/package.json, apps/web/next.config.ts, apps/web/tsconfig.json, apps/web/postcss.config.mjs, apps/web/components.json, apps/web/src/app/layout.tsx, apps/web/src/app/page.tsx, apps/web/src/app/globals.css, apps/web/src/components/providers/query-provider.tsx, pnpm-lock.yaml</files>
  <action>
    Replace the tsx placeholder shell (D-01): delete apps/web/src/index.ts; update package.json scripts to next dev --port 3001, next build, test via vitest web project, lint via biome check src.
    Add dependencies per RESEARCH Standard Stack: next@16.2.9, react@19.2.7, react-dom@19.2.7, @tanstack/react-query@5.101.0, tailwindcss@4.3.1, @tailwindcss/postcss@4.3.1, @ff-promo/contracts workspace:*, zod@4.4.3, class-variance-authority, clsx, tailwind-merge, lucide-react.
    Configure next.config.ts with transpilePackages: ['@ff-promo/contracts'] if needed for monorepo imports.
    Init shadcn/ui for Next.js + Tailwind v4 (D-03): components.json with empty tailwind config string; globals.css uses @import "tailwindcss" and shadcn CSS variables. Add primitives: table, badge, button, dialog, card, alert-dialog, skeleton.
    Create layout.tsx wrapping children in QueryProvider (D-04) — client component with QueryClientProvider, staleTime ~30s default.
    Create page.tsx server redirect to /runs (placeholder target until 06-02).
    Match Biome conventions: single quotes, 2-space indent, semicolons (D-02).
    Run pnpm install from repo root.
  </action>
  <verify>
    <automated>pnpm install && pnpm --filter @ff-promo/web run build</automated>
  </verify>
  <done>next build succeeds; old tsx entry removed; shadcn ui/ components exist under apps/web/src/components/ui/.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: List API endpoints, contracts, and repository methods (D-07)</name>
  <files>packages/contracts/src/api.ts, packages/contracts/src/index.ts, packages/db/src/repositories/promotion-run.repository.ts, packages/db/src/repositories/pipeline.repository.ts, apps/api/src/routes/promotion-runs.ts, apps/api/src/routes/pipelines.ts, apps/api/src/services/promotion-run.service.ts, apps/api/src/__tests__/promotion-runs.list.test.ts, apps/api/src/__tests__/pipelines.list.test.ts</files>
  <behavior>
    - Test: GET /v1/promotion-runs returns 200 with runs array ordered by updatedAt desc
    - Test: GET /v1/promotion-runs?status=active filters to active runs only
    - Test: List items include pipelineName, currentEnvironment, currentStageDisplayName derived from pipeline stages at currentStageIndex (no N+1 from dashboard)
    - Test: GET /v1/pipelines returns active pipelines with id, name, flagKey, stageCount
  </behavior>
  <action>
  Add to packages/contracts/src/api.ts per RESEARCH list shape:
    PromotionRunListQuerySchema (status optional, limit optional default 50 max 100)
    PromotionRunListItemSchema (id, status, flagKey, pipelineId, pipelineName optional, currentStageIndex, currentEnvironment optional, currentStageDisplayName optional, pauseReason nullable optional, createdAt, updatedAt)
    PromotionRunListResponseSchema ({ runs: PromotionRunListItemSchema[] })
    PipelineListItemSchema (id, name, flagKey, stageCount)
    PipelineListResponseSchema ({ pipelines: PipelineListItemSchema[] })
  Export inferred types from index.ts.
  PromotionRunRepository: add findRecent({ status?, limit? }) with orderBy updatedAt desc, include pipeline + stages for stage label join.
  PipelineRepository: add listActive() returning id, name, flagKey, stages count (where isActive true).
  promotion-runs.ts: register GET /v1/promotion-runs BEFORE /:id route; wire to service.listRuns(query).
  service.listRuns: map DB rows to PromotionRunListItemSchema using mapPromotionRun + stage lookup at currentStageIndex.
  pipelines.ts: register GET / at prefix /v1/pipelines (list) BEFORE /:id; return PipelineListResponseSchema.
  Write fastify.inject tests with mock DB or testcontainers pattern matching promotion-runs.read.test.ts — do NOT skip list endpoint (D-07 locked).
  </action>
  <verify>
    <automated>pnpm --filter @ff-promo/contracts run build && pnpm exec vitest run --project api promotion-runs.list pipelines.list</automated>
  </verify>
  <done>List endpoints return Zod-validated responses; API tests pass; contracts exported.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest web project, api-client, and BFF proxy (D-05, D-06, D-15, D-19)</name>
  <files>vitest.config.ts, apps/web/src/__tests__/setup.ts, apps/web/src/__tests__/api-client.test.ts, apps/web/src/lib/api-client.ts, apps/web/src/lib/api-errors.ts, apps/web/src/lib/env.ts, apps/web/src/app/api/ff-promo/[[...path]]/route.ts, .env.example, apps/web/package.json</files>
  <action>
  Add vitest web project (D-19): root vitest.config.ts project name web, root ./apps/web, environment jsdom, setupFiles ./src/__tests__/setup.ts, include src/**/*.test.ts(x), devDependency @vitejs/plugin-react@6.0.2.
  setup.ts: import @testing-library/jest-dom/vitest.
  env.ts: WebEnvSchema with API_URL default http://localhost:3000, NEXT_PUBLIC_API_URL default /api/ff-promo, API_KEY optional server-only, DASHBOARD_ACTOR_ID default dashboard — never NEXT_PUBLIC_API_KEY (D-15).
  api-errors.ts: ApiClientError class with status and code; isConflictError helper for 409.
  api-client.ts (D-05): createApiClient({ baseUrl }) with methods listPromotionRuns, listPipelines, getPromotionRun, listGateResults, listAuditEvents — typed returns from @ff-promo/contracts. Parse error JSON { error, message } matching apps/api/src/app.ts handler. No secrets in client bundle.
  Route Handler catch-all apps/web/src/app/api/ff-promo/[[...path]]/route.ts (D-06 allows BFF when needed): proxy GET/POST/PUT/PATCH/DELETE to API_URL/v1/... + search; inject X-API-Key from process.env.API_KEY; strip host header; return response body/status. Browser uses NEXT_PUBLIC_API_URL=/api/ff-promo (same-origin, no CORS).
  api-client.test.ts: stub global fetch; verify listPromotionRuns builds correct path and throws ApiClientError on 401.
  Update .env.example with API_URL, NEXT_PUBLIC_API_URL, API_KEY, NEXT_PUBLIC_DASHBOARD_ACTOR_ID.
  Add web devDependencies: vitest, @testing-library/react, @testing-library/jest-dom, jsdom, msw (for later waves).
  </action>
  <verify>
    <automated>pnpm exec vitest run --project web && pnpm --filter @ff-promo/web run build</automated>
  </verify>
  <done>Web vitest project passes smoke test; api-client exports typed methods; BFF proxy route exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js BFF | Untrusted client; must not receive API_KEY |
| BFF → Fastify API | Server-side; injects X-API-Key when configured |
| API → PostgreSQL | List queries return run metadata only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | api-client / env | mitigate | API_KEY only in Route Handler server env; never NEXT_PUBLIC_* for secrets (D-15) |
| T-06-02 | Tampering | BFF proxy | mitigate | Forward only v1 paths; do not expose arbitrary upstream URLs; validate API_URL at startup |
| T-06-03 | Elevation | List endpoints | accept | Same auth preHandler as Phase 5; RBAC deferred Phase 7 |
| T-06-SC | Tampering | npm installs | mitigate | RESEARCH Package Legitimacy Audit all OK; no [SUS]/[SLOP] packages |
</threat_model>

<verification>
- pnpm --filter @ff-promo/web run build
- pnpm exec vitest run --project web
- pnpm exec vitest run --project api promotion-runs.list pipelines.list
</verification>

<success_criteria>
- Next.js dashboard shell replaces tsx placeholder (D-01, D-02, D-03, D-04)
- GET /v1/promotion-runs and GET /v1/pipelines implemented with contracts (D-07)
- Typed api-client and BFF proxy ready for UI waves (D-05, D-06, D-15, D-19)
</success_criteria>

<output>
Create `.planning/phases/06-operator-dashboard/06-01-SUMMARY.md` when done
</output>
