---
phase: 06-operator-dashboard
plan: 03
type: execute
wave: 2
depends_on:
  - "06-02"
files_modified:
  - apps/web/src/app/runs/[id]/page.tsx
  - apps/web/src/app/runs/[id]/run-detail.tsx
  - apps/web/src/components/runs/gate-forensics-panel.tsx
  - apps/web/src/components/runs/gate-results-table.tsx
  - apps/web/src/components/runs/audit-events-list.tsx
  - apps/web/src/components/runs/run-stage-timeline.tsx
  - apps/web/src/hooks/use-promotion-run.ts
  - apps/web/src/hooks/use-pipeline.ts
  - apps/web/src/lib/polling.ts
  - apps/web/src/lib/api-client.ts
  - apps/web/src/__tests__/components/gate-forensics-panel.test.tsx
  - apps/web/src/__tests__/components/gate-results-table.test.tsx
  - apps/web/src/__tests__/integration/run-detail.test.tsx
autonomous: true
requirements:
  - UI-02
must_haves:
  truths:
    - "Operator on /runs/[id] sees run status, flag key, pipeline, and current stage/environment"
    - "When run is paused with gate failures, forensics panel shows metricType, verdict, threshold, observed values (D-08, UI-02)"
    - "Gate results history table shows pass/fail per evaluation"
    - "Audit events list shows chronological promotion actions"
    - "Active and paused runs poll every 5–10s; completed/aborted fetch once (D-13)"
  artifacts:
    - path: "apps/web/src/app/runs/[id]/run-detail.tsx"
      provides: "Client detail view with polling"
      contains: "usePromotionRun"
    - path: "apps/web/src/components/runs/gate-forensics-panel.tsx"
      provides: "Paused gate forensics display (UI-02)"
    - path: "apps/web/src/hooks/use-promotion-run.ts"
      provides: "Status-aware refetchInterval"
      pattern: "runPollIntervalMs"
    - path: "apps/web/src/lib/polling.ts"
      provides: "D-13 polling helper"
      exports: ["runPollIntervalMs"]
  key_links:
    - from: "apps/web/src/hooks/use-promotion-run.ts"
      to: "GET /v1/promotion-runs/:id"
      via: "api.getPromotionRun"
      pattern: "getPromotionRun"
    - from: "apps/web/src/components/runs/gate-forensics-panel.tsx"
      to: "gateForensics.results"
      via: "GateForensics type from contracts"
      pattern: "forensics.results"
---

<objective>
Build the run detail page with telemetry gate forensics, gate history, audit trail, and status-aware polling (UI-02).

Purpose: Operators diagnose pause-on-breach via gateForensics from API (D-08); D-11 detail layout without control actions yet (06-04).
Output: `/runs/[id]` with forensics panel, gate results table, audit list, stage timeline, polling hook.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-operator-dashboard/06-CONTEXT.md
@.planning/phases/06-operator-dashboard/06-PATTERNS.md
@apps/api/src/lib/forensics.ts
@apps/api/src/__tests__/promotion-runs.read.test.ts
@packages/contracts/src/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polling helper and usePromotionRun / usePipeline hooks (D-13)</name>
  <files>apps/web/src/lib/polling.ts, apps/web/src/hooks/use-promotion-run.ts, apps/web/src/hooks/use-pipeline.ts, apps/web/src/lib/api-client.ts</files>
  <action>
  polling.ts (D-13): runPollIntervalMs(status) returns 8000 for active|paused, false for completed|aborted|undefined; pending returns false on detail (static until started).
  use-promotion-run.ts: parallel useQueries or single useQuery for getPromotionRun(id) with refetchInterval callback reading query.state.data?.run.status through runPollIntervalMs. Additional useQuery for listGateResults(id) and listAuditEvents(id) with same poll interval when run non-terminal. queryKeys from 06-02.
  use-pipeline.ts: useQuery fetch GET /v1/pipelines/:id via api-client getPipeline(id) — add method if missing; enabled when run.pipelineId known. staleTime 60_000 (pipeline config static during run).
  Extend api-client with getPipeline(pipelineId) typed to pipeline detail response shape from apps/api/src/routes/pipelines.ts GET /:id.
  </action>
  <verify>
    <automated>pnpm --filter @ff-promo/web run build</automated>
  </verify>
  <done>Hooks compile; polling interval 8s for active/paused per D-13.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate forensics, gate results, and audit components (UI-02, D-11)</name>
  <files>apps/web/src/components/runs/gate-forensics-panel.tsx, apps/web/src/components/runs/gate-results-table.tsx, apps/web/src/components/runs/audit-events-list.tsx, apps/web/src/components/runs/run-stage-timeline.tsx, apps/web/src/__tests__/components/gate-forensics-panel.test.tsx, apps/web/src/__tests__/components/gate-results-table.test.tsx</files>
  <behavior>
    - Test: GateForensicsPanel renders metricType, verdict fail, threshold, observedValue from fixture matching promotion-runs.read.test.ts forensics shape
    - Test: GateForensicsPanel shows pauseReason header and stage displayName/environment
    - Test: Empty forensics results shows "No gate failures recorded" with pauseReason when manual pause (pitfall 4 from RESEARCH)
    - Test: GateResultsTable renders verdict and metricType columns for GateResultResponse[]
  </behavior>
  <action>
  gate-forensics-panel.tsx: accept GateForensics from contracts; render Card with pauseReason, stageIndex, displayName, environment; Table of results with metricType, verdict (Badge color pass/fail), threshold, observedValue, treatmentValue, controlValue, observedDelta, reason, evaluatedAt. Table-only display — no Recharts (UI-02 minimum per deferred scope).
  gate-results-table.tsx: full history from GateResultResponse[]; columns id/metricType/verdict/threshold/observedValue/evaluatedAt.
  audit-events-list.tsx: list AuditEventResponse chronologically (occurredAt desc in UI); show action, actorType/actorId, displayName.
  run-stage-timeline.tsx: horizontal step indicator from pipeline.stages marking currentStageIndex — use pipeline hook data + run.currentStageIndex.
  Copy test fixtures from apps/api/src/__tests__/promotion-runs.read.test.ts forensics assertions — do not re-query Prometheus (anti-pattern).
  </action>
  <verify>
    <automated>pnpm exec vitest run --project web -t "GateForensicsPanel|GateResultsTable"</automated>
  </verify>
  <done>Forensics and gate tables render API-shaped data with unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Run detail page shell and integration test (D-08, D-11)</name>
  <files>apps/web/src/app/runs/[id]/page.tsx, apps/web/src/app/runs/[id]/run-detail.tsx, apps/web/src/__tests__/integration/run-detail.test.tsx, apps/web/src/__tests__/mocks/handlers.ts</files>
  <action>
  page.tsx: server component reading params.id; render RunDetail client child with runId prop (or client page with useParams).
  run-detail.tsx (D-11): header with flagKey, RunStatusBadge, pipeline name, current stage label from pipeline stages[run.currentStageIndex]. Sections: RunStageTimeline, GateForensicsPanel when gateForensics present, GateResultsTable, AuditEventsList. Back link to /runs. Placeholder div for control bar ("Controls in next wave") OR omit action buttons entirely — 06-04 adds RunControlBar. Do NOT implement start/pause/resume/abort in this plan.
  MSW handlers: GET /v1/promotion-runs/:id returns paused run + gateForensics fixture; GET gate-results and audit-events arrays; GET /v1/pipelines/:id returns stages.
  run-detail.test.tsx: render RunDetail; assert forensics metric visible; assert audit action text.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project web -t "run detail"</automated>
  </verify>
  <done>Detail page displays forensics for paused run; integration test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser displays gate forensics | Renders API-assembled telemetry; no direct PromQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | XSS in forensics reason/pauseReason | mitigate | React default escaping; no dangerouslySetInnerHTML |
| T-06-07 | Denial of Service | Aggressive polling | mitigate | runPollIntervalMs disables poll for terminal states; 8s interval within D-13 bounds |
</threat_model>

<verification>
- pnpm exec vitest run --project web
- Forensics panel matches API gateForensics shape from paused run
</verification>

<success_criteria>
- UI-02: operators view gate pass/fail and metric values per run on detail page
- D-08 gateForensics from GET /v1/promotion-runs/:id rendered in forensics panel
- D-13 polling active for active/paused runs only
</success_criteria>

<output>
Create `.planning/phases/06-operator-dashboard/06-03-SUMMARY.md` when done
</output>
