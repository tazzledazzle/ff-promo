---
phase: 05-rest-api
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/promotion-control/package.json
  - packages/promotion-control/src/index.ts
  - packages/promotion-control/src/start-promotion-run.ts
  - packages/promotion-control/src/signal-promotion-run.ts
  - packages/promotion-control/src/signals.ts
  - apps/worker/package.json
  - apps/worker/src/lib/start-promotion-run.ts
  - apps/worker/src/scripts/start-run.ts
  - apps/worker/src/workflows/signals.ts
  - apps/worker/src/workflows/promotion.workflow.ts
  - apps/worker/src/__tests__/promotion.signals.test.ts
  - pnpm-lock.yaml
autonomous: true
requirements: []
must_haves:
  truths:
    - "@ff-promo/promotion-control exports startPromotionRun and signal helpers"
    - "Worker imports promotion-control; existing start-run tests still pass"
    - "Signals (pause/resume/abort/statusQuery) exported for API use"
  artifacts:
    - path: "packages/promotion-control/src/start-promotion-run.ts"
      provides: "Shared Temporal workflow start"
      exports: ["startPromotionRun"]
    - path: "packages/promotion-control/src/signal-promotion-run.ts"
      provides: "pause/resume/abort/query helpers"
      exports: ["signalPromotionRun", "queryPromotionStatus"]
  key_links:
    - from: "apps/worker/src/workflows/signals.ts"
      to: "packages/promotion-control/src/signals.ts"
      via: "re-export canonical signal definitions"
      pattern: "pauseSignal"
---

<objective>
Extract shared Temporal promotion control into `@ff-promo/promotion-control` so API and worker share one implementation of start + signal + query (D-06/D-07).

Purpose: Avoid duplicating Temporal client logic in API routes; Phase 4 `startPromotionRun` moves here.
Output: New workspace package; worker refactored to import from it.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-rest-api/05-CONTEXT.md
@.planning/phases/05-rest-api/05-RESEARCH.md
@apps/worker/src/lib/start-promotion-run.ts
@apps/worker/src/workflows/signals.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: promotion-control package scaffold</name>
  <files>packages/promotion-control/package.json, packages/promotion-control/tsconfig.json, packages/promotion-control/src/index.ts</files>
  <action>
    Create packages/promotion-control with dependencies: @temporalio/client@1.18.1, @ff-promo/contracts, @ff-promo/db.
    Export public API from index.ts. Add to pnpm workspace if not auto-discovered.
  </action>
  <verify>
    <automated>pnpm install && pnpm --filter @ff-promo/promotion-control run build</automated>
  </verify>
  <done>Package scaffold compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Move startPromotionRun and add signal helpers</name>
  <files>packages/promotion-control/src/start-promotion-run.ts, packages/promotion-control/src/signal-promotion-run.ts, packages/promotion-control/src/signals.ts</files>
  <action>
    Move startPromotionRun from worker (keep temporalClient injection for tests).
    Create signals.ts as **canonical** signal/query definitions using @temporalio/workflow defineSignal/defineQuery.
    Export: pauseSignal, resumeSignal, abortSignal, gatePassedSignal, gateFailedSignal, statusQuery.
    Refactor apps/worker/src/workflows/signals.ts to re-export from @ff-promo/promotion-control (workflow bundle must import from package).
    signalPromotionRun({ promotionRunId, action: 'pause'|'resume'|'abort', temporalClient? })
    queryPromotionStatus({ promotionRunId, temporalClient? }) → statusQuery shape
    Workflow handle: client.workflow.getHandle(workflowId) where workflowId = run.temporalWorkflowId ?? run.id.
  </action>
  <verify>
    <automated>pnpm --filter @ff-promo/promotion-control run build</automated>
  </verify>
  <done>Start and signal helpers exported.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor worker to use promotion-control</name>
  <files>apps/worker/package.json, apps/worker/src/workflows/signals.ts, apps/worker/src/workflows/promotion.workflow.ts, apps/worker/src/lib/start-promotion-run.ts, apps/worker/src/scripts/start-run.ts, apps/worker/src/__tests__/start-promotion-run.test.ts, apps/worker/src/__tests__/promotion.signals.test.ts</files>
  <action>
    Add @ff-promo/promotion-control dependency to worker.
    Replace worker start-promotion-run.ts with re-export from package (or thin wrapper).
    Refactor worker signals.ts to re-export from promotion-control; update promotion.workflow.ts imports.
    Update imports in start-run script and tests.
    Ensure worker signal and start tests still pass.
  </action>
  <verify>
    <automated>pnpm --filter @ff-promo/worker run build && pnpm exec vitest run --project worker start-promotion-run promotion.signals</automated>
  </verify>
  <done>Worker uses shared package without regression.</done>
</task>

</tasks>

<verification>
- pnpm --filter @ff-promo/promotion-control run build
- pnpm --filter @ff-promo/worker run build
</verification>

<success_criteria>
- Single source of truth for Temporal promotion control
- Worker start-run path unchanged behaviorally
</success_criteria>

<output>
Create `.planning/phases/05-rest-api/05-02-SUMMARY.md` when done
</output>
