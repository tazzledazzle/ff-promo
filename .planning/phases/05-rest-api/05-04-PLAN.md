---
phase: 05-rest-api
plan: 04
type: execute
wave: 2
depends_on:
  - "05-03"
files_modified:
  - apps/api/src/routes/promotion-runs.ts
  - apps/api/src/routes/pipelines.ts
  - apps/api/src/plugins/swagger.ts
  - apps/api/src/services/promotion-run.service.ts
  - apps/api/src/lib/forensics.ts
  - apps/api/src/__tests__/promotion-runs.read.test.ts
  - apps/api/src/__tests__/api.integration.test.ts
  - packages/contracts/src/api.ts
  - README.md
autonomous: true
requirements:
  - API-02
must_haves:
  truths:
    - "GET /v1/promotion-runs/:id returns status with optional live workflow overlay"
    - "GET .../gate-results returns gate evaluation history ordered by evaluatedAt"
    - "Paused run status includes gateForensics with metric values and stage context (SC-3)"
    - "OpenAPI spec served; README documents API endpoints and dev command"
  artifacts:
    - path: "apps/api/src/lib/forensics.ts"
      provides: "Gate forensics mapper for paused runs"
      exports: ["buildGateForensics"]
    - path: "apps/api/src/__tests__/api.integration.test.ts"
      provides: "E2E API flow test"
      contains: "API-02"
  key_links:
    - from: "apps/api/src/lib/forensics.ts"
      to: "packages/db"
      via: "GateResult + Stage join"
      pattern: "gateResult.findMany"
---

<objective>
Complete API-02 read endpoints, gate forensics on paused status (roadmap SC-3), OpenAPI docs, integration tests, and README Phase 5 section.

Purpose: Operators query promotion health and history programmatically; dashboard (Phase 6) will consume same routes.
Output: Read routes, forensics mapper, swagger plugin, integration test suite.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-rest-api/05-CONTEXT.md
@.planning/phases/05-rest-api/05-RESEARCH.md
@.planning/phases/05-rest-api/05-PATTERNS.md
@packages/db/src/repositories/gate-result.repository.ts
@packages/db/src/repositories/audit.repository.ts
@apps/worker/src/activities/evaluate-gate.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read routes and forensics mapper (API-02, SC-3)</name>
  <files>apps/api/src/lib/forensics.ts, apps/api/src/services/promotion-run.service.ts, apps/api/src/routes/promotion-runs.ts, packages/contracts/src/api.ts</files>
  <action>
    buildGateForensics(run, gateResults, stages):
    - When status paused: include latest failing GateResult rows per stage
    - Map metadata: treatmentValue, controlValue, observedDelta, reason, metricType, threshold
    - Include stage environment, orderIndex, displayName, pauseReason
    GET /v1/promotion-runs/:id → PromotionRunStatusResponseSchema
    GET /v1/promotion-runs/:id/gate-results → array of gate results
    GET /v1/promotion-runs/:id/audit-events → AuditRepository.findByRunId
    Optional queryPromotionStatus overlay when temporalWorkflowId set.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project api promotion-runs.read</automated>
  </verify>
  <done>Read routes return forensics on paused runs.</done>
</task>

<task type="auto">
  <name>Task 2: Pipeline read routes + Swagger</name>
  <files>apps/api/src/routes/pipelines.ts, apps/api/src/plugins/swagger.ts, apps/api/src/app.ts</files>
  <action>
    GET /v1/pipelines/:id — PipelineRepository.findById (404 if missing).
    Register swagger plugin with jsonSchemaTransform from Zod provider; UI at /documentation.
    Add swagger.routes.test.ts: fastify.inject GET /documentation/json returns 200 with openapi object.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project api swagger</automated>
  </verify>
  <done>OpenAPI spec generated from route schemas.</done>
</task>

<task type="auto">
  <name>Task 3: Integration tests + README Phase 5</name>
  <files>apps/api/src/__tests__/api.integration.test.ts, apps/api/src/__tests__/promotion-runs.read.test.ts, README.md</files>
  <action>
    api.integration.test.ts:
    - POST create → POST start (mock temporal) → GET status asserts active (D-18)
    - GET status on pending run (before start)
    - Seed gate results in DB for paused run; GET status asserts gateForensics present (SC-3)
    - GET gate-results returns policy count
    Tag tests API-02.
    Update README: Phase 5 REST API section, pnpm --filter @ff-promo/api dev, env vars, /documentation link.
    Note pipeline config deferred to Phase 7.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project api</automated>
  </verify>
  <done>Full api test suite green; docs updated.</done>
</task>

</tasks>

<verification>
- pnpm exec vitest run --project api
- pnpm exec vitest run --project worker start-promotion-run
- pnpm --filter @ff-promo/api run build
</verification>

<success_criteria>
- API-02 and SC-3 satisfied with automated tests
- OpenAPI docs available for Phase 6 dashboard consumer
</success_criteria>

<output>
Create `.planning/phases/05-rest-api/05-04-SUMMARY.md` when done
</output>
