---
phase: 05-rest-api
plan: 03
type: execute
wave: 1
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - apps/api/package.json
  - apps/api/src/app.ts
  - apps/api/src/lib/db.ts
  - apps/api/src/lib/temporal-client.ts
  - apps/api/src/plugins/auth.ts
  - apps/api/src/routes/promotion-runs.ts
  - apps/api/src/services/promotion-run.service.ts
  - apps/api/src/__tests__/promotion-runs.control.test.ts
  - packages/contracts/src/api.ts
autonomous: true
requirements:
  - API-01
must_haves:
  truths:
    - "POST /v1/promotion-runs creates pending run from pipelineId + flagKey"
    - "POST .../start calls startPromotionRun with request actor"
    - "POST .../pause|resume|abort send Temporal signals with 409 on invalid state"
    - "Optional API_KEY preHandler rejects unauthorized requests when configured"
  artifacts:
    - path: "apps/api/src/routes/promotion-runs.ts"
      provides: "API-01 control routes"
      contains: "start"
    - path: "apps/api/src/services/promotion-run.service.ts"
      provides: "DB + Temporal orchestration"
      exports: ["PromotionRunService"]
  key_links:
    - from: "apps/api/src/services/promotion-run.service.ts"
      to: "packages/promotion-control"
      via: "startPromotionRun signalPromotionRun"
      pattern: "startPromotionRun"
---

<objective>
Implement API-01 promotion run lifecycle control routes: create, start, pause, resume, abort — delegating to repositories and `@ff-promo/promotion-control`.

Purpose: Operators control promotions via REST; SAFE-02 abort exposed at API layer.
Output: Control routes with state validation, auth preHandler stub, route tests with mock Temporal.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-rest-api/05-CONTEXT.md
@.planning/phases/05-rest-api/05-PATTERNS.md
@packages/db/src/repositories/promotion-run.repository.ts
@packages/contracts/src/promotion-run.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB and Temporal client factories</name>
  <files>apps/api/src/lib/db.ts, apps/api/src/lib/temporal-client.ts, apps/api/src/plugins/auth.ts</files>
  <action>
    Add dependencies including @ff-promo/promotion-control workspace package.
    db.ts: createRequestDb() → prisma + repositories, disconnect in hook.
    temporal-client.ts: createTemporalClient() from env; support injection in buildApp opts.
    auth.ts: preHandler checks X-API-Key when API_KEY env set; attach actor { actorType: api_key, actorId } to request.
  </action>
  <verify>
    <automated>pnpm --filter @ff-promo/api run build</automated>
  </verify>
  <done>Infra libs ready for routes.</done>
</task>

<task type="auto">
  <name>Task 2: PromotionRunService control methods</name>
  <files>apps/api/src/services/promotion-run.service.ts, packages/contracts/src/api.ts</files>
  <action>
    Implement PromotionRunService:
    - createRun({ pipelineId, flagKey, actor }) → validate pipeline exists, PromotionRunRepository.create
    - startRun({ promotionRunId, actor }) → AuditRepository.append run_started with request actor (D-15), then startPromotionRun
    - pauseRun / resumeRun / abortRun({ promotionRunId, actor? }) → load run, validate status, signalPromotionRun; actor from body or auth preHandler (D-13)
    State matrix: pending→start only; active→pause/abort; paused→resume/abort; completed/aborted→409 on control.
    Add CreatePromotionRunRequestSchema and ControlActionRequestSchema (optional actor) to contracts/api.ts.
  </action>
  <verify>
    <automated>pnpm --filter @ff-promo/contracts run build</automated>
  </verify>
  <done>Service layer with state validation.</done>
</task>

<task type="auto">
  <name>Task 3: Control routes + tests (API-01, SAFE-02)</name>
  <files>apps/api/src/routes/promotion-runs.ts, apps/api/src/app.ts, apps/api/src/__tests__/promotion-runs.control.test.ts</files>
  <action>
    Register routes under /v1/promotion-runs with Zod schemas:
    POST / — create
    POST /:id/start — start
    POST /:id/pause — pause
    POST /:id/resume — resume
    POST /:id/abort — abort (SAFE-02)
    Tests with testcontainers DB + mock temporalClient:
    - create returns 201 pending run
    - start transitions to active (mock workflow.start); asserts run_started audit with request actor
    - active run → POST pause → 200; paused run → POST resume → 200
    - abort on active run sends abort signal (SAFE-02)
    - invalid transition returns 409
  </action>
  <verify>
    <automated>pnpm exec vitest run --project api promotion-runs.control</automated>
  </verify>
  <done>API-01 control routes tested.</done>
</task>

</tasks>

<verification>
- pnpm exec vitest run --project api promotion-runs.control
- pnpm --filter @ff-promo/api run build
</verification>

<success_criteria>
- Full promotion control lifecycle via REST
- Abort endpoint satisfies SAFE-02 at API layer
</success_criteria>

<output>
Create `.planning/phases/05-rest-api/05-03-SUMMARY.md` when done
</output>
