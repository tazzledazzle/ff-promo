---
phase: 05-rest-api
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/api/src/app.ts
  - apps/api/src/index.ts
  - apps/api/src/lib/env.ts
  - apps/api/src/routes/health.ts
  - apps/api/src/errors/api-error.ts
  - packages/contracts/src/api.ts
  - packages/contracts/src/index.ts
  - vitest.config.ts
  - pnpm-lock.yaml
autonomous: true
requirements: []
must_haves:
  truths:
    - "Fastify app builds with Zod type provider and serves GET /health"
    - "API contracts export request/response schemas for promotion runs"
    - "Vitest api project runs route tests via fastify.inject"
  artifacts:
    - path: "apps/api/src/app.ts"
      provides: "buildApp factory for server and tests"
      exports: ["buildApp"]
    - path: "packages/contracts/src/api.ts"
      provides: "REST API Zod schemas"
      exports: ["PromotionRunResponseSchema"]
  key_links:
    - from: "apps/api/src/app.ts"
      to: "@fastify/type-provider-zod"
      via: "validatorCompiler serializerCompiler"
      pattern: "validatorCompiler"
---

<objective>
Scaffold the Fastify API application with Zod validation, error handling, health route, and shared API contracts — establishing the testable `buildApp()` factory before routes land.

Purpose: Phase 5 starts from an empty API shell; D-10/D-11 require schema-first Fastify before promotion routes.
Output: Runnable API dev server, `api` vitest project, `packages/contracts/src/api.ts` stubs.
</objective>

<execution_context>
@/Users/terenceschumacher/dev/june-portfolio-projects/ff-promo/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-rest-api/05-CONTEXT.md
@.planning/phases/05-rest-api/05-RESEARCH.md
@.planning/phases/05-rest-api/05-PATTERNS.md
@apps/api/src/index.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: API package dependencies and env</name>
  <files>apps/api/package.json, apps/api/src/lib/env.ts, pnpm-lock.yaml</files>
  <action>
    Add dependencies: fastify@5.8.5, @fastify/type-provider-zod@1.0.0, @fastify/swagger@9.7.0, @fastify/swagger-ui@6.0.0, @ff-promo/contracts, @ff-promo/db, zod@4.4.3.
    Add devDependencies: vitest@4.1.9, tsx@^4.20.3.
    Create env.ts parsing PORT, DATABASE_URL, TEMPORAL_ADDRESS, TEMPORAL_TASK_QUEUE, API_KEY (optional).
    Run pnpm install.
  </action>
  <verify>
    <automated>pnpm install && pnpm --filter @ff-promo/api run build</automated>
  </verify>
  <done>API package deps installed and env parser compiles.</done>
</task>

<task type="auto">
  <name>Task 2: buildApp, error handler, health route</name>
  <files>apps/api/src/app.ts, apps/api/src/index.ts, apps/api/src/errors/api-error.ts, apps/api/src/routes/health.ts, apps/api/src/__tests__/health.routes.test.ts</files>
  <action>
    Create buildApp(opts?) factory:
    - Register Zod type provider (validatorCompiler, serializerCompiler)
    - Global error handler mapping ApiError → status codes (D-12)
    - GET /health → { status: 'ok' }
    Update index.ts to listen on PORT with graceful shutdown.
    Test health route with fastify.inject().
  </action>
  <verify>
    <automated>pnpm exec vitest run --project api health.routes</automated>
  </verify>
  <done>Health endpoint tested via inject.</done>
</task>

<task type="auto">
  <name>Task 3: API contracts and vitest project</name>
  <files>packages/contracts/src/api.ts, packages/contracts/src/index.ts, vitest.config.ts</files>
  <action>
    Create packages/contracts/src/api.ts with initial schemas:
    - ActorHeaderSchema (reuse ActorSchema fields)
    - PromotionRunResponseSchema (id, status, flagKey, pipelineId, currentStageIndex, pauseReason, temporalWorkflowId, timestamps)
    - GateForensicsSchema (stageIndex, environment, pauseReason, results[])
    - PromotionRunStatusResponseSchema (run + optional gateForensics + optional liveWorkflowStatus)
    Export from index.ts. Add vitest `api` project root apps/api.
  </action>
  <verify>
    <automated>pnpm --filter @ff-promo/contracts run build && pnpm exec vitest run --project api</automated>
  </verify>
  <done>Contracts and vitest api project configured.</done>
</task>

</tasks>

<verification>
- pnpm --filter @ff-promo/api run build
- pnpm exec vitest run --project api
</verification>

<success_criteria>
- Fastify scaffold with Zod type provider and health route
- API response contracts exported from @ff-promo/contracts
</success_criteria>

<output>
Create `.planning/phases/05-rest-api/05-01-SUMMARY.md` when done
</output>
