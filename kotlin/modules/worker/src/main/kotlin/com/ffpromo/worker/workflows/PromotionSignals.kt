package com.ffpromo.worker.workflows

/** Signal names matching v1 `@ff-promo/promotion-control` exports. */
object PromotionSignals {
    const val PAUSE = "pause"
    const val RESUME = "resume"
    const val ABORT = "abort"
}
